# Mario Golf 64 decomp sprint velocity (story points)

The story-point dashboard, activated 2026-06-11 by PO directive at the Sprint-5 review (the
"phase 2" named trigger in `CLAUDE.md`, fired at the ≈5-sprint mark). Scale = Fibonacci
1, 2, 3, 5, 8, 13. Scope = the current phase, the libultra/libkmc upstream-mirror band
(`regime: mirror`). The classical/game-code regime gets a separate track when it starts.
Never compare velocity across regimes (McConnell, *More Effective Agile*, Ch. 19).

Ported from `../marioparty7/VELOCITY.md` (23-sprint sibling system), but shipped in two
phases because MG64 differs from MP7: its cost axis is deterministic and its functions are
tiny and uniform (see `CLAUDE.md ## Story points`).

- **v1 (now): deterministic seed + the 8-point decompose gate.** The part with signal
  today. The seed is a pure function of `tools/pick_target.py`'s columns, so it is
  re-derivable: it needs no anti-retrofit freeze commit, and the plan gate creates no
  commit. The committed seed is logged here in the normal review/retro commit.
- **v2 (ACTIVE since Sprint 11, 2026-06-11): the realized-tier + residual + rolling/re-anchor
  machinery.** Dormant until the first classical or mixed sprint produced real residual
  variance to calibrate against; turning it on against the 5 identical verbatim mirror sprints
  would only measure a point mass. S11 (`func_800AB600`) was that sprint, a non-trivial
  classical leaf whose seed compiled at 0.80 and matched only after a fix-iteration, so the PO
  activated v2 at the S11 review. The realized tier is now scored per-sprint on the classical
  track; the mirror track stays seed-only (still a point mass there).

## Purpose: what the v1 number actually changes
1. **8-point decompose gate (primary).** A seed of 8 or 13 means do NOT commit it as a normal
   1-increment sprint. Decompose it (split the subseg at the upstream-file/function boundary)
   or pull a scaffolding enabler as the sprint goal. This catches an all-or-nothing bank stall
   *before* a sprint sinks into a too-big unit. Most remaining classical units seed 8/13 (see
   Regime), so this gate will fire on nearly every sprint once the mirror band is mined out.
   **Verbatim-mirror exemption (S64; generalized S69):** a size-only 8/13 verbatim `single-file-pack`
   mirror (every member fn from ONE upstream `.c`) with all callees placed + names curated may run as a
   normal sprint. It banks atomically, no iteration stall. Decompose is blocked because there is no
   inter-file split point, regardless of inner-boundary 16-alignment (S64 `lookathil` non16align,
   S69 `lookat` 16-aligned, both blocked). Full rule in `CLAUDE.md ## Story points`. First uses:
   S64 `gu/lookathil.c` + S69 `gu/lookat.c` (both pts-13, banked first-try).
2. **Regime indicator (primary).** A sustained seed/velocity shift flags the mirror→classical
   transition so the PO re-plans cadence and triggers v2.
3. **Forecasting, omitted in v1.** The backlog is overwhelmingly un-pointed classical work, so
   a remaining-points / velocity number would be a false deadline. Added with the classical
   track in v2, and even then "tertiary, noisy, not a deadline" (book Ch. 20).

## Seed rubric (deterministic; `tools/pick_target.py seed_points()`, the `pts` column)
Pure function of `size, upstream, band, nfns, hazards` plus a copyable-vs-blocked header classify.
Display-only: it does NOT change the smallest-first sort. A cluster seed = the sum of
its files' seeds; points bank per file. A spiked sibling scores 0 for that file only, not
the whole cluster, so sibling-pair batching is never punished.

```
blk  : a needs-header that can't be fixed by a companion-copy (unindexed -I / system header,
       e.g. <libaudio.h>, guint.h) → un-pickable, a DoR reject (not a cheap seed)
13   : size>=1536, or any classical multi-fn pack                  → must decompose
8    : nfns>=4 (large pack, any path), or a classical fn that is big/pack/drop/needs-copy
5    : a small single classical fn (no upstream, unproven, high variance)
mirror (has upstream): base = 1 if warm else 2;  +1 each for {static/data-drop, header-copy,
       needs-define, recover-extern, pack-or-big};  snap to Fibonacci (4→5, 6→8)
```
`drop` = `file-static`/`defines-data` (classical fallback, defs dropped). `warm` = the mirror
dir already holds a banked sibling (enabler-free). `big` = size≥768 B, `huge` = ≥1536 B. The
byte gates are dormant in the current <256 B regime and only bite for large/classical fns.

**S94 note: clean-atomic-mirror seed inflation (display-only, accepted as-is).** A coddog-matched
AUDIO mirror is NOT re-priced to `libultra`, since the S71 header-`-I`-gate carve-out left audio
header-blocked when the rule landed. So `seed_points` prices it as a classical multi-fn pack → 13
even when it is a zero-hazard verbatim `one-tu` cp (S94 `auxbus.c`, 272 B / 2 fn, seed 13). This
false 8-gate fire is deterministically absorbed by the S64/S69 verbatim-mirror exemption, and
pts is display-only (no smallest-first sort effect), so the seed is kept as-is: no down-weight.
The deeper root, that audio coddog COULD re-price to a mirror seed now that the header `-I` enabler
is paid (S15/S94), is a candidate future edit but deferred. It would re-seed every audio row, need a
golden regen, and need per-row vendorable-header FP analysis (reverb/drvrnew/env carry
`needs-header:*(vendorable)`), larger than this retro's flag-for-discussion scope.

## Realized tier + residual loop: ACTIVE (v2, since Sprint 11)
Scored at review from the RETRO facts: start at the seed band; +1 per
{stuck-far / permuter / re-attempt / novel bank-gotcha / mid-sprint split / carry-or-reopen};
−1 verbatim first-try ≤1 fix-iteration; per-file then summed. With it comes the two-pass
freeze: plan-time seed committed before `src/`, realized in a second commit. Caveat, do not
over-credit the freeze: it locks only the deterministic *seed*. The realized
tier is agent-scored and subjective, so the real anti-gaming guards are the per-file
all-or-nothing bank plus the quality counter-metric (stuck-far / permuter / carried / re-opened),
not the freeze. Scope: applied on the classical track only. The mirror track has no
residual variance to score (still a point mass), so it stays seed-only. S11 worked example:
seed 5, no escalations, single fix-iteration on a non-verbatim leaf → realized 5, residual 0.

**Near-verbatim reclassification (S119).** "Mirror track = seed-only" holds only for a TRUE
verbatim mirror (the verbatim cp banks first-build). A nominal mirror that SHA-MISSES on the
verbatim cp and needs an asm-driven diagnosis + hand-edit to land (a version drop, a cast fix, a
block reorder) carried real residual variance — so for THAT sprint's scoring it reclassifies to
the classical/near-verbatim track and is scored with the realized tier + residual (the
diagnosis+edit counts as a re-attempt, +1 over seed). A sprint mixing a true mirror with a
near-verbatim is logged `regime mixed`. Keep this distinct from the (version-artifact?) /
coddog-mirror cases, which the gate already prices and which still bank verbatim/clean-drop
seed-only. (S119 `nuContGBPakFread`: verbatim 2.07 cp SHA-missed → block-reorder hand-edit →
seed 2, realized 3, residual +1; logged on the classical track, not the mirror point mass.)

**Sibling-known refinement (S120).** The reclassification is PER-FN PROVENANCE, not a blanket
family tag. A near-verbatim whose fix is DISCOVERED in-sprint (S119, the re-attempt) scores +1; a
near-verbatim whose fix is already KNOWN from a banked sibling — surfaced by
`block-reorder-sibling:<file>` — and is applied UP-FRONT to bank FIRST-BUILD scores the −1
verbatim-first-try tier, exactly like a clean verbatim cp (the hand-edit is mechanical, 0
re-attempt). The plan-time seed stays conservative (`pick_target.py` keeps the +1 near-verbatim
risk; the hand-edit is real until applied), so the −1 shows up only as realized residual. (S120
`nuContGBPakFwrite`: the S119 sibling; swap applied up-front from the asm + precedent, banked
first-build 0 iteration → seed 3, realized 2, residual −1.)

## Bootstrap anchors (retro-pointed S1–5; the calibration loop, pre-run)
All `regime: mirror`, all tiny verbatim leaves (<256 B). `seed` = the rubric (a-priori, **live
in v1**). `(real)` is the v2 realized rule, shown *italic/illustrative* only — **not** logged
until v2. These rows double as the **reference stories** for the plan-time ±1 adjust.

| Sp | increment | nfns | path / band / hazard | seed | *(real)* | note |
|----|-----------|------|----------------------|------|----------|------|
| 1 | dp.c | 1 | mirror / cold / — | 2 | *2* | verbatim vs cpp-toolchain gotcha |
| 2 | createmesgqueue.c | 1 | mirror / cold / — | 2 | *2* | verbatim vs `__osThreadTail` asm-recovery |
| 3 | visetmode.c | 1 | mirror / warm / needs-copy | 2 | *1* | trivial `assert.h` copy |
| 4 | sirawread + sirawwrite | 1+1 | mirror / warm / — | 1+1=2 | *2* | both verbatim, warm floor 1 each |
| 5 | viswapbuf + visetevent | 1+1 | mirror / warm / — | 1+1=2 | *2* | both verbatim, warm floor 1 each |
| 6 | viblack | 1 | mirror / warm / — | 1 | *1* | first live-logged v1 sprint; verbatim, zero-enabler, banked 1pt |
| 7 | virtualtophysical | 1 | mirror / cold / — | 2 | *2* | opens convert band (vi exhausted); verbatim, zero-enabler, banked 2pt |
| 8 | stopthread | 1 | mirror / warm / — | 1 | *1* | opens thread band; verbatim, zero-enabler, banked 1pt |
| 9 | func_80099490 | 1 | **classical** / — / — | 5 | *—* | **first classical-track row** — no-upstream wrapper `nuPiInitSram()`; first-pass clean (score 0, 0 iter), banked 5pt; classical loop PROVEN. Kept OUT of the mirror seed-velocity (separate regime) |
| 10 | dpsetstat + dpctr | 1+1 | mirror / warm / — | 1+1=2 | *2* | 5th sibling-pair; split 0x86730 pack (osDpSetStatus+osDpGetCounters) at upstream-file boundary; both verbatim, zero-enabler, banked 2pt. 9th straight clean mirror |
| 11 | func_800AB600 | 1 | **classical** / — / — | 5 | **5** | **2nd classical-track row; v2 ACTIVATED here.** Non-trivial leaf (bit ops + branch + struct RMW). First classical sprint with **real residual variance**: seed compiled 0.80/score 400, matched after **1 fix-iteration** (register-reuse nudge). Realized=5 (seed band; no stuck-far/permuter/re-attempt/split/carry; not a verbatim mirror so the −1 first-try credit doesn't apply). Residual 0 — seed priced it right, but the loop *iterated*, so the realized tier finally has signal. `(real)` is now **logged, not illustrative** |
| 12 | yieldthread | 1 | mirror / warm / recover-extern | 2 | *2* | 2nd thread-band leaf (sibling of S8 stopthread); 10th straight clean mirror. seed 2 = warm-1 floor +1 for the `refs-unplaced` recover-extern enabler (`__osRunQueue`@0x800C8228, recovered at the S11 gate). Verbatim cp, 0 iterations, zero header copies, one symbol add. Mirror track stays seed-only — `(real)` illustrative |
| 13 | visetyscale | 1 | mirror / warm / — | 1 | *1* | 6th vi-band mirror (`osViSetYScale`). **Seed-miss event:** pick mis-seeded it **5** (mislabeled classical, un-named func_800AD370) — corrected to **1** at the gate after asm-vs-upstream confirmed the libultra mirror. Verbatim cp, 0 iterations, zero header copies, one symbol add. Motivated the S13 retro's pick_target signature-matcher + 3-lib scan. Mirror track stays seed-only — `(real)` illustrative |
| 14 | setthreadpri | 1 | mirror / warm / — | 1 | *1* | 3rd thread-band mirror (`osSetThreadPri`, after S8 stopthread + S12 yieldthread). Seed **correct first time** (pick said 1/mirror, no gate correction). Largest mirror banked to date (208 B, queue dequeue/enqueue + yield branch) yet still pts 1 → confirms byte-gate-dormant calibration. Verbatim cp, 0 iterations, name pre-curated, all 7 refs + 3 headers pre-placed — **one yaml flip the only enabler** (zero symbol add, zero header copy). Retro applied #3 open-band fast-path. Mirror track stays seed-only — `(real)` illustrative |
| 15 | nugfxswapcfb | 1 | mirror / cold / band-unlock | 3 | *3* | **1st libnusys row — opens the whole nuGfx*/nuCont* band.** seed 3 = cold-mirror floor 2 + 1 scaffolding-enabler (`-I include/libnusys` CFLAGS + verbatim `nusys.h`, deps `<ultra64.h>`+`<PR/gs2dex.h>` both in-tree; callee `osViSwapBuffer` banked S5). One paid-once enabler, then the band ranks as pts-2 cold mirrors. Verbatim cp, 0 iterations, name pre-curated (`nuGfxSwapCfb`@0x800A15E0 in ghidra_symbols → zero symbol add). Retro applied #1 (un-blk nusys ranker `-I`) + #2 (CLAUDE.md libnusys path table). Mirror track stays seed-only — `(real)` illustrative |
| 16 | nugfxtaskallendwait + nugfxdisplayoff | 1+1 | mirror / cold / recover-extern | 2+2=4 | *4* | **6th sibling-pair; 1st libnusys batch (S15-note).** Both verbatim cp, 0 iterations. Each needed a recover-extern (nuGfxTaskSpool=0x8012D478, nuGfxDisplay=0x80104E6C) — **non-`__` library globals pick_target's refs-unplaced grep missed → a false-clean** (seeded 2 each, no hazard, but actually recover-extern). Recovered deterministically at the gate (lui/lw + lui/sw). Callee `osViBlack` banked S6; names pre-curated in ghidra_symbols. Retro applied #1 (broaden refs-unplaced to header-declared non-`__` data externs — now flags nuGfxFunc/nuScPreNMIFunc etc.) + #2 (CLAUDE.md: mirror branch's proof IS the full-make SHA). Mirror track stays seed-only — `(real)` illustrative |
| 17 | nucontgbpak{getstatus,power,readid} | 1+1+1 | mirror / cold→warm / — | 2+2+2=6 | *6* | **7th sibling-batch (first trio); opens the nuCont sub-band warm.** All 3 verbatim cp, 0 iterations. **First nuGfx/nuCont leaves with NO data globals** — shared sole callee `nuSiSendMesg`@0x800A2824 (already in ghidra_symbols), struct types/constants/prototypes all in nusys.h → zero new symbols, zero header copies, zero splits, three yaml flips the only enabler. S16#1's broadened refs-unplaced grep correctly reported all 3 no-hazard (no false-clean this time); gate jal/lui scan confirmed truly clean. Retro applied 0 of 3 (all confirmatory observations). Mirror track stays seed-only — `(real)` illustrative |
| 18 | nucontinit | 1 | mirror / warm / jal-divergence | 2 | *2* | **near-verbatim mirror sub-case (1st):** upstream calls 4 managers, ROM's asm has only **3 jals** (no `nuContPakMgrInit` in this build). Not a missing symbol/def — an upstream-vs-ROM build **divergence**. Copied upstream verbatim, dropped the diverging line, banked on full-make SHA, 0 iteration. Caught at the gate by disassembling (pick reported no-hazard — its ref-grep counts data refs, not jal callees). Retro applied #1 (new `jal-count-mismatch` hazard in pick_target — verified it flags 4vs3) + #2 (CLAUDE.md near-verbatim-mirror bullet). Mirror track stays seed-only — `(real)` illustrative |
| 19 | nu{prenmi,gfx,gfxswapcfb}funcset | 1+1+1 | mirror / cold / recover-extern | 3+3+3=9 | *9* | **8th sibling-batch (2nd trio); the S15-note nuGfx\*FuncSet trio.** All 3 verbatim cp, 0 iterations. Each stores one callback global behind an `osSetIntMask` critical section; one recover-extern each (nuScPreNMIFunc=0x800B6780, nuGfxFunc=0x800C7DC0, nuGfxSwapCfbFunc=0x800B67B4) — S16#1's broadened refs-unplaced grep flagged all 3 correctly (no false-clean), vrams confirmed deterministically at the gate via each fn's own lui/addiu (3/3 matched pick's inlined vram). 3 fns + callee `nuGfxTaskAllEndWait` pre-curated in ghidra_symbols → only enabler = 3 symbol_addrs adds + 3 yaml flips. **First 3-file homogeneous batch banked at the ≤3-4 cap** (vs S4/S5's pairs). Retro applied #1 (CLAUDE.md recover-extern-mirror sub-case bullet) + #2 (CLAUDE.md fill-the-cap batch-sizing rule) + #3 (keep gate MCP re-confirm mandatory; 3/3 logged). Mirror track stays seed-only — `(real)` illustrative |
| 20 | nucont{rmbstart,gbpakopen} | 1+1 | mirror / cold / recover-extern | 3+3=6 | *6* | **9th sibling-batch (3rd recover-extern batch); 2nd libnusys pair.** Both verbatim cp, 0 iterations. One recover-extern each (nuContRmbCtl=0x80104F50, nuContPfs=0x801B8A18) behind shared callee `nuSiSendMesg` (banked S17). **First gate vram-miss on the inlined `refs-unplaced` vram:** pick reported nuContRmbCtl@0x80104F57 = the `.mode` field addr (offset 7) of an indexed-struct array — true array base 0x80104F50, recovered via the mandatory lui/addiu re-confirm (nuContPfs@0x801B8A18 matched exactly). Homogeneous pair; genuinely-clean set was exactly 2 (trimmed nuContDataGetEx — extra MISSING fn symbol nuContDataOpen). Retro applied #1 (CLAUDE.md field-addr-vs-base note + keep re-confirm mandatory) + #2 (CLAUDE.md array-extern size rule: scalar 0x4 / array stride×count). Mirror track stays seed-only — `(real)` illustrative |
| 21 | nugfxretracewait | 1 | mirror / warm / — | 2 | *2* | **First zero-hazard clean cp since the libnusys band opened (S15).** Verbatim cp, 0 iterations, name pre-curated, all refs + `nusys.h` pre-placed → **one yaml flip the only enabler** (zero symbol add, zero header copy, zero known-edit). Refutes the stale S11 "warm pool mined out" note — that predates the S15 libnusys unlock; this band still yields zero-enabler clean cp's, not just recover-extern fillers. Retro applied #1 (refresh BACKLOG PO note accordingly). Mirror track stays seed-only — `(real)` illustrative |
| 22 | epilinkhandle | 1 | mirror / warm / recover-extern | 3 | *3* | **First `nintendo/` variant-dir mirror** (prior libultra were `monegi/`/`shared/`); 1st libultra recover-extern since the band's mined-out note, refuting it like S21 did for libnusys. Verbatim cp, 0 iterations; one recover-extern (`__osPiTable`=0x800C7E8C, simple pointer global → size:0x4) behind `__osDisableInt`/`__osRestoreInt`, both pre-placed; `piint.h` in-tree → one symbol add + one yaml flip. **Process catch:** the mandatory re-confirm exposed a *target-fn* vram error — a hand-guessed flat `rom + 0x80020000` resolved mid-function and returned a wrong ~1000 B containing fn; real base is `rom + 0x80024C00` (= ghidra_symbols `osEPiLinkHandle`=0x800A3420). Retro applied #1 (CLAUDE.md: look up target vram, never guess a flat offset) + #2 (`pick_target.py` vram column). Mirror track stays seed-only — `(real)` illustrative |
| 23 | epidma | 1 | mirror / warm / recover-extern + calls-unplaced | 2 | *2* | **2nd `nintendo/pi/` mirror (sibling of S22 epilinkhandle).** Verbatim cp, 0 iterations. One recover-extern (`__osPiDevMgr`=0x800C7E70, OSDevMgr struct → size:0x1C, confirmed from the fn's own `lui 0x800c`/`lw 0x7e70` `.active` off0 + the 0x1C gap to placed `__osPiTable`). **New friction → the S23 fix:** the leaf ALSO calls an unplaced *function* (`osPiGetCmdQueue`=0x800B06F0) — pick's `refs-unplaced` *excludes anything called*, so this was a false-clean caught only at the **execution-middle link** (the INCLUDE_ASM gate-build resolves the jal directly → can't see the missing C symbol). Recovered from its jal target, add-only `// type:func`, re-extract+make → green SHA. Retro applied #1 (new `calls-unplaced:<fn>@0x<addr>` hazard — function dual of `refs-unplaced`, comment/string/dead-block-stripped; verified it flags `nuContDataGetEx`→`nuContDataOpen`@0x800A2CAC) + #2 (CLAUDE.md: reconcile the full data+function callee list at the gate). Mirror track stays seed-only — `(real)` illustrative |
| 24 | nucontdatagetex | 1 | mirror / cold / recover-extern + calls-unplaced | 3 | *3* | **First leaf carrying BOTH `refs-unplaced` AND `calls-unplaced` simultaneously** — the S20 data-recover and S23 function-dual doctrines composed cleanly, no special handling. Verbatim cp, 0 iterations. Two gate recoveries from one disassemble pass: data `nuContData`=0x801051F8 (`OSContPad[NU_CONT_MAXCONTROLLERS]`, stride 6 from the asm index-multiply + `li a2,0x6` bcopy size × 4 controllers → size:0x18; confirmed BASE not field via the offset-0 `lhu`, S20 caution cleared) + callee `nuContDataOpen`=0x800A2CAC (jal target, S23 dual). jal-count reconcile 3vs3 clean (`nuContDataClose`/`bcopy` pre-placed) — pick correctly did NOT flag jal-count-mismatch. **Closes the S20 trim** (this fn was dropped from the S20 pair for the then-unhandled missing `nuContDataOpen`). Retro applied 0 of 3 (PO: apply none — #1 size-hint tooling declined; #2/#3 confirmatory). Mirror track stays seed-only — `(real)` illustrative |
| 25 | nucontdatalock | 2 | mirror / cold / recover-extern | 5 | *5* | **Whole-file pack: one subseg (0x7E2D0) = exactly the 2 fns of `nucontdatalock.c` → no split, no orphan asm.** Both verbatim, 0 iterations. Single recover-extern `nuContDataLockKey`=0x800FED38 — the simplest S20 sub-case: a plain **scalar** word (size:0x4, assigned directly, no index-multiply) so the inlined `refs-unplaced` vram needed no base-offset correction; re-confirmed via the fn's own `lui 0x8010`/`sw -0x12c8`. Both fn names pre-curated, `osSetIntMask` pre-placed, lock macros in `nusys.h` → one gate add. Two `jal 0x800a2f60` both = osSetIntMask, reconciled clean (no jal-count flag, correctly). Retro applied 0 of 0 (buffer "None new"). Mirror track stays seed-only — `(real)` illustrative |
| 26 | nucontrmbcheck + nucontqueryread | 1+1 | mirror / cold / jal-divergence + pack | 2+3=5 | *5* | **Heterogeneous pair: `nuContRmbCheck` (near-verbatim/drop — `osSetIntMask` critical section absent from this ROM build; 3 upstream jals → 1 ROM jal confirmed via disassemble; dropped `mask` + `osSetIntMask`×2) + `nuContQueryRead` (verbatim pack-split; trivial 1-jal fn released by splitting the 0x7E330 pack at rom 0x7E350; unnamed 16B sibling `func_800A2F50` stays asm).** Both names pre-curated in ghidra_symbols; no new symbol adds, no header copies. Both 0 iterations. PO directive at S26 retro: target ≥5pt per sprint going forward → batch 2+ files consistently. Retro applied 0 of 0 (buffer "None new"). Mirror track stays seed-only — `(real)` illustrative |
| 27 | setglobalintmask + resetglobalintmask + gettime | 1+1+1 | mirror / cold / pack+recover-extern | 5+3=8 | *8* | **Opens `nintendo/exception/` (3rd `nintendo/` band after S22/S23 `pi/`) + `monegi/time/` (new band).** Pack split at 0x8B9D0; shared recover-extern `__OSGlobalIntMask`@0x800C9470 (inlined vram, size:0x4) for the `exception/` pair; `__osBaseCounter`@0x800FBE04 (size:0x4) + `__osCurrentTime`@0x801052F0 (size:0x8 OSTime) for `gettime.c` from asm disassembly. `__osViDevMgr` dead-`#ifdef _DEBUG` over-flag confirmed (no symbol add — pick's `#ifdef`-blind grep expected behavior). 3 symbol adds + 1 pack split + 3 yaml flips at gate. All verbatim cp, 0 iterations. seed 8pt (5+3); 8-gate clear (3-increment batch, not a single large item). Retro applied 0 of 0 (buffer "None new"). Mirror track stays seed-only — `(real)` illustrative |
| 28 | nucontgbpakreadwrite + nucontgbpakcheck + memset+setmem | 1+1+2 | mirror / cold / pack-split+NU_DEBUG+header | 3+3=6 | *6* | **Heterogeneous 3-file sprint: libnusys pack-split (0x7D710 → two single-fn leaves) + libkmc whole-file pack + companion header copy.** `nuContGBPakReadWrite`: verbatim cp — `#ifdef NU_DEBUG` block compiles out automatically (ROM build doesn't define NU_DEBUG; no manual drop needed, unlike S26's near-verbatim). `nuContGBPakCheckConnector`: verbatim cp, simple 1-jal leaf. `memset`+`setmem`: verbatim cp of upstream `libkmc/memset.c`; FAST_SPEED=1 path confirmed consistent; `memory.h` companion copied from `~/development/repos/libkmc/include/memory.h` → `include/libkmc/memory.h` (self-contained: `size_t` + memory fn decls); libkmc `-O` profile auto-applied by Makefile. All 4 fns 0 iterations, all names pre-curated. md5-candidate 46→49. **Retro fix:** `pick_target.py` `jal-count-mismatch:5vs1` was a tool bug — (1) `_DEAD_OPEN_RE` missing `#ifdef NU_DEBUG`; (2) `C_CALL_RE` matching `address(`/`size(` inside format-string literals. Fixed: `NU_DEBUG` added to dead-block pattern + `_strip_string_literals()` helper applied at both `call_divergence` and `calls_unplaced`. After fix `osSetTimer` correctly shows `5vs2`. Retro applied 1 of 1 (#1 fix jal-count-mismatch). Mirror track stays seed-only — `(real)` illustrative |
| 29 | nupireadwritesram + _matherr | 1+1 | mirror / cold+warm / recover-extern+pack+needs-define | 3+3=6 | *6* | **libnusys (cold): `nuPiReadWriteSram` — entire body behind `#ifdef USE_EPI`; compiles to empty stub without it. One-time Makefile enabler (`LIBNUSYS_CFLAGS := $(CFLAGS) -DUSE_EPI` + libnusys pattern rule) at gate; `nuPiSramHandle`=0x8012F4D8 recover-extern. `pick_target.py` false-clean (USE_EPI not detected as a hazard). libkmc (warm): `__matherr` — pack-split 0x8EBE0 → C 112 B (`_matherr.c`) + hasm 56 B (`__muldi3`, permanent); `errno`=0x800FE3D0 recover-extern; libkmc `-O` auto-applied. Both verbatim cp, 0 iter. md5-candidate 49→51. Retro applied 1 of 1 (#1 `needs-define` hazard in `pick_target.py` — `function_gating_define()` detects top-level `#ifdef` body gate; `_parse_makefile_defines()` cross-checks against effective library CFLAGS; +1 in seed_points). Mirror track stays seed-only — `(real)` illustrative |
| 30 | strcmp + osSetTimer + __osDequeueThread | 1+1+1 | mixed: mirror/warm/verbatim + classical/warm/jal-mismatch-stripped + classical/warm/defines-data-drop | 1+2+3=6 | **6** | **First mixed sprint; 3rd+4th classical-track fns.** `strcmp` (libkmc verbatim cp, 0 iter, warm mirror, seed 1; libkmc `-O` auto-applied). `osSetTimer` (**mis-routed as near-verbatim at DoR** — `jal-count-mismatch:5vs2` flagged; asm revealed fundamentally stripped impl — no interrupt disable/restore/counter update, different `__osSetTimerIntr` arg; routed to classical; `__osTimerList`@0x800C8240 recover-extern; score 0 first pass; realized=seed=2). `__osDequeueThread` (classical, defines-data drop — 5 file-scope defs from thread.c dropped; 64 B pointer-walk loop; register params + `(OSThread*)queue` head cast; score 0 first pass; realized=seed=3). All 3 green SHA first pass, 0 stuck-far/permuter/carried/re-opened. md5-candidate 51→54. Retro applied 1 of 1 (#1 CLAUDE.md gate note: large jal-count-mismatch >2 is `classical-likely` — disassemble before routing to mirror branch). Classical track adds 2 fns, both residual 0 — S30 classical seed+realized **5 logged** |
| 31 | func_800A2F50 + nuGfxInit | 1+1 | classical / warm / first-pass-clean + classical / warm / novel-libnusys-gotcha | 3+5=8 | **10** | **5th+6th classical-track fns; first pure classical sprint.** `func_800A2F50` (16 B getter, score 0 first pass; realized 2 = seed 3 − 1 first-pass-clean; residual −1). `nuGfxInit` (176 B, 6 jals; jal-count-mismatch 13vs6 >2 → classical; Ghidra seed + v2.00 SDK `~/n64sdk/4.0/pc/basic/nusys/` as template — v2.07 libultra_modern was wrong for this build; **novel gotcha × 2**: (1) `Gfx gfxList[0x100]+Gfx *gfxList_ptr` locals + GBI macros `gSPDisplayList/gDPFullSync/gSPEndDisplayList` with `gfxList_ptr++` required to force 0x820 frame (raw array writes gave 0x818); (2) `D_B6698 = 0xB6698` in `undefined_syms_auto.txt` is an **absolute-physical-address linker symbol** for `rdpstateinit_dl`; referenced as `(u32)&D_B6698` to generate matching `R_MIPS_HI16/LO16 D_B6698` relocs — distinct from the standard recover-extern vram pattern; `#undef nuGfxInit` needed to override in-tree nusys.h macro. realized 8 = seed 5 + 1 novel-gotcha → Fibonacci snap 6→8; residual +3). All 0 stuck-far/permuter/carried/re-opened. md5-candidate 54→56 (56/56 = ALL FILES). Retro applied 1 of 2 (#1 CLAUDE.md split-subseg spot-check cmp note; #2 libnusys classical pattern NOT applied — PO deferred). Classical track seed+realized **S31: 8+10 logged** |
| 32 | syncprintf + sptaskyield | 2+1 | mirror / cold / FINALROM-known-edit + zero-enabler | 10 | *10* | **-D_FINALROM global (Makefile gate enabler). `syncprintf.c` VERSION_J near-verbatim: drop `__osSyncVPrintf` (VERSION_K+ only); cross-ref `ultralib/src/libc/syncprintf.c` for VERSION_J layout; `#ifndef _FINALROM` bodies → empty MIPS O32 vararg stubs (save $a0–$a3 + jr $ra). `include/stdarg.h` from ultralib GCC; `-nostdinc` removed (ROM SHA-1 green). `sptaskyield.c` verbatim zero-enabler. Mirror track stays seed-only — `(real)` illustrative** |
| 33 | piacs.c | 3 | classical / warm / file-static-drop + defines-data + recover-extern | 5 | **5** | **7th+8th+9th classical-track fns; first sprint requiring per-lib CFLAGS discovery.** `__osPiCreateAccessQueue` (trivial 3-call init, score 0 first pass). `__osPiGetAccess` (requires both fns in same TU — `-O3` inlines `__osPiCreateAccessQueue`; `-mips3` schedules `sw $ra` into `bnez` delay slot; score 0 first seed with correct flags). `__osPiRelAccess` (trivial `osSendMesg` wrapper, score 0 first seed). **Compile-flag discovery:** ultralib gcc.mk reveals `OPTFLAGS=-O3 MIPS_VERSION=-mips3 -funsigned-char` for VERSION_J; global `-mips2`→`-mips3` (ROM-wide SHA-1 verified); `LIBULTRA_CFLAGS` added to Makefile; `decomp_loop.py` libultra auto-detect added. `sync-names` mid-sprint eviction caused 3 undefined refs + label mismatch (recovery: add evicted syms to `symbol_addrs.txt`; fix stale asm label). All 0 stuck-far/permuter/carried/re-opened. md5-candidate 58→59. **realized=5** (seed 5; all first-pass; no permuter; the compile-flag debugging was discovery overhead, not classical iteration); residual 0. Classical track: **n=7** (S9/S11/S30/S31×2/S33×3) |
| 35 | startthread + vigetcurrframebuf + vigetnextframebuf + vigetmode | 1+1+1+1 | mirror/warm/verbatim × 4 | 1+5+5+5=16 | *16* | **thread-band + vi-band (3rd vi-band trio); mmuldi3 hasm vendor.** `osStartThread` (288 B, `jal-count-mismatch:9vs7` — **GCC -O3 tail-merge**: three `__osEnqueueThread(&__osRunQueue, t)` sites in two switch cases share one jal; verbatim cp, 0 drops, 0 iterations). `osViGetCurrentFramebuffer` / `osViGetNextFramebuffer` / `osViGetCurrentMode` (all 56–60 B, no hazards, verbatim cp, 0 iterations). **Side-win enabler:** vendor `src/libkmc/mmuldi3.s` (KMC asm compat patch: 6× `li $r,0xffffffff` → `addiu $r,$0,-1`; Makefile explicit-rule override for `build/asm/8EC50.o`; consolidates 5 hasm subsegs into 1+1). All 4 fns 0 stuck-far/permuter/carried/re-opened. md5-candidate 62→66. Mirror track stays seed-only — `(real)` illustrative |
| 37 | nupireadrom + aigetlen + aigetstat + visetspecial | 1+1+1+1 | mixed: classical/cold/custom-ROM-variant + mirror/warm/IO_READ × 2 + mirror/warm/verbatim | 3+1+1+2=7 | **7** | **First libnusys main-segment classical; IO_READ pair + vi mirror.** `nuPiReadRom` (224 B, libnusys cold; no upstream match — ROM uses undocumented variant: osInvalDCache×2/iter, all struct setup inside loop; 3 iterations to converge on delay-slot ordering; realized=seed=3, residual 0). `osAiGetLength` + `osAiGetStatus` (16 B each; IO_READ verbatim mirrors; reloc-vs-literal isolation artifact expected from S34 precedent; .o spot-check skipped per IO_READ convention; ROM SHA-1 green). `osViSetSpecialFeatures` (368 B; verbatim mirror; `refs-unplaced:__osViDevMgr` = dead `_DEBUG` FP; spot-check MATCH 368B). 1 carry-over: osAiSetFrequency (osViClock + rodata unplaced). md5-candidate 68→72; matched 75→79/2090. Classical track: **n=6** (S9/S11/S30/S31×2/S33×3/S34×1/S37×1); **S37 seed 3 / realized 3 logged** |
| 38 | osaisetfrequency | 1 | mirror/warm/ai-band | 2 | *2* | **Retroactively banked (same session).** Layer 1 (`.lit4`/`.lit8`) = FIXED (`-G 0` on `tools/cc/as`, confirmed from binutils src + ultralib gcc.mk). Layer 2 (`.rodata` layout): resolved by splitting `[0xAD5E0, rodata]` at 0xAD6A0 and adding `[0xAD6A0, .rodata, libultra/monegi/ai/aisetfreq]` — dot-prefix subseg becomes splat's sibling of the `c` subseg, suppressing `auto_link_sections` from placing `aisetfreq.o(.rodata)` at 0x800CA270; lands correctly at 0x800D22A0. Verbatim mirror proved. 0 stuck-far/permuter/carried=0/re-opened. md5-candidate 72→73. Mirror track: committed 2pt / banked 2pt. |
| 36 | alcopy + alheapinit | 1+1 | mirror / cold / audio-band-unlock+header-copy | 3+3=6 | *6* | **First libultra audio-band mirrors; opens `monegi/audio/`.** `-I include/libultra/PR` Makefile enabler + `synthInternals.h` companion header copy (parallel to S15's libnusys unlock). `alHeapInit` verbatim cp; `alCopy` verbatim cp (trivial byte-copy loop). PO mid-sprint libnaudio concern resolved — libnaudio has no separate versions; shared utilities identical in both libraries. Both 0 iterations, 0 stuck-far/permuter/carried/re-opened. md5-candidate 66→68. Mirror track stays seed-only — `(real)` illustrative |
| 34 | jammesg + recvmesg + aisetnextbuf | 1+1+1 | mixed: mirror/warm/verbatim + mirror/warm/verbatim + classical/warm/static-drop | 1+2+2=5 | *5* | **message-band pair + AI set-buffer; 10th classical-track fn.** `osJamMesg` (verbatim cp, 0 iter, warm mirror, seed 1; name pre-curated). `osRecvMesg` (verbatim cp, 0 iter, warm mirror, seed 2; `MQ_IS_EMPTY` hazard flag = macro FP only, gate confirmed). `osAiSetNextBuffer` (**classical/static-drop**: file-scope `static u8 hdwrBugFlag` dropped, extern recovered at 0x800C7EC0 from fn's own `lui 0x800c`/`lbu 0x7ec0`; size:0x1; **IO_WRITE isolation artifact** — score never reaches 0: macros bake in literal 0xa450xxxx constants, reference asm uses `%hi(AI_DRAM_ADDR_REG)` symbolic relocs → ~600-point penalty with `total_rows==match_count` and `top_mismatches:[]`; recognized as isolation-only; C logic verified against asm, inlined, spot-check MATCH, SHA-1 green; 0 real iterations; realized=seed=2; residual 0). **Data-global label sync friction**: `D_800C7EC0` in stale `asm/7EFE0.s` vs `hdwrBugFlag` in compiled obj → reloc-name mismatch in asm-differ (fixed by renaming all 3 occurrences + `make build/asm/7EFE0.o`). 0 stuck-far/permuter/carried/re-opened. md5-candidate 59→62. Mirror track stays seed-only — `(real)` illustrative. Classical track: **n=8** (S9/S11/S30/S31×2/S33×3/S34×1) |
| 39 | viinit | 1 | classical / warm / defines-data + IO_WRITE | 3 | **3** | **11th classical-track fn; first libultra vi-band classical.** `__osViInit` (260 B, `ultralib/src/io/vi.c` VERSION_J; file-scope `static __OSViContext vi[2]` + `__osViCurr`/`__osViNext` defines-data → BSS-layout-conflict → classical, not mirror; 3 defs dropped, externs placed at gate via 5 symbol_addrs adds). **IO_WRITE isolation artifact** (same pattern as S34 osAiSetNextBuffer). **New isolation artifact observed:** struct-field access via base symbol (`lui %hi(vi); sh v0, offset(at)`) encodes inline LO16 addend in compiled obj vs zero-placeholder `D_<addr>` reloc in reference asm — byte-level `cmp` MISMATCH on addend bytes, but both link identically; all 0x110 B resolve to same ROM bytes. C logic verified vs asm (ultralib vi.c matches exactly), inline direct, spot-check MISMATCH explained, `make` → ROM SHA-1 green. 0 iterations, 0 stuck-far/permuter/carried/re-opened. md5-candidate 73→74 (all 74 C files now 0-stub). **realized=seed=3** (first-pass non-trivial classical; analysis overhead but no C edit iterations; no escalations; struct-reloc observation new but benign → no +1). Classical track: **n=7** (S9/S11/S30×2/S31×2/S33×3/S34×1/S37×1/S39×1); **S39 seed 3 / realized 3 logged** |
| 41 | epirawwrite | 1 | mirror / warm / verbatim + recover-extern (nintendo/pi/) | 2 | *2* | **libultra pi IO_WRITE verbatim mirror; first macro-hidden recover-extern.** Looked zero-enabler (`__osEPiRawWriteIo` symbol pre-placed, no `.c`-body data refs) but the `EPI_SYNC` macro in `piint.h` references `__osCurrentHandle[domain]` → `undefined reference` at link in the **execution middle** (blind spot like S23 `calls-unplaced` / S40 wrong-lib-header: gate build-check resolves the INCLUDE_ASM scaffold, never the C body). Recovered `__osCurrentHandle`=0x800C7E90 (`OSPiHandle*[2]` → size:0x8) deterministically from the fn's own `lui $a3,%hi(D_800C7E90)`+`lw $a3,%lo(D_800C7E90)($a3)`; index `domain*4` is a separate `addu` so the `%hi/%lo` base is direct (no field-offset subtraction). **IO_WRITE isolation artifact** (S34) → no isolation spot-check; ROM SHA-1 the proof; 0 iterations. 0 stuck-far/permuter/carried/re-opened. md5-candidate 72→73. Retro applied 2 of 2 (#1 CLAUDE.md macro-hidden recover-extern bullet; #2 `pick_target.py` refs/calls-unplaced **follow one level of macro expansion** — params stripped, `__builtin_*`/nested-macro names excluded; full-table diff confirmed strict de-noise: dropped 15+ macro FPs e.g. `IO_READ`/`IO_WRITE`/`ARRLEN`/`MQ_IS_FULL`/`va_start`, surfaced real macro-hidden callee `__osMotorAccess` via `osMotorStart`). Mirror track: committed 2pt / banked 2pt — `(real)` illustrative |
| 42 | sendmesg + seteventmesg | 1+1 | mixed: mirror/warm/verbatim + classical/warm/defines-data-drop | 2+3=5 | **4** | **message-band pack (0x86550) split at the fn boundary; 12th classical-track fn; first defines-data verbatim-body fast path.** `osSendMesg` (304 B, verbatim libultra mirror, warm message band, all 6 refs pre-placed, 0 iter; seed 2). `osSetEventMesg` (176 B, **defines-data drop**: body verbatim, dropped file-scope `__OSEventState __osEventStateTab[15] ALIGNED(8)` + `u32 __osPreNMI` defs → 3 data externs placed add-only at gate (`__osEventStateTab`=0x801B7078 size:0x78 [`_FINALROM`→OS_NUM_EVENTS=15 ×8], `__osPreNMI`=0x800C81F0, `__osShutdown`=0x800C946C); **fast path**: only edit is dropped defs → known-edit *mirror*, proved by full-make SHA, no seed/decomp_loop/spot-check; matched in one `make`; realized 2 = seed 3 − 1 verbatim-first-try; residual −1). PO swapped this defines-data sibling in over a libnusys filler at the plan gate. Both 0 stuck-far/permuter/carried/re-opened. md5-candidate 76→78. **Tooling gaps found:** (a) `jal-count-mismatch:7vs6` on osSendMesg was a `MQ_IS_FULL` macro-pseudo-call FP in the jal-count C side (per-fn 6vs6 clean) — fixed (#1: `MQ_IS_FULL`/`MQ_IS_EMPTY` → `_C_NONCALL`); (b) `defines_data_globals` never flagged `__osEventStateTab` (the `ALIGNED(8)` suffix defeats both the regex and the paren-guard) → caught manually at gate, carried as a new suggestion. Retro applied 3 of 3 (#1 jal-count macro FP; #2 defines-data verbatim-body fast-path doc; #3 array-dim annotation `defines-data:<name>[DIM]`). Classical track: **n=8**; **S42 seed 3 / realized 2 logged** |
| 40 | ldiv | 2 | mirror / warm / verbatim (libc/) | 2 | *2* | **First `monegi/libc/` mirror; whole-file 2-fn pack (one subseg 0x8DF50 = exactly `ldiv`+`lldiv`, no split).** Both verbatim cp, 0 iterations, names pre-curated, `__divdi3` (lldiv's 64-bit divide callee) pre-placed. **Build-system friction (not a matching spike):** verbatim `ldiv.c`'s `#include "stdlib.h"` resolved to `include/libkmc/stdlib.h`, which lacks libultra-only `lldiv_t` — a *resolvable-but-wrong-library* header that `pick_target.py`'s `needs-header` grep AND the gate build-check both miss (scaffold never compiles the C body; surfaces in execution middle like `calls-unplaced`). Fixed in the build system per PO redirect: vendored `ultralib/include/compiler/gcc/stdlib.h` verbatim → `include/libultra/compiler/gcc/stdlib.h`, prepended `-I` to `LIBULTRA_CFLAGS` only (wins over `include/libkmc` for libultra srcs; libkmc/libnusys unaffected; verified per-file with `gcc -M`). libkmc header left verbatim — never pollute it. 0 stuck-far/permuter/carried/re-opened. md5-candidate 71→72. Retro applied 1 of 2 (#2 CLAUDE.md per-library std-header isolation bullet; #1 `cross-lib-header` hazard NOT applied — PO deferred). Mirror track: committed 2pt / banked 2pt — `(real)` illustrative |
| 43 | gbpakpower + gbpakgetstatus | 1+1 | classical-flagged / cold / both turned out verbatim mirror (shared/gbpak/) | 3+3=6 | **4** | **13th+14th classical-track fns; first sprint where a classical-flagged target proved a pure verbatim mirror (the jal-mismatch was a macro FP, not a real divergence).** `osGbpakPower` (288 B, jal `6vs5` was `OS_USEC_TO_CYCLES(120000)` macro counted as a call; upstream body verbatim, score 0 first try; refs-unplaced `__osGbpakTimer`=0x8012F490 size:0x20 / `__osGbpakTimerMsg`=0x800FF4AC size:0x4 / `__osGbpakTimerQ`=0x801B70F8 size:0x18 recovered at gate from osSetTimer arg setup; realized 2 = seed 3 − 1 verbatim-first-try; residual −1). `osGbpakGetStatus` (288 B, jal `6vs4` was 2× `ERRCK` macro; calls-unplaced-dual `__osContRamRead`=0x800AF420 / `__osPfsGetStatus`=0x800AE710 (both `func_<addr>` in Ghidra) recovered at gate; **isolation artifact** — isolated score 15 / 99.8% with empty `top_mismatches` AND 76/76 rows match = struct-field reloc addend + extern HI/LO16; full-make ROM SHA-1 == baserom = byte match, no C iteration; realized 2 = seed 3 − 1 verbatim-first-try; residual −1). Both 0 stuck-far/permuter/carried/re-opened. md5-candidate 78→80. **Tooling gap found + fixed:** the S42 `MQ_IS_FULL` jal-count fix was a 2-macro hardcode; both gbpak FPs (`OS_USEC_TO_CYCLES`, `ERRCK`) escaped it → generalised `_c_jal_count` to drop every invoked function-like macro via the S41 `all_func_macros()` table (also corrected sprintf `2vs1`→clean [va_start FP] and osCartRomInit `21vs5`→`6vs5` [macro-inflation]; golden regen, 23 pass). Retro applied 2 of 2 (#1 jal-count macro-table generalization; #2 isolation-artifact recognition signal doc). Classical track: **n=9**; **S43 seed 6 / realized 4 logged** (residual −2, most-negative yet — classical-flag that was really a mirror) |
| 44 | createthread + destroythread + gbpakinit | 1+1+1 | mirror / warm / verbatim×2 (1 cast-edit) + defines-data-fast-path (monegi/thread/ + shared/gbpak/) | 3+3=6 | *6* | **3-file libultra epic increment; thread pack split + gbpak initializer.** `osCreateThread` (208 B, thread-pack split at vram 0x800AC2D0; **near-verbatim cast divergence** — verbatim copy matched every instr except `context.ra`: baserom sign-extends `(s64)(s32)__osCleanupThread` (`sra v0,a0,0x1f`) where libultra_modern zero-extends `(u64)(u32)` (`move v0,zero`); VERSION_J sign-extends the whole OSContext block — sibling `sp`/`a0` already did; one-token fix → match; `.o`-diff caught it on the first SHA miss). `osDestroyThread` (224 B, clean verbatim, 0 edits). One recover-extern enabler `__osCleanupThread`=0x800B04E8 (refs-unplaced, createthread's context.ra); all other thread callees pre-placed; `_FINALROM` drops the `thprof` block. `osGbpakInit` (512 B, **defines-data verbatim-body fast-path** S42 — dropped file-scope `__osGbpakTimer`/`__osGbpakTimerMsg`/`__osGbpakTimerQ` defs → externs from `controller_gbpak.h`, storage from the S43-placed data region; all 6 callees pre-placed incl. S43's `__osContRamWrite`/`__osContRamRead`/`__osPfsGetStatus`; matched first `make`, 0 iter). All 3 mirror track. 0 stuck-far/permuter/carried/re-opened. md5-candidate 80→83. Retro applied 1 of 2 (#1 new `docs/hazards.md#mirror-cast-divergence-sign--vs-zero-extend` section + CLAUDE.md index row; #2 confirmatory, log-only). Mirror track stays seed-only — committed 6pt / banked 6pt — `(real)` |
| 46 | pirawdma + pigetcmdq | 1+1 | mirror / cold / verbatim×2 + PRinternal band-unlock (libultra/io/) | 2 | *2* | **Reopens the libultra PI/SI/cont/pfs mirror band, banks the first 2 io/ fns.** 0x8BA20 3-fn pack split at vram 0x800B06F0 + 0x800B0710 (`func_800B0710` left asm). Root unblock: ultralib mirrors `#include "PRinternal/<h>"` but the project shipped those headers under `internal/`, so the whole band read `blk needs-header` — a **`pick_target` false-`blk`**: `include_is_blocked` matched the include's *basename* (`piint.h`) against in-tree `internal/piint.h` and called it a deferred -I, masking a cheap companion-copy. Fix: full-relative-path match (not basename) + ultralib/include as the primary libultra companion-header root → 11 band fns (`osCartRomInit`, `__osContRam{Read,Write}`, `__osPfsGetStatus`, `osSpTaskLoad`, `osContInit`, `osCreateViManager`, …) flip `blk`→pickable. Enabler: verbatim `cp ultralib/include/PRinternal/piint.h → include/libultra/PRinternal/` (deps `PR/os_internal.h`+`PR/rcp.h` in-tree). `__osPiRawStartDma` (224 B, `_DEBUG` compiled out; recover-extern `osRomBase`=0x80000308 — a libultra **boot-region global**, asm-baked as `D_80000308`, missed by refs-unplaced's `__`-prefix grep → 2nd fix: `BOOT_GLOBALS` table surfaces the 0x80000300-0x1C globals with known vram). `osPiGetCmdQueue` (32 B, 2-line getter, only ref `__osPiDevMgr` placed). Both verbatim cp, 0 iterations, names pre-placed. 0 stuck-far/permuter/carried/re-opened. md5-candidate 85→87. Retro applied 2 of 2 (#1 `include_is_blocked` full-path + ultralib root; #2 `BOOT_GLOBALS` recover table — golden regen, 20 pass / 3 skip). Mirror track stays seed-only — committed 2pt / banked 2pt — `(real)` |
| 45 | gbpakreadwrite + gbpakreadid | 1+1 | mirror / warm / near-verbatim drop + defines-data-fast-path (shared/gbpak/) | 3 | *3* | **Closes the `libultra/shared/gbpak/` band (S43 power/getstatus, S44 init).** 0x88FC0 2-fn pack split at vram 0x800ADD90. Both near-verbatim mirrors — divergence surfaced only on the first SHA miss via `.o`-diff (same late-surfacing class as S18/S44). `osGbpakReadWrite` (464 B): verbatim cp SHA-missed → baserom omits the upstream `if (size == 0) return 0;` block (a **jal-less** early-return that folds into the later `blez` size<=0 path with uninit `ret`); dropped it → match, 1 edit. Invisible to jal-counting (no call in the block). Recover-callee `__osGbpakSetBank`=0x800B1A90 resolved at gate; `__osContRamWrite`/`__osContRamRead` placed S44. `osGbpakReadId` (400 B): verbatim cp built 144 insns / 12 jals vs baserom 97/7 — MG64 omits the whole upstream `if(bcmp){ write-temp; reread; recheck }` retry block (5 calls + `temp[32]` local) → `if (bcmp(...)) return 4;`; `.text` then matched 97/97 but SHA still missed on `.data`: function-local `static nintendo[]`/`mmc_type[]` live in the shared `main_data` blob (0x800C93F0/0x800C9420, `D_<vram>` name == real vram; the +0x50 map figure was the `.NON_MATCHING` alias). Dropped the static defs → sized `extern u8 nintendo[48]; extern u8 mmc_type[20];` (so `ARRLEN`/`sizeof` compiles) + 2 `symbol_addrs` adds renaming the dlabels → no `.data` emitted, no segment shift → match. bcmp recover-callee `bcmp`=0x800B0A20 resolved at gate. 0 stuck-far/permuter/carried/re-opened. md5-candidate 83→85 (all 85 c files now md5-candidate, 0 stubs in tree). Retro applied 2 of 3 (#1 hazards.md jal-less dropped-block note; #2 hazards.md defines-data function-local-static extension; #3 log-only). Mirror track stays seed-only — committed 3pt / banked 3pt — `(real)` |
| 47 | cartrominit | 1 | mirror / warm / verbatim cross-jump tail-merge (libultra/io/) | 3 | *3* | **First banked fn from the S46-reopened io/ band's near-verbatim tier (vs S46's clean getters); the cross-jump/wrong-size lesson.** Single fn 0x7E870 (no split). Verbatim ultralib VERSION_J source. Two defines-data drops (`__CartRomHandle`=0x80105BC0 size:0x74; the function-local `static int first` → `osCartRomInitFirst`=0x800C7EA0) + companion `PRinternal/macros.h`. **The trap:** a hand-fold of the upstream `if(!first){rel;return;}` early-return into `if(first){body}` compiled **0x10 SHORTER** than baserom (1 vs 2 callee-saved regs) → shifted EVERY downstream fn → whole-ROM mismatch (not a local diff). Fix = copy upstream VERBATIM; KMC GCC -O3 **cross-jumps** the two identical `{rel;return;}` tails into one shared block itself (that IS the jal `6vs5`), reproducing the exact 0x20-frame/s1-anchor/s0=base-in-delay-slot regalloc. Residual isolated 4400 / 99-of-99-rows = pure reloc HI/LO16 addend (isolation artifact) → full-make SHA proved it; no permuter. 0 stuck-far/permuter/carried/re-opened (the near-miss self-corrected mid-build). md5-candidate 87→88. Retro applied 3 of 3 (#1 hazards.md verbatim-first/wrong-size-diagnostic in Near-verbatim section; #2 `pick_target.py` `#if BUILD_VERSION` branch-stripping in refs/calls-unplaced — drops dead-`#else` FPs `CartRomHandle`/`osPiRawReadIo` + the `endif`-token FP, golden regen 20 pass/3 skip; #3 log-only defines-data blind-spot note). Mirror track stays seed-only — committed 3pt / banked 3pt — `(real)` |
| 49 | random (guRandom) | 1 | mirror / cold / defines-data fast-path + needs-header false-blk (libultra/gu/) | 2 | *2* | **Opens the cold `gu/` band; 1st gu fn.** Single fn 0x85420 (`guRandom`, 12 insns). Verbatim ultralib `src/gu/random.c` except the **S45 defines-data fast path**: function-local `static unsigned int xseed = 174823885` emits a 2nd `main_data` copy (segment shift -> SHA miss), so dropped it -> file-scope `extern unsigned int xseed;` + recover `xseed`=0x800C81C0 (size:0x4, was `D_800C81C0` w/ `.NON_MATCHING` alias). The `blk needs-header:guint.h` was a **false-block**: `guint.h` is missing-but-copyable (source-private header), and its `mbi.h`/`gu.h` deps already resolve via the existing `-I include/libultra/PR` (the needs-header grep's `-I` set omits PR, over-flagging). Copied `guint.h` verbatim next to `random.c`; `.text` `%hi/%lo(xseed)` identical either way -> matched first `make`. `guRandom` name pre-curated in ghidra_symbols (no func add). 0 stuck-far/permuter/carried/re-opened. md5-candidate 89->90. Retro applied 0 of 0 (clean first-build, empty suggestion buffer). Mirror track stays seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 48 | viswapcontext | 1 | mirror / warm / recover-extern + rodata-sibling (libultra/io/) | 5 | *5* | **11th vi-band sibling; clean verbatim VERSION_J mirror.** Single fn 0x88810. Verbatim ultralib `src/io/viswapcontext.c` (include `PRinternal/viint.h` -> bare `viint.h`, sibling convention). One recover-extern at gate: `__additional_scanline`=0x800C826C (size:0x4, `extern u32` per viint.h, from `lui 0x800d / lw -0x7d94`). `__osViNext`/`__osViCurr` already placed; `__OSViContext` from pick_target refs-unplaced was a **struct TYPE** (0x30B, viint.h), not a data symbol -- ruled out (motivated retro #1). `.text` matched on first compile (0x310B); only the **rodata-sibling** for the `2^32` double (u32->float cvt) needed the yaml split: `[0xAD9C0, rodata]` -> insert `[0xAD9E0, .rodata, libultra/io/viswapcontext]` (16B = double + 8 pad), same as S38 aisetfreq. 0 stuck-far/permuter/carried/re-opened. md5-candidate 88->89. Retro applied 2 of 2 (#1 `pick_target.py`/`decomp_asm.py` exclude typedef'd type names from refs-unplaced via `declared_type_names`; #2 `rodata-literal:<addr>` pre-flag for mirror candidates loading anonymous `ldc1/lwc1 %lo(D_)` FP constants; golden 23 pass). Mirror track stays seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 50 | scale (guScaleF + guScale) | 2 | mirror / warm / verbatim band-open fast path (libultra/gu/) | 2 | *2* | **2nd gu/ file; band-open fast path off S49's guRandom.** Single-file 2-fn pack 0x85A50 (`guScaleF` 0x800AA650 + `guScale` 0x800AA6B0, 224 B, no split). Verbatim ultralib `src/gu/scale.c`, zero edits. The `blk needs-header:guint.h` was a **false-blk from band warmth**: scale.c `#include "guint.h"` (quote) resolves source-relative to `src/libultra/gu/guint.h`, shipped alongside random.c in S49 — `missing_includes` greps only the `-I` set, missing source-relative resolution. S49's BACKLOG note predicted this pack would carry `guMtxIdentF`/`guMtxF2L` as calls-unplaced; **wrong** — both callees (0x80067CB4 / 0x80067B00) were already placed, no recovery. Names `guScaleF`/`guScale` pre-curated. Single yaml-flip enabler; matched first `make`, full-make SHA-1 == baserom. 0 stuck-far/permuter/carried/re-opened. md5-candidate 90->91. Retro applied 1 of 1 (#1 `missing_includes` takes the mirror dir + drops band-local quote-includes that resolve source-relative -> `guMtxCatF` and future gu/ siblings no longer false-`blk`; docs/hazards.md#needs-header note; golden regen, 23 pass). Mirror track stays seed-only -- committed 2pt / banked 2pt -- `(real)` |

| 51 | mtxcatf (guMtxCatF + guMtxXFMF) | 2 | mirror / warm / combined-subseg non16align (libultra/gu/) | 2 | *3* | **3rd gu/ file; combined-subseg flip (non16align forbids per-fn split).** Single 2-fn subseg 0x847D0 (`guMtxCatF` 0xDC + `guMtxXFMF` 0xAC at non-16 `0x848AC`). Verbatim ultralib `src/gu/mtxcatf.c` — the original SGI file holding BOTH fns as C — zero edits, matched first `make`, full-make SHA-1 == baserom. **non16align combined case:** `guMtxXFMF` is tight-packed after `guMtxCatF` at a non-16 offset, so a per-fn yaml split fails (KMC `as` pads fn1's standalone `.o` to 16, shifting fn2 → bare-stub SHA miss, observed at gate); fix = one combined `[0x847D0, c, libultra/gu/mtxcatf]` subseg, both fns in one `.o`. **Gate red-herring:** `guMtxCatF` mapped to libultra_modern's hand-asm `mtxcatf.s` (deprecated split distro) → mis-warned hand-asm; disasm was a textbook compiled nested-loop matmul, and ultralib (the sole source) ships it as C. Both names pre-curated, `guint.h` shipped S49. **Fixup:** the reverted per-fn split left an orphan `gu/mtxxfmf.c` stub that `git add -A` swept into the bank commit → removed in a fixup commit (build stayed green). 0 stuck-far/permuter/carried/re-opened. md5-candidate 91->92 (all 92 .c files 0-stub). Retro applied 3 of 3 (#1 pack `.s`/`.c`-boundary disasm-probe before classical-flag; #2 hazards.md#non16align combined-subseg + reverted-split cleanup; #3 hazards.md#upstream-mirror-pattern S51 cautionary note — reach ultralib first, a libultra_modern `.s` is not evidence of hand-asm). **Estimate note:** deterministic pts seed = 2 (= S50's identical 2-fn gu shape); gate committed 3 (+1 one-time non16align adjust). Mirror track stays seed-only -- committed 3pt / banked 3pt -- `(real)` |
| 52 | rotaterpy (guRotateRPYF + guRotateRPY) + lookatref (guLookAtReflectF + guLookAtReflect) | 4 | mirror / warm / data-static + recover-callee + rodata-sibling (libultra/gu/) | 5 | *5* | **4th+5th gu/ files; the clean gu/ leaf pool, now mined out.** Two verbatim ultralib VERSION_J mirrors, both clean first-build (0 iter). rotaterpy (seed 2): S49 static-float fast path — fn-local `static float dtor = 3.1415926/180.0` → file-scope `extern float dtor;` + recover `dtor`@0x800C81E0 (the `rodata-literal` pre-flag was really a **data-static**, 0x800C8xxx data region not 0x800Dxxxx rodata; the asm `lwc1 %lo` confirmed). lookatref (seed 3): recover-callee `sqrtf`@0x800B0A10 (S23 dual) + **rodata-sibling split at finalize** — two doubles `-1.0`@0x800D2510 (`ldc1`) + `1.0`@0x800D2518 (an `lw` pair, FP-scan-invisible) = 16 B at rom 0xAD910 → `[0xAD910, .rodata, libultra/gu/lookatref]` + `[0xAD920, rodata]`. All callees pre-placed. 0 stuck-far/permuter/carried/re-opened. md5-candidate 92->94 (asm subsegs 180->178). Retro applied 3 of 3 (#1 stale S51 gu/ band-note correction — `guMtxIdentF`/`guMtxF2L` are inside the pts-13 main pack `func_800660A0`, not separable; #2 `pick_target.py` data-static-vs-rodata-literal segment classifier; #3 full rodata-extent enumeration incl. `lw`-pair double 2nd words; golden regen, 23 pass). Mirror track stays seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 53 | heapalloc (alHeapDBAlloc) | 1 | mirror / warm / needs-header false-blk (libultra/audio/) | 1 | *1* | **Closes the named-clean `libultra/audio/` leaf (sibling of S36 heapinit/copy).** Single fn 0x82320 wedged between banked heapinit (0x822E0) and copy (0x82370). Verbatim ultralib `src/audio/heapalloc.c`, zero edits, matched first `make`, full-make SHA-1 == baserom. Body trivial under `_FINALROM` (no `_DEBUG`): align+bounds+ptr-advance; no data externs, `__osError` is `_DEBUG`-only → no callees. The `blk needs-header:libaudio.h,os_internal.h,ultraerror.h` was a **false-block of a new class**: all 3 ship at `include/libultra/PR/` and resolve via `LIBULTRA_CFLAGS -I include/libultra/PR`, but `missing_includes`'s `-I` set (base INCLUDE_DIRS) omits the PR/ path → over-flags every libultra PR-band mirror. Name `alHeapDBAlloc` pre-curated in ghidra_symbols (no func add). Single yaml-flip enabler. 0 stuck-far/permuter/carried/re-opened. md5-candidate 94->95 (asm subsegs 178->177). Retro applied 3 of 3 (#1 `missing_includes` lib-aware `-I` set via `LIB_EXTRA_INCLUDE_DIRS` — libultra adds compiler/gcc + PR, kills the PR-band false-blk; golden regen 23 pass; #2 drop stale "deferred Makefile enabler" comment at the INCLUDE_DIRS note; #3 BACKLOG libultra hazard-map re-survey). **Estimate note:** pts column read `blk` (the false-block); true seed = 1 (warm single-fn mirror), refuted at gate. Mirror track stays seed-only -- committed 1pt / banked 1pt -- `(real)` |
| 54 | sprintf (sprintf + proutSprintf) | 2 | mirror / warm / file-static + needs-header false-flags (libultra/libc/) | 3 | *3* | **Closes the last named-clean libultra leaf; after S54 `--lib libultra` is empty (remainder = intrinsic-likely register/FPU shims → classical-or-hasm).** Whole-file 2-fn pack 0x861F0 (`sprintf` 0x800AADF0 0x58 + static `proutSprintf` 0x800AAE48 0x34, 144 B, combined subseg, no split — S40 ldiv pattern). Verbatim ultralib VERSION_J `src/libc/sprintf.c`, zero edits, matched first `make`, full-make SHA-1 == baserom. Callees both pre-placed (`_Printf`=0x800B0B30, `memcpy`=0x800AADC4); no data externs; names pre-curated → zero symbol adds. **Two false-flags, both refuted at the gate:** (a) `file-static` was the `static void* proutSprintf(...)` *function* proto (shares the TU, no BSS hazard) — not a static variable; (b) `needs-header:xstdio.h,string.h` is the vendorable false-blk class (S49/S53) — vendored `xstdio.h`→`src/libultra/libc/` (source-private, deps stdlib.h/stdarg.h in-tree) + `string.h`+`memory.h`→`include/libultra/compiler/gcc/` (kept source-relative inside the libultra compiler dir per S40 cross-lib lesson). `os.h` resolves via `-I include/libultra/PR`. 0 stuck-far/permuter/carried/re-opened. md5-candidate 95->96 (all 96 .c files 0-stub; asm subsegs 177->176). Retro applied 2 of 3 (#1 `pick_target.py` file-static detector ignores static *function* decls, strips `__attribute__((...))` so attributed static arrays still flag — fixed a gfxThread/nuGfxMesgBuf regression caught pre-regen; #2 `needs-header:<h>(vendorable)` annotation via `UPSTREAM_SRC_ROOTS` source-private index + `include_is_vendorable`, so a copyable header is a 1pt enabler not `blk`; #3 log-only S40 cross-lib confirmatory; golden regen 23 pass). Mirror track stays seed-only -- committed 3pt / banked 3pt -- `(real)` |
| 55 | perspective (guPerspectiveF + guPerspective) | 2 | mirror / warm / rodata-literal (libultra/gu/) | 2 | *2* | **6th `gu/` sibling; last clean low-cost libultra leaf (the only remaining libultra candidate with no jal-mismatch / unplaced-ref / needs-header).** 2-fn pack 0x84CE0 (`guPerspectiveF` 0x800A98E0 + `guPerspective` 0x800A9A90 wrapper, 896 B). Verbatim `libultra_modern/src/monegi/gu/perspective.c`, zero edits, matched first `make`, full-make SHA-1 == baserom. Callees all pre-placed (`cosf`/`sinf`/`guMtxIdentF`/`guMtxF2L`); `guint.h` sibling in-tree; names pre-curated → zero symbol adds. rodata-sibling split [0xAD920, rodata]→[0xAD920, .rodata, gu/perspective] + [0xAD960, rodata] for an **8-double** FP pool (0x800D2520..0x2558). **Bank-gotcha (caught pre-finalize, not a spike):** `pick_target.py` rodata-literal pre-flag UNDERCOUNTED 4 of 8 — the pool's 2nd half (0x2540..0x2558) is loaded by the *sibling* `guPerspective`, and the scan was per-primary; the asm (ground truth) gave the full extent. 0 stuck-far/permuter/carried/re-opened. md5-candidate 96->97 (all 97 .c files 0-stub; asm subsegs 176->175). Retro applied 1 of 1 (#1 `rodata_literals`/`rodata_word_refs` now scan the whole subseg via new `iter_subseg_body`, since the `.rodata` sibling places the whole object's `.rodata` — a sibling fn's pooled literals are in the same split extent; + 5 new `tests/tooling/test_decomp_asm.py` regression unit tests, suite 23->28 pass, pick_target golden unchanged). Mirror track stays seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 56 | reg-shim asm TUs (getcount+getcause+getsr+setcompare) | 2 | mirror / asm-mirror PILOT / KMC-as padding gotcha (libultra/os/) | 2 | *2* | **First asm-mirror vendoring (asm analog of the C upstream-mirror). libultra C-mirror band exhausted (97/97 .c 0-stub); scope `libultra` empty → PO pivoted to vendoring the `intrinsic-likely` hand-asm TUs.** Pilot of 4 single-instr reg shims: copy ultralib `src/os/{getcount,getcause,getsr,setcompare}.s` verbatim → `src/libultra/os/`, flip subsegs `asm`→`hasm`, build via the new `VENDOR_ASM` map. **Load-bearing discovery (PO directive to use ultralib's exact flags):** assemble with the KMC/N64 gcc (`LIBULTRA_ASFLAGS`, gcc.mk profile), NOT modern `mips-linux-gnu as` — KMC `as` pads each fn's `.text` up to its 16-byte ROM slot; modern `as` emits the bare 0xC and every following subseg shifts → SHA-1 break (the whole first-attempt failure). Added `MFC0`/`MTC0` to `include/sys/asm.h` (MONEGI header lacked them). No `.c` md5-candidate delta (asm-vendor/hasm housekeeping, not a C-decomp increment): asm subsegs 175→171, hasm 2→6, intrinsic-likely candidates de-noised. 0 stuck-far/permuter/re-opened; ~11 remaining intrinsic-likely TUs + 2 mixed packs carried *by plan* to BACKLOG. Retro applied 3 of 4 (#1 docs/hazards.md#asm-mirror-vendoring + CLAUDE.md index/intrinsic-likely row; #2 `pick_target.py` intrinsic-likely carries vendorable ultralib TU path — `build_asm_tu_index` LEAF/XLEAF/WEAK scan, golden regen 28 pass; #4 pin ultralib gcc.mk as the libultra source in CLAUDE.md; #3 carry-over-wording fix NOT selected). Mirror track stays seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 57 | cache/TLB asm primitives (osWritebackDCacheAll+osUnmapTLBAll+osWritebackDCache+__osProbeTLB) | 4 | mirror / asm-mirror vendoring / clean (libultra/os/) | 4 | *4* | **2nd asm-mirror vendoring sprint (S56 pilot → repeatable pattern). Smallest-first clean cache/TLB single-fn primitives.** Copy ultralib `src/os/{writebackdcacheall,writebackdcache,unmaptlball,probetlb}.s` verbatim → `src/libultra/os/`, add 4 `VENDOR_ASM` pairs, flip subsegs `asm`→`hasm`. **All 4 ultralib version-matched on the FIRST attempt** (one atomic `make extract && make`: `build/mariogolf64.z64: OK`, SHA-1 == baserom) — the per-TU version-match risk (unproven beyond S56's reg shims) did not bite for the cache/TLB family. **Zero enablers beyond the flip:** names pre-curated in `ghidra_symbols.txt` (`LEAF` supplies the symbol → zero symbol adds); all cache/TLB macros (`DCACHE_*`/`C_HWB`/`C_IWBINV`/`CACH_PD`/`TLB*`/`C0_*`) already in `include/libultra/PR/R4300.h` → zero header copies, zero define adds. No `.c` md5-candidate delta (asm-vendor/hasm housekeeping): asm subsegs 171→167, hasm 6→10. 0 stuck-far/permuter/carried/re-opened. Retro applied 2 of 2 (#1 `pick_target.py` `vendorable_tu_missing_defines` pre-check — flags `intrinsic-likely:<tu>.s(needs-define:<MACROS>)` so a missing macro is priced at the gate not at a failing vendor-compile; strips comments + `#include` paths to kill `R4300`/`TLB` false-flags; +1 unit test, suite 28→29 pass, pick_target golden UNCHANGED — inert on the self-contained backlog; #2 docs/hazards.md#asm-mirror-vendoring SHA-breaker bisect protocol + needs-define note + S57 provenance). **Backlog correction:** `osMapTLBRdb` is NOT enabler-blocked (`PR/rdb.h` is in-tree; the pre-check confirms 0 missing macros) — it joins the clean vendorable pool. Mirror track stays seed-only -- committed 4pt / banked 4pt -- `(real)` |
| 59 | gbpakcheckconnector (osGbpakCheckConnector) | 1 | mirror / warm / verbatim C, header no-op (libultra/io/) | 3 | *3* | **Return to the C-mirror track (after S56-58 asm-mirror vendoring); a clean verbatim libultra leaf.** Single fn 0x89320 (1120 B), sibling of the already-banked `gbpak{getstatus,power,readwrite}.c` (its exact callees). Verbatim ultralib `src/io/gbpakcheckconnector.c` with the io-band include adaptation (`PRinternal/controller.h` → `"controller.h"` + `controller_gbpak.h`), zero edits, matched first `make`, full-make SHA-1 == baserom. The sole hazard `needs-header:controller.h(vendorable)` was a **0-work no-op** — the header was already vendored at `include/libultra/internal/controller.h` (which defines `ARRLEN`/`ERRCK` inline) and already on the io band's `-I` set, so the stripped-include adaptation resolved it for free. No `jal-mismatch`/`calls-unplaced`/`refs-unplaced`; name pre-curated in `ghidra_symbols.txt` → zero symbol adds. md5-candidate +1 (the only libultra C subseg flipped this sprint). 0 stuck-far/permuter/carried/re-opened. Retro applied 1 of 1 (#1 `pick_target.py` `include_is_already_vendored` — a missing include whose BASENAME resolves under the `-I` set is tagged `(already-vendored)` not `(vendorable)`, distinguishing a 0-work no-op from a real cp; re-tags the warm io/cont/pfs/vimgr/timer band — `__osContRam{Read,Write}`/`osContInit`/`__osGbpakSetBank` etc. now show their header hazards are no-ops; golden regen, 29 pass). Mirror track stays seed-only -- committed 3pt / banked 3pt -- `(real)` |
| 58 | intrinsic-likely asm TUs (sqrtf+osMapTLBRdb+bcopy) | 3 | mirror / asm-mirror vendoring / cross-dir, WEAK+FPU caveats (libultra/gu+os+libc) | 2 | *2* | **3rd asm-mirror vendoring sprint; first cross-dir batch (gu/ + os/ + libc/) and first to clear caveated TUs.** Copy ultralib `src/gu/sqrtf.s` (0x8BE10, 16 B), `src/os/maptlbrdb.s` (0x8CD10, 96 B), `src/libc/bcopy.s` (0x85DA0, 800 B) verbatim → `src/libultra/`, add 3 `VENDOR_ASM` pairs, flip subsegs `asm`→`hasm`. **All 3 version-matched on the FIRST attempt** (one atomic `make extract && make`: `build/mariogolf64.z64: OK`, SHA-1 == baserom; all 3 `.o` `.text` == subseg slot: 0x10/0x60/0x320). **The two flagged caveats both resolved clean, no special handling:** (a) `bcopy` `#ifdef __sgi` is NOT defined in the KMC build → the `#else` `#define _bcopy bcopy` path makes `LEAF(_bcopy)` emit `bcopy` directly (the in-tree `WEAK` macro was never reached); (b) `sqrtf` has no `.set noreorder`, so the assembler auto-fills the `j ra` delay slot — matched baserom. **Zero enablers beyond the flip:** names supplied by `LEAF` (zero symbol adds); macros self-contained (`C0_*`/`TLBLO_*`/`RDB_*` in `PR/R4300.h`+`PR/rdb.h`; FPU `sqrt.s` native). No `.c` md5-candidate delta (asm-vendor/hasm housekeeping): asm subsegs 167→164, hasm 10→13. 0 stuck-far/permuter/carried/re-opened. Retro applied 0 of 0 (empty suggestion buffer — all first-try clean, no new gotchas; the bcopy/sqrtf caveats were pre-priced at the gate and held). **Pattern note: the asm-mirror pattern now proven across 3 dirs (os/gu/libc) and across the WEAK-alias + FPU-op cases — extends to any single-fn vendorable ultralib `.s` by adding a `VENDOR_ASM` pair.** Mirror track stays seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 60 | mtxcatl (guMtxCatL + guMtxXFML) + ortho (guOrthoF + guOrtho) | 2+2 | mirror / cold+warm / gu-pack decompose + stale-header enabler (libultra/gu/) | 5+2=7 | *7* | **Decomposed the pts-8 `[0x84960,asm]` 4-fn gu pack at the `mtxcatl.c \| ortho.c` upstream-file boundary; banked both verbatim mirrors. The lone hazard `needs-header:../gu/guint.h(vendorable)` was a FALSE-POSITIVE — guint.h is vendored co-located at `src/libultra/gu/guint.h` (S49); pick mis-attributed guMtxXFML to `mgu/mtxxfml.c`, whose `../gu/guint.h` include cascaded the flag.** `mtxcatl.c` (guMtxCatL + guMtxXFML; seed 5 cold) hit one genuine enabler: the in-tree `os_version.h` was the stripped 2.0L revision defining no `VERSION_*` constants, so `#if BUILD_VERSION < VERSION_K` (the guMtxXFML guard) evaluated `0<0`=false and `guMtxXFML` dropped to a **link error** (not a compile error). Fix = add `VERSION_D..L` verbatim from the gcc.mk `os_version.h` (additive; **clean-rebuild SHA-safe** — every existing guard compares `VERSION_J`=`BUILD_VERSION`, evaluates identically; only the `<VERSION_K` guard flips). `ortho.c` (guOrthoF + guOrtho; seed 2 warm) first-pass clean, `guint.h` only. All callees pre-placed (`guMtxL2F`/`guMtxXFMF` in banked `mtxcatf.c`, `guMtxCatF`/`guMtxF2L`/`guMtxIdentF`); all 4 names pre-curated → zero symbol adds. md5-candidate 98→100. 0 stuck-far/permuter/carried/re-opened. Retro applied 4 of 4 (#1 `pick_target.py` `stale_version_header` detector → `stale-header:os_version.h(<V>)` + `docs/hazards.md#stale-vendored-header` + CLAUDE.md index; #2 `missing_includes` normpath for `..` quote-includes; #3 `docs/hazards.md#clean-rebuild-after-shared-header-edit` + CLAUDE.md finalization bullet; #4 `build_upstream_index` deterministic sort [gu<mgu] + version-strip — fixes guMtxXFML/guRotateF mgu-mislabel, removes the phantom `../gu/guint.h` needs-header from guRotateF, golden regen 29 pass; supersedes the S59 #1 claim that guRotateF's `../gu/guint.h` was "genuine"). Mirror track stays seed-only -- committed 7pt / banked 7pt -- `(real)` |
| 61 | rotate (guRotateF + guRotate) | 2 | mirror / warm / near-verbatim (callee substitution) + data-static carve (libultra/gu/) | 3 | *3* | **Verbatim `gu/rotate.c` under VERSION_J, sole edit `guNormalize`→`vec3f_normalize`.** The ROM `jal`s `vec3f_normalize`@0x80029900 — body-compare proved a *genuinely different* fn (game region, not libultra; −0x28 frame + `osSyncPrintf` degenerate-input error path + (0,1,0) fallback + bare `sqrt.s`, vs `guNormalize`'s −0x20 frame + `sqrtf` NaN-check), so the edit was a substitution, not a name reconciliation. The detector flags `jal-count-mismatch:7vs4` + `calls-unplaced:...,xxsine,yxsine,zxsine` were FALSE — under VERSION_J the `#if >= VERSION_K` block is dead, so `xxsine/yxsine/zxsine` are `#define` macros whose definition lines `#define xxsine (x*sine)` matched `C_CALL_RE` as phantom calls. `data-static:0x800C81D0` (the function-local `static float dtor`) handled by a 16B `main_data` `.data` carve (`0xA35D0`→`0xA35E0`); `dtor` name already claimed by the banked sibling `rotaterpy.c`@0x800C81E0, so the S52 drop-extern hoist would collide → kept the static verbatim + carved (the more-verbatim fallback). Bank-gotcha: a 4B carve double-counted (rotate.o(.data) is 16B = 4B + section pad) → ROM +16B reflow; correct carve sized from the compiled `.o`. md5-candidate 100→101. 0 stuck-far/permuter/carried/re-opened; 1 novel bank-gotcha (carve sizing, fixed same-session). Retro applied 3 of 3 (#1 `docs/hazards.md#defines-data` function-local-vs-file-scope hoist/carve distinction + carve-sizing rule; #2 `pick_target.py` `_strip_define_lines` — drops in-body `#define` lines before the call scan so a macro definition line is not a phantom call, golden regen 29 pass; #3 `docs/hazards.md#calls-unplaced` renamed-vs-substituted body-compare step). Mirror track stays seed-only -- committed 3pt / banked 3pt -- `(real)` |
| 62 | invaldcache (osInvalDCache) + invalicache (osInvalICache) | 1+1 | mirror / asm-mirror vendoring / combined-subseg split (libultra/os/) | 2 | *2* | **4th asm-mirror vendoring sprint; first combined-subseg split.** The `[0x823B0,asm]` subseg held BOTH cache-invalidate primitives in one 304B slot (no per-fn boundary in the yaml). Split at the osInvalICache boundary 0x82460 into two `hasm` subsegs (the asm analog of a multi-file C-pack split), vendored ultralib `src/os/invaldcache.s` (176B/0xB0) + `invalicache.s` (128B/0x80) verbatim, +2 `VENDOR_ASM` pairs. Both slots filled exactly, full-make ROM SHA-1 == baserom first try. Macros self-contained (`DCACHE_*`/`ICACHE_*`/`CACH_*`/`C_*`/`K0BASE` in `PR/R4300.h`, `CACHE`/`LEAF`/`END` in `sys/asm.h` — the S57-proven set); names pre-curated in ghidra_symbols → 0 symbol adds. asm subsegs 161→160, hasm 13→15 (md5-candidate .c unchanged 101/101 — asm-mirror is asm→hasm, not asm→c). 0 stuck-far/permuter/carried/re-opened; 1 mild novelty (combined-subseg split, mechanical). Retro applied 2 of 2 (#1 `pick_target.py` `combined-subseg:<n>tu[…]` pre-flag — ≥2 asm-ONLY members from distinct ultralib `.s` files, gated on no-C-upstream so C-mirror gu packs like `sinf+translate` don't mis-flag; surfaced `__osSetFpcCsr` as a 3-TU reg-shim pack; golden regen, suite 29 pass; #2 BACKLOG carry-over wording fix — `__osDisableInt`/`__osRestoreInt` is a partial-TU split in `setintmask.s`, not "source not located"). Mirror track stays seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 63 | reg-shim asm TUs (__osSetFpcCsr+__osSetSR+__osSetWatchLo) + sp (__osSpDeviceBusy) | 1+1+1+1 | mirror / asm-mirror vendoring + warm C / combined-subseg 4-way split (libultra/os/ + libultra/io/) | 4 | *4* | **5th asm-mirror vendoring sprint; first mixed asm-mirror + C-mirror subseg-clear.** Decomposed the pts-8 `__osSetFpcCsr` pack (8-gate fired) — the `[0x8CA50,asm]` 80B subseg held 3 reg-shim asm TUs (`setfpccsr.s`/`setsr.s`/`setwatchlo.s`, continuing the adjacent 0x8CA20/30/40 S56 run) + 1 C-mirrorable `__osSpDeviceBusy` (`io/sp.c`, sibling of dp/ai/si device-busy). `combined-subseg:3tu` (not 4tu — the C member is excluded by the no-C-upstream gate, S62 #1) correctly identified the split shape. 4-way split at 0x8CA60/70/80 → 3 `hasm` + 1 `c`; +3 `VENDOR_ASM` pairs; all 3 `.text` slots exactly 0x10; C mirror trimmed to the dp.c io-band include set (`os_internal.h`+`rcp.h`), 0 iteration, full-make ROM SHA-1 == baserom. **One gate-surfaced enabler:** `setfpccsr.s` references `CFC1`/`CTC1` (FPU control-reg moves) absent from in-tree `sys/asm.h` (only `MTC0`/`MFC0` were vendored at S56) → vendored both verbatim; the `combined-subseg` flag did NOT carry the `needs-define` pre-check (only `intrinsic-likely` did), so the macro gap surfaced at a failing vendor-compile, not at the gate (→ S63 #1). `setfpccsr.s`'s upstream `@bug END(__osSetSR)` is metadata-only, .text clean. All 4 names pre-curated → 0 symbol adds. asm 160→159, hasm 15→18, md5-candidate 101→102. 0 stuck-far/permuter/carried/re-opened; 1 enabler-discovery (CFC1/CTC1, priced reactively). Retro applied 1 of 1 (#1 `pick_target.py` — run `vendorable_tu_missing_defines` on the `combined-subseg:<n>tu` path too, unioning misses across the pack's TUs so a missing asm macro is priced at the gate; provably golden-inert — 0 remaining combined-subseg packs after the banking; golden regen for the banking, suite 29 pass). Mirror track stays seed-only -- committed 4pt / banked 4pt -- `(real)` |
| 64 | gu/lookathil.c (guLookAtHiliteF+guLookAtHilite) | 13 | mirror / verbatim gu-band C mirror / **8-gate EXEMPTION (S64)** | 13 | *13* | **Single cleanest remaining libultra mirror; the 8-gate verbatim-mirror exemption's first use.** Verbatim ultralib `gu/lookathil.c` (VERSION_J), 2-fn 2656B subseg 0x83780. pts-13 tripped the 8-gate purely on SIZE, but the decompose path was mechanically blocked (single upstream file; internal fn boundary guLookAtHilite@0x84104 non-16-aligned) and all callees were placed (`guMtxIdentF`/`sqrtf`[S58]/`guMtxF2L`/self) + both names curated → ran as a documented exemption (CLAUDE.md ## Story points), banked atomic first-try, 0 iteration, full-make ROM SHA-1 == baserom. **rodata sibling-split** for the 6-double ANONYMOUS pool (`[0xAD8E0, .rodata, libultra/gu/lookathil]`, 0x800D24E0..0x2510 = exactly the lookatref boundary). NO vec3f_normalize substitution (that's the align/lookat 0x82B80 combined subseg, not this file). asm subsegs 159→158, md5-candidate 102→103 (all .c stub-free). 0 stuck-far/permuter/carried/re-opened. Retro applied 3 of 3 (#1 8-gate verbatim-mirror exemption [CLAUDE.md+VELOCITY]; #2 `pick_target.py` rodata-literal carve-end boundary; #3 `pick_target.py` `c-combined:<n>file[…]` C analog of combined-subseg surfacing multi-file C packs, e.g. the buried sp pack func_800B16A0→sprawdma|spsetpc|spsetstat; golden regen, suite 29 pass). Mirror track stays seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 65 | string.c (strchr+strlen+memcpy) + bzero asm-mirror | 3+1 | mirror / mixed C-mirror + asm-mirror / **char-signedness band fix (libultra/libc/)** | 4 | *4* | **Clear the [0x860C0] libc pack; first mirror sprint with real variance + a band-wide build-config correction.** Decomposed the pts-8 4-fn pack at the bzero|string boundary (rom 0x86160, 16-aligned) → 1 `hasm` (bzero, ultralib `libc/bzero.s` vendored via VENDOR_ASM, .o .text=0xA0, bcopy.s pattern, proven at gate 0 iter) + 1 `c` (ultralib `libc/string.c` verbatim). **char-signedness:** the verbatim string.c SHA-missed — ROM compiled it SIGNED-char (`strchr`/`strlen` load `lb`+`sll/sra` + phantom empty frame) but the band forced `-funsigned-char` (`memcpy` matched either way). Initial per-file `-fsigned-char` override → then the authoritative `make clean` + global `-fsigned-char` rebuild reproduced the baserom SHA-1 EXACTLY, proving `-funsigned-char` a wrong band default → **flipped LIBULTRA_CFLAGS to `-fsigned-char`, removed the override** (ROM-proven deviation from ultralib's J profile, which adds `-funsigned-char` for D–J). asm subsegs 158→157, hasm 18→19, md5-candidate 103→104. 0 stuck-far/permuter/carried/re-opened (1 build-config gotcha, not a code-iteration stall). Retro applied 3 of 3 (#1 `pick_target.py` pack-member labels resolve `.s` TUs `bzero=?`→`bzero=bzero.s`; #2 `docs/hazards.md#char-signedness` + CLAUDE.md index row, pre-flag descoped as #3 subsumed it; #3 band flip to `-fsigned-char`; golden regen, suite 26 pass). **Calibration: mirror-track point-mass VIOLATED — a verbatim mirror needed a build-flag recovery (+1 novel-gotcha if classical-tracked); still logged seed-only per the mirror-track rule, committed 4pt / banked 4pt.** `(real)` |
| 66 | gu/cosf.c (cosf) + gu/sinf.c (sinf) | 1+1 | mirror / verbatim gu-band C mirror / **first C `#pragma weak` mirror; named-rodata caveat RETIRED + shared recover-extern (libultra/gu/)** | 2+2=4 | *4* | **Banked the two gu polynomial-trig leaves; the libultra cheap-mirror band's last clean siblings.** Decomposed both pts-13 combined packs at the upstream-file boundary (8-gate satisfied by decompose, S60 pattern): `cosf` out of `[0x82B80,asm]` 5-fn (align/cosf/lookat) at 0x82F20+0x83070; `sinf` out of `[0x85B30,asm]` 3-fn (sinf/translate) at 0x85CD0 → 2 verbatim ultralib VERSION_J `gu/` mirrors, both 0 iteration, full-make ROM SHA-1 == baserom. **First C `#pragma weak` mirror:** `#pragma weak cosf=__cosf` + `#define fcos __cosf` — KMC gcc 2.7.2 emits both symbols, the curated weak alias `cosf` resolves to the subseg address, no edit (C analog of S58 bcopy `_bcopy=bcopy`). **Shared gate-missed recover-extern** `__libm_qnan_f`=0x800D2640 (size:0x4): the NaN-path return refd by BOTH fns as anonymous `D_800D2640` — refs-unplaced can't bind an anon label, and the fns were hidden pack members (primary=align.c) so refs_unplaced never scanned cosf.c/sinf.c. rodata sibling carves: `sinf` ANONYMOUS pool `[0xAD960,.rodata,libultra/gu/sinf]` (.o 0x60 exact fit); `cosf` NAMED pool `[0xAD860,.rodata,libultra/gu/cosf]` — the **S64 named-rodata collision CAVEAT verified PHANTOM** (splat carves cleanly with ghidra_symbols labels kCoeff4..kOneHalf2 inside; PO-directed byte-compare of `.o(.rodata)` vs ROM 0xAD860..0xAD8C0 confirmed 96B identical → matches libultra, not discarded). VERIFIED libultra NOT libkmc (ROM rodata byte-id to ultralib; libkmc has no cosf/sinf/__libm_qnan_f). md5-candidate 104→106 (all 106 .c stub-free); asm subsegs 157→158 (cosf split nets +1; sinf split nets 0; surfaced `gu/translate.c`). 0 stuck-far/permuter/carried/re-opened (1 gate-missed enabler recovered in-execution, not a spike). Retro applied 3 of 3 (#1 `docs/hazards.md` .rodata-sibling named-pool note — retire the phantom caveat + S66 provenance; #2 `pick_target.py PRAGMA_WEAK_RE` keys weak aliases in `build_upstream_index` [cosf=cosf not cosf=?], provably golden-inert — the only weak fns cosf/sinf are now banked, suite 29 pass; #3 `docs/hazards.md` `#pragma weak` C-mirror pattern note). Carried (tooling follow-up, not a file): the deeper half of #2 — union `refs_unplaced` over c-combined members so a hidden member's `__`-extern surfaces at the gate. Mirror track stays seed-only -- committed 4pt / banked 4pt -- `(real)` |
| 67 | gu/translate.c (guTranslateF+guTranslate) | 2 | mirror / verbatim gu-band C mirror / **last gu asm leaf; gu band 0x82F20..0x85CD0 now fully decompiled** | 2 | *2* | **The cleanest mirror of the band and the gu band's last un-flipped leaf** (the perspective S55 → lookathil S64 → sinf/cosf S66 → translate S67 trail ends here). Verbatim ultralib `gu/translate.c` (VERSION_J), byte-identical `cp`, 0 iteration, full-make ROM SHA-1 == baserom first try. Zero enablers beyond the one yaml flip ([0x85CD0,asm]→c): both fn names pre-curated (guTranslateF=0x800AA8D0, guTranslate=0x800AA924), `guint.h` vendored, callees `guMtxIdentF`/`guMtxF2L` placed, NO float literals → NO rodata-sibling split (simpler than its perspective/lookathil siblings). The `pack:2fn` flag was the single-upstream-file false-flag class (both fns mirror atomically). md5-candidate 106→107 (all 107 .c stub-free). 0 stuck-far/permuter/carried/re-opened. Retro applied 1 of 2 (PO selected #2 only): `pick_target.py` emits `single-file-pack:<n>fn[…]` for a pure single-upstream-file C pack [all members → one stem] instead of the split-implying `pack` — display-only (pack penalty keys on nfns>1, not the kind), CLAUDE.md hazard-index + docs/hazards.md synced, golden regen (translate banked changed the set), suite 29 pass. Not applied: #1 (BACKLOG gu-trail-retired note). Mirror track stays seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 68 | gu/align.c (guAlignF+guAlign) | 2 | mirror / warm / known-edit near-verbatim (**S61 rotate.c twin**) | 3 | *3* | **The verbatim twin of S61 rotate.c** — a known-edit near-verbatim libultra gu mirror, first-build match (full-make ROM SHA-1 == baserom, 0 iteration). Verbatim ultralib `gu/align.c` (VERSION_J, no version conditionals) + the **one** S61-proven edit `guNormalize`→`vec3f_normalize` (the asm `jal`s `vec3f_normalize`@0x80029900, a *game-region* substituted callee, implicitly declared exactly as rotate.c). `guAlign` (func_800A794C, the s16 wrapper) **inlines the full guAlignF body** (-0xA8 frame) — gcc -O2 same-TU inlining, as predicted. Shared `static float dtor` (=`3c8efa35`) placed via a `.data` sibling `[0xA35A0, .data, libultra/gu/align]` (0x10-byte slot, rotate's `[0xA35D0]` twin). **Process: the `.data` carve was DEFERRED gate→execution** — an INCLUDE_ASM stub emits no `.data`, so an ld-section sibling shifts the data segment and fails the gate green-ROM check; the gate flips text only (S68 #1, now codified). md5-candidate 107→108 (all 108 .c stub-free); remaining libultra asm-flip candidates 16→15. 0 stuck-far/permuter/carried/re-opened. Retro applied 3 of 3: #1 ld-section split-timing (sprint-plan Step-7 + docs/hazards.md #defines-data carve-timing + #.rodata-sibling timing); #2 `pick_target.py twin-of:<file>` hint (a candidate re-emitting a function-local static whose mirror dir already carved that ld-section → names the proven sibling; `_static_carve_siblings` dot-aware yaml scan since SUBSEG_RE drops the leading dot); #3 `pick_target.py` single-file-pack recognition for an unnamed trailing member (the gu F-variant + s16-wrapper idiom — when the one named C stem's upstream file defines exactly nfns functions). golden regen (align banking + the new flags on out-of-window rows), suite 26 pass / 3 skip. Mirror track stays seed-only -- committed 3pt / banked 3pt -- `(real)` |
| 69 | gu/lookat.c (guLookAtF+guLookAt) | 13 | mirror / verbatim gu-band C mirror / **8-gate EXEMPTION (S64, GENERALIZED S69); GU BAND CLOSED** | 13 | *13* | **The TRUE last un-flipped gu asm leaf; closes the gu band (all 109 .c stub-free).** Verbatim ultralib `gu/lookat.c` (VERSION_J), 2-fn 1808B subseg 0x83070, byte-identical `cp`, 0 edits, 0 iteration, full-make ROM SHA-1 == baserom. **Carry-over CORRECTED:** NOT the `vec3f_normalize` substitution class (that's align/rotate) — guLookAtF uses `sqrtf` **inline**, a PURE verbatim mirror like S64 lookathil; callees `guMtxIdentF`/`sqrtf`×3/`guMtxF2L` placed; `guLookAt` inlines guLookAtF (-O2 same-TU, S68 guAlign pattern). pts-13 tripped the 8-gate purely on SIZE → **verbatim-mirror exemption, GENERALIZED at S69:** the inner boundary guLookAt@0x800A7FF0 is **16-aligned** (unlike lookathil's `non16align`), but a `single-file-pack` is decompose-blocked regardless (you cannot mirror half a source file) — the `non16align` test was one mechanical case of single-file-pack, not the rule. rodata sibling carve `[0xAD8C0, .rodata, libultra/gu/lookat]` = `.o(.rodata)` 0x20 exact (anon pool 0x800D24C0..0x800D24E0, the whole generic block bounded by lookathil). One gate enabler: `guLookAt`=0x800A7FF0 added to symbol_addrs (guLookAtF pre-curated). md5-candidate 108→109, asm subsegs 156→155. 0 stuck-far/permuter/carried/re-opened. Retro applied 2 of 2 (#1 generalize the exemption condition (b) — `single-file-pack ⇒ decompose-blocked` regardless of inner 16-alignment, CLAUDE.md + VELOCITY; #2 BACKLOG carry-over `vec3f_normalize` correction). No tooling/golden change (pick_target's `single-file-pack` tag from S67 already IS the exemption signal; the generalization is doctrine-only). Mirror track stays seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 70 | osMapTLB + osUnmapTLB (libultra os TLB asm-mirror) | 2 | mirror / asm-mirror vendoring / TLB CP0 primitives, un-named-TU identify (libultra/os/) | 2 | *2* | **6th asm-mirror vendoring sprint; first to identify an un-named (`func_<addr>`) TU at the gate.** Vendored ultralib `os/maptlb.s` (osMapTLB, 0x87F40) + `os/unmaptlb.s` (osUnmapTLB, 0x880C0) verbatim, flip 2 subsegs `asm`→`hasm`, +2 `VENDOR_ASM` pairs +2 curated names. Both byte-id to ROM disasm (the `_DEBUG && __sgi` block compiles out under KMC — emitted code = post-`#endif` only), full-make ROM SHA-1 == baserom **first make, 0 iteration**. `.o(.text)` 0xC0/0x40 = 0xB4/0x3C bodies 16-padded (KMC-`as`, S56) → exact slot fill. **Resolved the ~14-sprint carry-over** "un-named func_<addr>, no identified ultralib .s yet": one MCP `disassemble_function` named both by their CP0/TLB signature (Index/EntryHi/EntryLo + tlbwi). All macro/header infra pre-present (MFC0/MTC0 S56, C0_*/TLBLO_*/K0BASE in R4300.h, ta0=$12 o32). No `.c` md5-candidate delta (asm→hasm housekeeping): asm subsegs 155→153, hasm 19→21. 0 stuck-far/permuter/carried/re-opened. Retro applied 1 of 2 (PO selected #1 only): `pick_target.py` `privileged_asm` fingerprint — an un-named asm subseg with a privileged op gcc never emits (`tlbwi`/`mtc0`/`mfc0`/`eret`/`cfc1`/…) now flags `intrinsic-likely:cp0-asm(identify-TU)` even with branches/calls (broader than the pure-shim `intrinsic_likely`), so smallest-first stops mis-surfacing hand-asm as classical (caught the 496B CP0 routine `audio_sched_thread_entry`@0x800B3E50, prior `-`); +4 unit tests, CLAUDE.md index + docs/hazards.md synced, golden regen (1 vetted row), suite 33 pass. #2 (asm-mirror gate-note) NOT selected. Mirror track stays seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 71 | io/crc.c (__osContAddressCrc+__osContDataCrc) | 2 | mirror / coddog-reclassified verbatim / **FIRST coddog-driven target; pts-13 false-price corrected** | 2 | *2* | **First coddog cross-ref sprint (PO directive at gate).** Verbatim ultralib `src/io/crc.c` (VERSION_J branch), 2-fn 240B subseg 0x8CC20 (= crc.o `.text` 0xF0 exact), byte-identical `cp`, 0 edits, 0 iteration, full-make ROM SHA-1 == baserom. **Seed correction:** `pick_target` mis-seeded pts-13 (`upstream none` + `pack:2fn`) ONLY because both fns were un-named; `coddog compare2` matched both 99.99% to crc.c → a trivial verbatim 2-fn mirror. **Zero callees** (nm: no `U` syms, pure CRC integer math), `PR/os_internal.h` in-tree, no rodata, `u8*` arg (no char-signedness). 8-gate fired on pts-13 → cleared by the verbatim-mirror exemption (single-file-pack, zero callees, names curated at gate); the pts-13 is the **S43 tooling-artifact false-price class** (a tooling FP, not real complexity), logged at the corrected mirror seed **2pt**, NOT 13. md5-candidate 109→110 (all 110 .c stub-free), asm subsegs 153→152. Two gate enablers: `__osContAddressCrc`=0x800B1820 + `__osContDataCrc`=0x800B188C added to symbol_addrs. **Unlocks `__osContRamRead`/`__osContRamWrite`** (CRC helpers now placed). 0 stuck-far/permuter/carried/re-opened. Retro applied 4 of 4: #1 `docs/hazards.md#coddog-cross-ref` + CLAUDE.md index/preamble; #2 `pick_target.py build_coddog_index` reads `tools/coddog/coddog_map.tsv` → `coddog-mirror:<file>@<pct>` flag + ≥99% non-audio re-price (env-overridable `CODDOG_MAP`, golden regen map-free, new repricing test); #3 BACKLOG S71 regime note (libultra ~all verbatim-mirrorable; classical only libc xprintf band; audio header-gated); #4 `tools/coddog_sweep.sh`+`parse_map.py` harness + `make coddog-sweep`. suite 34 pass. Mirror track stays seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 72 | io/epirawread.c (__osEPiRawReadIo) + io/pfsselectbank.c (__osPfsSelectBank) | 2+2=4 | mirror / coddog-driven verbatim io pair / clean (libultra/io/) | 4 | *4* | **2nd coddog cross-ref sprint; the clean io-band pair the S71 map revealed.** Two single-fn verbatim ultralib VERSION_J mirrors, both byte-clean first build, 0 iteration, full-make ROM SHA-1 == baserom. `epirawread.c` = the read-twin of banked `epirawwrite.c` (EPI_SYNC+IO_READ via vendored `piint.h`); its bare upstream `assert(data != NULL)` (outside `_DEBUG`) was `_DEBUG`-wrapped per the banked `sirawread` convention — NDEBUG undefined here, so a bare assert emits `jal __assert`; read==write subseg size (both 0x170) confirms the ROM strips it (new `#assert-strip` hazard). `pfsselectbank.c` = include adapted `PRinternal/controller.h`→`"controller.h"` (internal path), callee `__osContRamWrite` pre-placed @0x800AF610. md5-candidate 110→112 (all 112 .c stub-free), asm subsegs 152→150. Two gate enablers: `__osEPiRawReadIo`=0x800B0710 + `__osPfsSelectBank`=0x800AE920 added to symbol_addrs. **The other coddog io leaves are traps, not clean** (the S72 #1 fix now surfaces them): piacs.c = defines-data + `static piAccessBuf` (re-priced pts-3→5); motor.c = defines-data + file-static; contquery = pts-8 c-combined/8-gate. 0 stuck-far/permuter/carried/re-opened. Retro applied 2 of 2: #1 `pick_target.py build_rows` coddog trap re-scan (re-run defines_data/file_static/needs_header on the coddog-resolved `.c`; shared `_tagged_missing_includes` helper, `_coddog_upstream_path`; +1 unit test, golden regen, suite 35 pass); #2 `docs/hazards.md#assert-strip` playbook + CLAUDE.md index row + coddog-cross-ref trap-re-scan note. Carried (tooling follow-up, not a file): optional `bare-assert` advisory flag. Mirror track stays seed-only -- committed 4pt / banked 4pt -- `(real)` |
| 73 | gu/position.c (guPositionF+guPosition) | 2 | mirror / coddog-driven verbatim gu / defines-data .data carve (libultra/gu/) | 3 | *3* | **3rd coddog cross-ref sprint; closes the gu text band (last flippable asm).** Verbatim ultralib `gu/position.c` (coddog 99.99), byte-identical `cp`, 0 edits, 0 iteration, full-make ROM SHA-1 == baserom. `guPositionF` does the trig (sinf×3/cosf×3); `guPosition` inlines guPositionF + calls `guMtxF2L` (both load the same `D_800C81B0` dtor). **defines-data carve:** the function-local `static float dtor = π/180` re-emits a 16B `.data` (4B + 12B pad) → carve `[0xA35B0, .data, libultra/gu/position]`→`[0xA35C0, data]` for random's `xseed` remainder; align.c/rotate.c are the identical 16B precedents (S61/S68). Callees `sinf`/`cosf`/`guMtxF2L` pre-placed; header `guint.h` same-dir; no `.rodata` (1.0/0.0 inline immediates). **The coddog row UNDER-flagged it** (clean pts-3, no defines-data): the asm-side `data-static` pre-flag (S52) does NOT fire on un-named coddog candidates, and the S72 coddog re-scan ran only `defines_data_globals` (skips `static`, scans only brace-depth 0) — doubly blind to a fn-local static. Caught at the gate by reading the upstream + asm data refs. md5-candidate 112→113 (all 113 .c stub-free), asm subsegs 150→149. Two gate enablers: `guPositionF`=0x800A9C60 + `guPosition`=0x800A9E38 added to symbol_addrs. 0 stuck-far/permuter/carried/re-opened. Retro applied 2 of 2: #1 `pick_target.py` `defines_local_static_data()` — greps function-body `static <type> <name> = <init>;` and merges into the `defines-data` hazard on BOTH the named-upstream + coddog re-scan paths (the source-side backstop the coddog band needed; verified flags `dtor` on position/rotate/align/rotaterpy, clean on perspective/epirawread; suite 35 pass, no golden regen); #2 `docs/hazards.md#defines-data` S73 source-side-backstop note. Mirror track stays seed-only -- committed 3pt / banked 3pt -- `(real)` |
| 74 | io/contreaddata.c (osContStartReadData+osContGetReadData+__osPackReadData) | 5 | mirror / coddog-driven verbatim io / c-combined decompose + 6 recover-extern (libultra/io/) | 5 | *5* | **4th coddog cross-ref sprint; decomposed the pts-8 c-combined 0x82590 cont subseg (= contquery.c head + contreaddata.c tail) at the upstream-file boundary and banked the clean TAIL.** Verbatim ultralib `io/contreaddata.c` (VERSION_J), byte-identical `cp`, 0 edits, 0 iteration, full-make ROM SHA-1 == baserom. 16-aligned split [0x82590,asm]→keep [0x82590,asm] (contquery head) + add [0x82630,c,libultra/io/contreaddata] (tail). **Pure text mirror** (no ld-section sibling — static `__osPackReadData` is text, constants are immediates, all touched data lives in still-asm controller.c). **Heaviest recover-extern load to date — 6 symbols, all found by MANUAL gate recon (the ranker under-flagged):** the un-named `func_` members blocked the named-keyed refs/calls-unplaced scan, and the named members (contreaddata) didn't re-surface them, so the row showed NO calls/refs-unplaced despite 3 SI callees (`__osSiGetAccess`=0x800AC164, `__osSiRawStartDma`=0x800AC060, `__osSiRelAccess`=0x800AC1D0) + 3 data globals (`__osContPifRam`=0x800FE340 size:0x40, `__osContLastCmd`=0x8012F4CC, `__osMaxControllers`=0x80105274) being unplaced. `D_800FE37C`=__osContPifRam+0x3C (pifstatus addend, auto-resolved). One non-codegen fix: verbatim `PRinternal/{controller,siint}.h` didn't resolve (in-tree copies at `internal/`, not `PRinternal/`) → adapted to bare includes (matches banked sibling pfsselectbank.c; SHA-neutral). md5-candidate 113→**114** (all 114 .c stub-free). 0 stuck-far/permuter/carried/re-opened. Retro applied 2 of 2: #1 `pick_target.py` `_append_recover_hazards()` factored out + run on the coddog re-scan path too (unions refs/calls-unplaced over the coddog-resolved upstream so an un-named func_'s recover-extern load is priced at the gate; golden regen, suite 35 pass); #2 the `needs-header (already-vendored)` tag now shows the in-tree adapt target `(already-vendored,adapt->internal/<h>)` via `already_vendored_intree_path()`, so the include-line edit is priced at the gate not hit at first build. Carry: contquery.c head (the direct coddog 99.99 hit). **Calibration note: the coddog-mirror sub-band runs seed 3–5 (recover-extern + c-combined decompose load), heavier than the 2.0 clean-singleton anchor — the mirror point-mass is drifting up as the band depletes to its trap-laden tail.** Mirror track stays seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 75 | io/contquery.c (osContStartQuery+osContGetQuery) | 2 | mirror / coddog-driven verbatim io / near-free-retry of the S74 split HEAD (libultra/io/) | 5 | *5* | **5th coddog cross-ref sprint; banked the asm HEAD left by S74's tail split — the 0x82590 cont subseg is now fully C.** Verbatim ultralib `io/contquery.c` (VERSION_J, coddog 99.99), byte-identical `cp` (`PRinternal/{controller,siint}.h`→bare, same as S74 sibling), 0 edits, 0 iteration, full-make ROM SHA-1 == baserom. Flip [0x82590,asm]→[0x82590,c,libultra/io/contquery]. **Pure text mirror** — defines no data/static (only refs the placed externs `__osContPifRam`=0x800FE340/`__osContLastCmd`=0x8012F4CC), no ld-section sibling, no rodata (immediates only). **A MECHANICAL REPLAY of the S74 carry-over** (the model near-free-retry pre-scope: all 4 addresses verbatim-correct, 0 rework). Four gate enablers, all pre-listed in that carry-over: 2 fn names `osContStartQuery`=0x800A7190 + `osContGetQuery`=0x800A7210, 2 calls-unplaced callees `__osPackRequestData`=0x800A7660 + `__osContGetInitData`=0x800A75AC (jal targets re-confirmed via Ghidra disasm; both defined in still-asm controller.c 0x82810). md5-candidate 114→**115** (all 115 .c stub-free), asm subsegs 170→169. 0 stuck-far/permuter/carried/re-opened. Retro applied 2 of 2: #1 `pick_target.py build_rows` suppresses the weaker `maybe-upstream` IDF guess when a ≥99% non-audio `coddog-mirror` hit is on the SAME row (S75 func_800A7190 carried both `coddog-mirror:src/io/contquery.c@99.99` AND a mis-pointed `maybe-upstream:voice*` — the definitive identity now stands alone); +1 unit test `test_coddog_suppresses_maybe_upstream`, golden regen (the contquery flip), suite 36 pass; #2 `BACKLOG.md ## Carry-overs` two-kind format (spike vs near-free-retry) + the 5-point near-free-retry **completeness checklist** (flip line / placed-ref inventory / new-recovery vrams / include-adapt / upstream pin) that made this sprint a replay, + `sprint-review.md` pointer. **Calibration: confirms the S74 calibration note — the coddog io band's traps (piacs/motor) are the only un-banked tail; clean coddog leaves are mined out after contquery.** Mirror track stays seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 76 | io/devmgr.c (__osDevMgrMain) | 1 | mirror / coddog-named verbatim io / **switch-jtbl .rodata sibling** (libultra/io/) | 3 | *3* | **Smallest clean libultra coddog mirror remaining (`__osDevMgrMain` is NAMED → coddog map confirms `src/io/devmgr.c`@99.99; the coddog-mirror flag only surfaces on un-named func_).** Verbatim ultralib `io/devmgr.c` (VERSION_J), `cp` + 1 include adapt (`PRinternal/piint.h`→bare, S72 epirawread convention), 0 body edits, full-make ROM SHA-1 == baserom. Flip [0x7E9F0,asm]→[0x7E9F0,c,libultra/io/devmgr]. Both flagged hazards were the **indirect-call false-positive class**: `jal-count-mismatch:25vs21` + `calls-unplaced:dma,edma` = 4 `jalr` calls through `dm->dma`/`dm->edma` (OSDevMgr struct fn-ptr members; C-call-counted, not `jal`; 25−4=21). Zero symbol adds (name pre-curated), all 7 callees + all LEO/OSDevMgr macros pre-placed. **One in-execution surprise (resolved, NOT a spike):** the `switch (mb->hdr.type)` compiles to a jump table `jtbl_800D2280`@0x800D2280/ROM 0xAD680, whose 7 `.word .L800A39xx` entries are the fn's own internal labels → flipping text→C deleted them → undefined-ref link break the gate's text-only green-ROM check cannot catch by construction. Fixed with a `.rodata` sibling carve `[0xAD680, .rodata, libultra/io/devmgr]` (split AD5E0 at 0xAD680); the C-compiled jtbl reproduced the 8-word block (7 case + 1 zero pad to 0x20) byte-for-byte. md5-candidate 115→**116** (all .c stub-free), asm subsegs 148→147. 0 stuck-far/permuter/carried/re-opened. Retro applied 3 of 3: #1 `pick_target.py` `rodata-jtbl:0x<vram>` pre-flag (jump-table analog of `rodata-literal`; `decomp_asm.rodata_jtbls` scans `%lo(jtbl_…)` whole-subseg, wired in the SHARED recover-battery so it prices both named-upstream + coddog paths; +3 unit tests, golden regen, suite 39 pass); #2 `docs/hazards.md#rodata-sibling-yaml-pattern` switch-jtbl sub-case (automatic byte-match + zero-pad note + gate-can't-catch); #3 CLAUDE.md hazard-index row. **Calibration: the jal-mismatch/calls-unplaced hazards on a coddog-named libultra fn are the indirect-call false-positive class — do NOT route to classical; the rodata-jtbl was the only real work and is now pre-flagged.** Mirror track stays seed-only -- committed 3pt / banked 3pt -- `(real)` |
| 77 | io/sp c-combined subseg [0x8CAA0] → spgetstat+spsetstat+spsetpc+sprawdma | 1+1+1+1 | mirror / coddog+decompose verbatim io (libultra/io/) / **c-combined 4-way split** + assert-strip (sprawdma) + **caller-evict (func_800B16A0→func_800AB600.c)** | 1+1+1+1=4 | *4* | **6th coddog cross-ref sprint; cleared a whole c-combined subseg into 4 single-fn mirrors.** Decomposed the 4-fn `c-combined:3file` subseg at upstream-file boundaries (all 16-aligned: 0x8CAA0/AB0/AC0/AF0). Verbatim ultralib VERSION_J: spgetstat/spsetstat/spsetpc pure `cp` (IO_READ/IO_WRITE SP regs, only `PR/rcp.h`+`PR/os_internal.h`); sprawdma assert-strip (3 bare asserts `_DEBUG`-wrapped per S72 sirawread convention; asm-confirmed no `jal __assert`, callees `__osSpDeviceBusy`/`osVirtualToPhysical` placed). No data/rodata siblings (register immediates only). **Leader `func_800B16A0` confirmed `__osSpGetStatus` at the gate via Ghidra disasm** (`lui 0xa404;ori 0x10;lw` = IO_READ(SP_STATUS); coddog couldn't fingerprint a 4-instr fn) → the 3-file `c-combined` became a 4-file clear. **Gate surprise (resolved, NOT a spike):** adding `__osSpGetStatus`=0x800B16A0 renamed the symbol in the scaffold → the banked classical caller `src/main/func_800AB600.c` (`func_800B16A0()`) failed to link → renamed call site to `__osSpGetStatus` (same addr, SHA-neutral); the green-ROM gate check caught it. md5-candidate 116→**120** (all .c stub-free), asm subsegs 147→146. 0 stuck-far/permuter/carried/re-opened. Retro applied 1 of 1: #1 `pick_target.py` `caller-evict:<func_vram>@<file>` pre-flag (`src_func_callers()` walks `src/` for un-named members a banked C file references, INCLUDE_ASM-excluded; display-only; +1 unit test `test_caller_evict_flag`, golden regen [0x8CAA0 row removed + caller-evict on func_80029250], suite 40 pass) + `docs/hazards.md#caller-evict` + CLAUDE.md hazard-index row. **Calibration: a c-combined subseg's un-named leader can be a clean mirror coddog just can't fingerprint (too few instrs) — disasm it at the gate before assuming the split leaves an asm remnant.** Mirror track stays seed-only -- committed 4pt (plan-gate est ~6) / banked 4pt -- `(real)` |
| 78 | io [0x8CE90] → gbpaksetbank (__osGbpakSetBank) + pfsisplug (osPfsIsPlug+__osPfsRequestData+__osPfsGetInitData) | 1+3 | mirror / coddog+decompose verbatim io (libultra/io/) / **c-combined 2-way split** + **defines-data fast-path (pfsisplug __osPfsPifRam BSS)** | 1+3=4 | *4* | **7th coddog cross-ref sprint; cleared the 4-fn c-combined subseg [0x8CE90] into 2 mirror files.** Decomposed at the upstream-file boundary (split 0x8CF50/vram 0x800B1B50, 16-aligned): gbpaksetbank HEAD (1fn) + pfsisplug TAIL (3fn). Verbatim ultralib VERSION_J both: **gbpaksetbank pure `cp`** (0 edits; callees `__osContRamWrite`[S43]+`osGbpakInit`[S44] placed, hdrs os_internal.h/controller.h vendored); **pfsisplug defines-data fast-path** — file defines `OSPifRam __osPfsPifRam`@0x801B7EF0 (64-byte BSS global in the shared main `bss` blob), dropped the def → vendored `internal/controller.h:227` extern + `symbol_addrs __osPfsPifRam=0x801B7EF0 size:0x40`, **NO `.bss` carve** (the sized blob already reserves the range; bss has no ROM bytes → SHA-neutral). Include adapt `PRinternal/{controller,siint}.h`→bare (kept `PRinternal/macros.h`, resolves via -I include/libultra). Both files full-make ROM SHA-1 == baserom, 0 iteration. 3 gate fn-name adds (osPfsIsPlug=0x800B1B50/__osPfsRequestData=0x800B1CCC/__osPfsGetInitData=0x800B1D70; __osGbpakSetBank pre-placed S45). coddog fuzzy-labelled func_800B1D70 as controller.c's __osContGetInitData (a near-twin); asm call-structure + verbatim SHA confirm it is pfsisplug's __osPfsGetInitData. md5-candidate 120→**122** (all .c stub-free), asm subsegs 146→145. 0 stuck-far/permuter/carried/re-opened. Retro applied 1 of 1: #1 `pick_target.py build_rows` — scan ALL subseg members for a definitive coddog hit (not just `fns[0]`) → surface the tail identity even under a named/mis-attributed leader, AND exempt such subsegs from the over-broad `carried` name-drop filter (`carry_over_names()` scoops every backticked token from the digest log; S45 name-dropped `__osGbpakSetBank` → [0x8CE90] was de-ranked invisible); +2 unit tests (`test_coddog_tail_overrides_carried_namedrop`, `test_carried_namedrop_still_drops_without_coddog`), golden stable (the only live case [0x8CE90] is now flipped), suite 42 pass. **Calibration: the [0x8CE90] subseg was found only via the S77 band-note + manual coddog grep — the named-leader+coddog-tail blind spot (also S76 devmgr) is now ranker-visible. Follow-up logged: `UPSTREAM_DEF_RE` matches forward prototypes as defs (__osGbpakSetBank mis-attributed to gbpakreadwrite.c).** Mirror track stays seed-only -- committed 4pt / banked 4pt -- `(real)` |
| 79 | io contramread.c (__osContRamRead) + contramwrite.c (__osContRamWrite) | 1+1 | mirror / coddog @99.99 verbatim io cont-pak RAM I/O pair (libultra/io/) / **trailing-128-align pad split** + **defines-data fast-path (__osPfsLastChannel)** | 3+3=6 | *6* | **8th coddog cross-ref sprint; the S71-named contram sibling pair, smallest-first off the re-priced list.** Both verbatim ultralib VERSION_J; include adapt `PRinternal/{controller,siint}.h`→bare (kept `PRinternal/macros.h`). **contramread defines-data fast-path** — file defines `s32 __osPfsLastChannel = -1` → drop to `extern` + `symbol_addrs __osPfsLastChannel=0x800C9450 size:0x4` (recovered `lui 0x800d`/`lw -0x6bb0`, disjoint), NO `.data` carve (byte already in the data blob); contramwrite externs it. contramread mirrored clean (`.o .text` 0x1F0 == slot). **contramwrite SHA-missed first make (ROM 96B short):** the 516B fn (129 instrs byte-identical) is followed by 27 trailing nops padding to the 128-aligned `osAfterPreNMI`@0x800AF880; GCC emits only 16B align (`.o`=0x210), so the 0x60 residual was lost. Fix: split nop-pad `[0x8AC20, asm]` (=0x8AA10+0x210). Re-extract + make → ROM SHA-1 == baserom, 0 real iteration. md5-candidate 122→**124** (all .c stub-free), asm subsegs 145→144 (+1 pad subseg). 0 stuck-far/permuter/carried/re-opened. Retro applied 2 of 2: #1 `pick_target.py` `trailing-pad:<n>B@<align>` pre-flag (`code_end_rom` in decomp_asm; fires only when the next boundary is >16-aligned, the contramread sibling slot==fn-size shows the FP guard) + **all-nop asm subseg skip** (retired the pad subseg AND 8 pre-existing all-nop `func_ovl*_801F4A30` overlay stubs from the ranker); #2 `docs/hazards.md#trailing-alignment-pad-after-a-c-mirror` + CLAUDE.md index row; golden regen, suite 42 pass. Mirror track stays seed-only -- committed 6pt / banked 6pt -- `(real)` |
| 80 | io/pfsgetstatus.c (__osPfsGetStatus+__osPfsRequestOneChannel+__osPfsGetOneChannelData) | 3 | mirror / coddog @99.99 verbatim io pfs (libultra/io/) / clean standalone-subseg cp + 2 sibling-name + 1 recover-extern | 5 | *5* | **9th coddog cross-ref sprint; clean single-file 3-fn pfs mirror, smallest-first off the re-priced list.** Verbatim ultralib VERSION_J `io/pfsgetstatus.c`, byte-identical body (only `PRinternal/{controller,siint}.h`→bare include adapt), 0 edits/iteration, full-make ROM SHA-1 == baserom. Flip [0x89B10,asm]→[0x89B10,c,libultra/io/pfsgetstatus], standalone subseg (no split). Gate enablers: recover `__osPfsInodeCacheBank`=0x800C9444 size:0x1 (byte store `li 0xfa; lui 0x800d; sb -0x6bbc(at)`; defined by still-asm contpfs.c, extern for us) + curate 2 sibling names `__osPfsRequestOneChannel`=0x800AE800 / `__osPfsGetOneChannelData`=0x800AE894 (`__osPfsGetStatus` pre-placed). **2 hazard flags debunked at gate — both pick_target false-positives fixed this retro:** `refs-unplaced:__OSContRequesFormatShort` = a struct TYPE in the (unresolved) `PRinternal/controller.h`; `jal-count-mismatch:7vs6` = the non-J `#else` branch's `__osPfsRequestOneChannel(channel)` double-counted (NOT the CHNL_ERR macro — the execution-time hypothesis was refuted at the retro by reading the code). md5-candidate 124→**125** (all .c stub-free), asm subsegs 144→143. 0 stuck-far/permuter/carried/re-opened. Retro applied 3 of 3, all `pick_target.py` accuracy fixes: #1 `_resolve_include` basename fallback (vendored-prefix `PRinternal/X.h`→`internal/X.h` now scanned → declared_type_names sees the typedef → refs_unplaced drops it); #2 `call_divergence` strips inactive `#if BUILD_VERSION` branches (lib-threaded) so a dead-branch call no longer double-counts; #3 factor `_append_coddog_trap_hazards`, now called from the S78 tail-identity block too (initialize.c's defines-data was invisible because its coddog hit keys on the sibling `create_speed_param`, not leader `__osInitialize_common`). +3 unit tests, golden stable (no covered-row change), suite 45 pass. Mirror track stays seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 81 | io/siacs.c (__osSiCreateAccessQueue+__osSiGetAccess+__osSiRelAccess) | 3 | mirror / coddog @99.99 verbatim io SI access-queue (libultra/io/) / verbatim piacs.c twin: drop-def + 1 fn-name + 3 data-global recover-extern | 5 | *5* | **10th coddog cross-ref sprint; smallest remaining libultra candidate (240B), banked first-build 0-iteration.** Verbatim **twin of banked `io/piacs.c`** — coddog matched the twin `piacs.c@99.99` but named members (`__osSiGetAccess`/`__osSiRelAccess`) name the real source `siacs.c`; mirrored from siacs.c. Drop-def fast path (S42): 3 data globals placed add-only at asm-recovered vrams (`__osSiAccessQueueEnabled`=0x800C8210 size:0x4, `__osSiAccessQueue`=0x801EFFB0 size:0x18, `siAccessBuf`=0x800FAA00 size:0x4 — all visible as `D_<vram>` in scaffold asm). Gate: `__osSiCreateAccessQueue`=0x800AC110 (func) + flip [0x87510,asm]→[0x87510,c,libultra/io/siacs] (standalone 240B subseg, no split). Bonus: placing `__osSiCreateAccessQueue` resolves controller.c's `calls-unplaced` callee. Friction: needed a 2nd `make extract` after the data-symbol adds (gate placed only the fn sym) — retro #2 doctrine note: a drop-def symbol-add naming an existing `D_<vram>` (no section carve) is gate-safe, distinct from the S68 execution-only ld-section carve. md5-candidate 125→**126**, asm subsegs 143→142. 0 stuck-far/permuter/carried/re-opened. Retro applied 2 of 2, both `pick_target.py`/doctrine: #1 `coddog-twin:<matched>!=<member-src>` pre-flag (cross-checks coddog basename vs named-member upstream; fires piacs!=siacs); #2 #defines-data gate-safe symbol-add note. +helper unit-verified, golden stable (map-free → inert), suite 45 pass. Mirror track stays seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 82 | io/controller.c (osContInit+__osContGetInitData+__osPackRequestData) | 3 | mirror / coddog @99.99 verbatim io controller-init (libultra/io/) / single-file-pack:3fn drop-def + 3 data recover-extern | 5 | *5* | **11th coddog cross-ref sprint; the teed-up next-up after S81 (siacs.c was banked precisely to place `__osSiCreateAccessQueue`, controller's last real callee).** Verbatim ultralib VERSION_J `io/controller.c`, single-file-pack:3fn (atomic, no split), drop-def fast path (S42), banked first-build 0-iteration. 7 file-scope data globals dropped → extern: 3 pre-placed (`__osContPifRam`/`__osContLastCmd`/`__osMaxControllers`), **3 placed at gate** from `osContInit.s` `D_` refs (`__osContinitialized`=0x800C8190 size:0x4, `__osEepromTimerQ`=0x801B8A00 size:0x18, `__osEepromTimerMsg`=0x8012F4DC size:0x4), and `__osEepromTimer` **pure drop-def** (defined here but referenced only by still-asm eeprom.c → its own `D_` resolves it, no extern/placement). Includes adapted to in-tree io form (`PRinternal/macros.h`+`PR/os_internal.h`+`controller.h`+`siint.h`, all already-vendored, matches `contreaddata.c`). Gate flip [0x82810,asm]→[0x82810,c,libultra/io/controller] (single 784B 16-aligned block, no split). Friction: the gate `D_<vram>`→name rename failed an **incremental** link (`undefined reference to D_8012F4DC`, stale stub `.o` — INCLUDE_ASM `.s` dep untracked by make), masked by a stale-`.z64` SHA; `make clean` produced the green ROM. Clean-make ROM SHA-1 == baserom. md5-candidate 126→**127**, asm subsegs 142→141. 0 stuck-far/permuter/carried/re-opened. Retro applied 1 of 3 (#1 `docs/hazards.md#defines-data` clean-rebuild caveat for a `D_`-rename symbol-add; #2 defines-data referenced-by-self-vs-elsewhere split NOT selected; #3 confirmatory). Mirror track stays seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 83 | io/sptask.c (osSpTaskLoad+osSpTaskStartGo) | 2 | mirror / coddog @99.99 verbatim io RSP task-load (libultra/io/) / single-file-pack:2fn drop-def + **GBI-microcode build-define enabler** | 5 | *5* | **12th coddog cross-ref sprint; the S82-teed-up next-cleanest io leaf.** Verbatim ultralib VERSION_J `io/sptask.c`, single-file-pack:2fn (atomic, no split), 0 C-body edits. **All gate hazards were FPs** (gate-verified before flip): `jal-count-mismatch:7vs14` = static `_VirtualToPhysicalTask` inlined into osSpTaskLoad (7× `jal osVirtualToPhysical` in asm confirms; coddog 99.99 holds, no version divergence); `calls-unplaced:_osVirtualToPhysical` = the line-11 MACRO (real callee `osVirtualToPhysical`=0x800A7720 placed); `needs-header` = no-op. Only real work = drop-def `static OSTask tmp_task` → extern @0x800FA9C0 size:0x40 (in .bss ADD30.bss.s, abuts S81 siAccessBuf), placed add-only at gate (SHA-neutral). **The one non-trivial bit (in-execution, invisible to the gate stub):** the verbatim body LINKED clean but full-make SHA-1 missed by EXACTLY ONE WORD @0x800AB504 — `addiu v0,v0,imm` for `IO_READ(...+OS_YIELD_DATA_SIZE-4)`, built 0x8FC vs baserom 0xBFC. Root cause: `PR/sptask.h` guards `OS_YIELD_DATA_SIZE` 0xc00 (GBI-microcode defined) vs 0x900 (#else); `LIBULTRA_CFLAGS` had no GBI define. Fix (PO directive): `+LIBULTRA_CFLAGS -DF3DEX_GBI_2` (MG64's actual microcode; same 0xc00 guard as ultralib's default -DF3DEX_GBI). Clean rebuild → ROM SHA-1 == baserom, every other banked libultra file SHA-1-neutral. Localized via the S44 `.o`-diff (byte form: venv-python cmp the subseg, group differing bytes into words → one off-by-0x300 immediate). md5-candidate 127→**128** (all .c stub-free), asm subsegs 141→140. 0 stuck-far/permuter/carried/re-opened; 0 C-body iterations (the divergence was a build-define, not code). Retro applied 3 of 3: #1 `pick_target.py` GBI-value-guard pre-flag (parse `LIBULTRA_CFLAGS` defines → libultra active set; `gbi_value_guard_needs_define` flags a candidate using a GBI-guarded macro when no guard define is active — dormant while -DF3DEX_GBI_2 stands) +1 unit test, golden-neutral, suite 46 pass; #2 `docs/hazards.md#needs-define` GBI-microcode sub-case (the 1-word SHA-miss tell + byte `.o`-diff); #3 `.o`-diff localization (confirmatory, log-only). Mirror track stays seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 84 | io/epirawdma.c (__osEPiRawStartDma) + os/setintmask.s (osSetIntMask) | 2 | mirror / verbatim io C-mirror + first hasm asm-vendor carrying a `.rodata` LUT (libultra/io + libultra/os) / c-combined 3-unit split; `has-rodata:__osRcpImTable` strip+rename carve | 4 | *4* | **Cleared 2 of 3 members of the `[0x7E360]` c-combined pack.** Gate split it 3-way at the upstream-file boundaries (all 16-aligned). **epirawdma** = clean verbatim ultralib VERSION_J io mirror (jal=1 osVirtualToPhysical S7; `__osCurrentHandle`=0x800C7E90 placed; include adapt = sibling epidma.c's `#include "piint.h"`; no rodata; name pre-curated), first-build SHA == baserom, 0 iteration, zero symbol adds. **setintmask** = first VENDOR_ASM `.s` with a `.rodata` section (`__osRcpImTable`, 64-`.half` LUT @0x800D2200/rom 0xAD600, referenced cross-TU by the exception dispatcher `asm/8AF90.s`). **Novel:** splat auto-links a hasm `.o`'s `.rodata` at the section END (out of address order) → vendoring the full `.s` would duplicate+misplace the 0x80B → SHA break. Resolved by vendoring `.text` only (strip the `.rdata` block) + keeping the LUT as the extracted generic blob renamed via `symbol_addrs += __osRcpImTable=0x800D2200 size:0x80` (D_<vram> rename → clean rebuild; `7E360.o` confirmed `.text`-only 0xA0). First-build SHA == baserom, 0 iteration. **pimgr** (osCreatePiManager, file-static+defines-data) carried. md5-candidate 128→**129** (.c), hasm 21→**22**, asm 140→140. 0 stuck-far/permuter/re-opened; 1 carried (planned spike). Retro applied 3 of 3: #1 `docs/hazards.md#asm-mirror-vendoring` vendored-`.s`-with-`.rodata` sub-case (+ CLAUDE.md index row); #2 `pick_target.py` needs-define skips a macro the `.s` `#define`s itself (MI_INTR_MASK FP); #3 `pick_target.py` `has-rodata:<sym>` pre-flag (`vendorable_tu_data_symbols`); suite 46 pass, golden-neutral. Mirror track seed-only -- committed 4pt / banked 4pt -- `(real)` |
| 86 | os/timerintr.c (__osTimerServicesInit+__osTimerInterrupt+__osSetTimerIntr+__osInsertTimer) | 4 | mirror / coddog @99.99 verbatim os timer-service (libultra/os/) / single-file-pack:4fn drop-def + 2 data recover-extern + **8-gate EXEMPTION (drop-def sub-case)** | 8 | *8* | **The os timer-service core (`vimgr.c`/`settimer.c` dependency root); pts-8 single-file-pack ran under the verbatim-mirror exemption, now extended to the drop-def sub-case.** Verbatim ultralib VERSION_J `src/os/timerintr.c`, single 0x300 subseg = exactly the 4 fns (no split), flip [0x87C40,asm]→[0x87C40,c,libultra/os/timerintr]. **Drop-def** the 6 file-scope data globals → extern (`-D_FINALROM` strips the `#ifndef _FINALROM` profile block; the `VERSION_K` `tim<468` clamp is correctly excluded for J): 3 pre-placed (`__osCurrentTime`/`__osBaseCounter`/`__osTimerList`@S27/S30), 2 recovered from `__osTimerServicesInit`/`__osSetTimerIntr` asm (`__osViIntrCount`=0x800FF1E0, `__osTimerCounter`=0x80132364), and **`__osBaseTimer` needed NO extern/placement** — named only in the dropped `.data` initializer `OSTimer* __osTimerList = &__osBaseTimer;`, so the placed pointer's bytes encode its address. All 4 names pre-curated, all callees placed (osSendMesg/osGetCount/__osDisableInt/__osRestoreInt/__osSetCompare). Include adapt `PRinternal/osint.h`→bare (sibling settimer/gettime). Full-make ROM SHA-1 == baserom, 0 iteration. md5-candidate 130→**131** (all .c stub-free), asm subsegs 139→138. 0 stuck-far/permuter/carried/re-opened. Retro applied 4 of 4: #1 `pick_target.py refs_unplaced` drops a self-defined global named only in another global's depth-0 initializer (`_names_in_function_bodies` body-depth scan; +1 unit test, suite 48 pass, golden-neutral); #2 CLAUDE.md bank-step generated-artifact (`undefined_syms_auto.txt`+`mariogolf64.ld`) commit-hygiene note; #3 CLAUDE.md 8-gate exemption wording extended to drop-def mirrors; #4 BACKLOG `vimgr.c` carry-over update (timer deps now placed). Mirror track seed-only -- committed 8pt / banked 8pt -- `(real)` |
| 87 | io/vimgr.c (osCreateViManager+viMgrMain) | 2 | mirror / coddog @99.99 verbatim io VI-manager (libultra/io/) / **drop-STATIC mirror — 8 .bss drop-to-extern, NO carve** (S81 pattern generalized to file-statics) | 5 | *5* | **The vimgr.c carry-over, banked first-build once S86 cleared its timer-wall.** Verbatim ultralib VERSION_J `src/io/vimgr.c`, single 0x340 subseg = exactly the 2 fns (single-file-pack, no split), flip [0x88210,asm]→[0x88210,c,libultra/io/vimgr]. **The carry-over's "heavy file-static `.bss` carve" framing was the false-flag this sprint retires:** the 6 file-statics + 2 globals + 1 func-local static are all UNINITIALIZED → pure `.bss` (no ROM bytes) → dropped to sized `extern`s placed at recovered `main_bss` vrams (`retrace`=0x800FAA10/`viThread`=0x800FAA18/`viThreadStack`=0x800FABD0/`viEventQueue`=0x800FBBD0/`viEventBuf`=0x800FBBE8/`viRetraceMsg`=0x800FBC00/`viCounterMsg`=0x800FBC18/`__osViDevMgr`=0x800C8250), **NO carve, NO classical loop** (the S81 `siacs.c` drop-to-extern pattern). `osCreateViManager` pre-curated; `viMgrMain` (static fn) name added at gate. All callees placed (timer-side `__osTimerInterrupt`/`__osCurrentTime`/`__osBaseCounter`/`__osViIntrCount` by S86). Include adapt `PRinternal/{viint,osint}.h`→bare (sibling visetevent/visetmode). Full-make ROM SHA-1 == baserom, 0 iteration. md5-candidate 131→**132** (all .c stub-free), asm subsegs 138→137. 0 stuck-far/permuter/carried/re-opened. Retro applied 3 of 3: #1 `docs/hazards.md#file-static-bss-layout-conflict` uninitialized-static = pure-`.bss` drop-to-extern-mirror split + the `drop-static-mirror:<n>bss` re-frame tag in `pick_target.py` (`drop_static_mirror_hazard`; coddog@≥99 + file-static + no carve signal; +1 unit test, golden-neutral [map-free], suite 49 pass) + CLAUDE.md index row; #2 `pick_target.py` `_C_NONCALL += aligned,__attribute__` (the `ALIGNED`/`STACK` macro-expansion attribute residue mis-flagged `calls-unplaced:aligned`; golden regen = 4 `aligned`-only removals, real callees retained); #3 collapsed into #1 (the tag IS the hazard-collapse). Mirror track seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 85 | os/initialize.c (__osInitialize_common+create_speed_param) | 2 | mirror / coddog @99.99 verbatim os/ (libultra/os/) / drop-def + 2 in-exec reconcile classes (header-renames-symbol #undef + VERSION_K-gate un-gate) | 5 | *5* | **13th coddog cross-ref sprint; the S80-teed-up os/ leaf, banked.** Verbatim ultralib VERSION_J `src/os/initialize.c` (single 0x2F0 subseg, no split). **Carry-over over-stated it** (recurring): framed a cross-region `.data` carve, but actual path = **drop-def** (S82 default; extern the 4 `.data` globals + __osFinalrom, main_data provides bytes, NO carve). **Two NEW reconcile classes → tooling:** (1) **header-renames-symbol** — os_host.h `#define __osInitialize_common() osInitialize()` (transitively-included K→J shim) rewrote the C fn def → curated symbol undefined at link; fixed `#undef __osInitialize_common` (S31 nuGfxInit class, 2nd instance). (2) **VERSION_K-gate too aggressive for J** — `__osSetWatchLo(0x4900000)` gated `>= VERSION_K` but present in MG64-J → exact 8-byte/2-instr SHA-miss (`.o` fn 0x228 vs asm 0x230), localized `.o`-disasm vs Ghidra MCP, un-gated to `>= VERSION_J`. 7 symbol_addrs adds (5 gate recover + 2 boot-globals osResetType/osAppNMIBuffer). 0 C-body iterations, clean-rebuild ROM SHA-1 == baserom. md5-candidate 129→**130** (.c), asm 140→139. 0 stuck-far/permuter/carried/re-opened. Retro applied 3 of 3: #1 `pick_target.py header_renames_symbol` detector + flag + unit test + `docs/hazards.md#header-renames-symbol` + CLAUDE.md row; #2 `docs/hazards.md#needs-define` VERSION_K-gate-present-in-J sub-case + CLAUDE.md symptom row; #3 BACKLOG drop-def-default carry-over guidance. suite 47 pass, golden-neutral. Mirror track seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 88 | io/contpfs.c (__osSumcalc+__osIdCheckSum+__osRepairPackId+__osCheckPackId+__osGetId+__osCheckId+__osPfsRWInode) | 7 | mirror / coddog @100.00 verbatim io pfs (libultra/io/) / single-file-pack:7fn drop-def + 2 data recover-extern + **8-gate EXEMPTION (drop-def)**; vendored-header-incomplete in-exec reconcile | 13 | *13* | **The pfs id/inode core; pts-13 single-file-pack ran under the verbatim-mirror exemption.** Verbatim ultralib VERSION_J `src/io/contpfs.c`, single 0x2C0 subseg = exactly the 7 fns (one .o: all inner boundaries non-16; `#if <J` strips `__osPfsSelectBank` = the banked pfsselectbank.c, `#ifdef _DEBUG` strips `__osDumpId`). Flip [0x89D90,asm]→[0x89D90,c,libultra/io/contpfs]. **Drop-def** the 3 cache globals → extern: `__osPfsInodeCacheBank`=0x800C9444 pre-placed, recovered `__osPfsInodeCacheChannel`=0x800C9440 (.data,-1) + `__osPfsInodeCache`=0x801B68E8 (.bss,0x100). All callees placed (__osContRamRead/Write, __osSi*, osRecvMesg, __osContAddressCrc/DataCrc, bzero, osGetCount, the 2-arg __osPfsSelectBank). **Novel bank-gotcha — vendored-header-incomplete:** the reconstructed `controller.h` was `(already-vendored)` yet missing `SELECT_BANK` + had an object-like `SET_ACTIVEBANK_TO_ZERO` (vs the source's `()` call) → 5 parse errors mid-execution; aligned both to VERSION_J (add `SELECT_BANK`, make function-like; grep confirmed no other src consumer) + clean-rebuild. Full-make ROM SHA-1 == baserom, 0 C-body iteration. md5-candidate 132→**133** (all .c stub-free), asm subsegs 137→136. 0 stuck-far/permuter/carried/re-opened; **1 novel bank-gotcha**. Retro applied 4 of 4: #1 `docs/hazards.md#vendored-header-incomplete` + CLAUDE.md index row (the `needs-macro` auto-detector deferred — preprocessing needed to avoid FP); #2 `pick_target.py` `coddog-fncount-mismatch` (under-count direction only, so a version/_DEBUG over-count like contpfs 9-raw-vs-7 never false-fires); #3 `pick_target.py` `one-tu` + `decomp_asm.asm_function_addrs` (all-inner-non-16 ⇒ one .o); #4 BACKLOG `motor.c` version-branch trap note. suite 52 pass, golden regen (28 `one-tu` additions, every changed line differs ONLY by the new flags). Mirror track seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 89 | io/sirawdma.c (__osSiRawStartDma) | 1 | mirror / coddog @99.99 verbatim io SI-DMA (libultra/io/) / **decompose sched\|sirawdma c-combined** (S74 pattern), bank the clean tail | 2 | *2* | **The cleanest libultra increment after the cheap mirror band mined out — decompose the pts-13 `osCreateScheduler` c-combined and bank the clean `sirawdma.c` tail.** The `0x86A50` subseg = heavy `sched.c` head + clean `sirawdma.c` tail; pts-13 tripped the 8-gate → decomposed at the upstream-file boundary (S74/S75 contquery/contreaddata pattern): keep `[0x86A50,asm]` (sched head 0x800AB650..0x800AC060), add `[0x87460,c,libultra/io/sirawdma]` (176B tail, `__osSiRawStartDma`=0x800AC060, 16-aligned, pre-placed S74). Verbatim ultralib VERSION_J `src/io/sirawdma.c`, sibling of S4 sirawread/sirawwrite; uses the `>= VERSION_J` `IO_READ(SI_STATUS_REG)` busy-check (fn EXISTS in J — NOT the motor.c version trap). Two known edits: include adapt `PRinternal/siint.h`→bare + `#ifdef _DEBUG`-wrap the bare `assert` (assert-strip; held). All callees pre-placed (osWritebackDCache=0x800A70E0/osVirtualToPhysical=0x800A7720/osInvalDCache=0x800A6FB0); SI/PIF macros in vendored siint.h/rcp.h; the row's file-static/defines-data:count,firsttime/rodata-jtbl:0x800D25C0/log-fn hazards all belong to the still-asm sched head. Pure text mirror, zero new symbols. First-build full-make ROM SHA-1 == baserom, 0 iteration. md5-candidate 133→**134** (all .c stub-free), asm subsegs 136→136 (head stays asm, +1 C subseg). 0 stuck-far/permuter/carried/re-opened. Retro applied 1 of 1: #1 BACKLOG staleness reconciliation (piacs.c marked BANKED — stale "remaining io trap" through S88, green ROM since cbaf80a 2026-06-13; carry-over de-paired from motor.c). Mirror track seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 90 | io/pimgr.c (osCreatePiManager) | 1 | mirror / coddog @99.99 verbatim io PI-manager (libultra/io/) / **drop-def mirror — 4 .bss drop-to-extern + globals→extern, NO carve** (vimgr S87 / pimgr drop-def class); closes the S84-split [0x7E360] pack | 5 | *5* | **The pimgr carry-over, banked first-build — the "mixed `.data`/`.bss` carve" spike framing was over-cautious, the same false-flag vimgr S87 retired.** Last member of the S84 3-way split (setintmask+epirawdma banked S84). Verbatim ultralib VERSION_J `src/io/pimgr.c`, single 0x190 subseg, flip [0x7E400,asm]→[0x7E400,c,libultra/io/pimgr]. Under `-D_FINALROM` the `#ifndef _FINALROM` ramrom block strips + `_DEBUG __osError` strips → ONE fn; every file-scope DEF drops to `extern` so the `.o` emits only `.text` (atomic drop-def). The only `.data` global `__osCurrentHandle` was already placed (S84) → **NO carve**. 4 asm-recovered `.bss` drop-to-externs (one disassembly, contiguous main_bss block below piacs's `piAccessBuf`@0x800FA9B0, sizes from inter-symbol gaps): `piThread`=0x800F97E0 (0x1B0 OSThread), `piThreadStack`=0x800F9990 (0x1000 OS_PIM_STACKSIZE), `piEventQueue`=0x800FA990 (0x18), `piEventBuf`=0x800FA9A8 (0x4). Header `piint.h` provides `__osPiDevMgr`/`__osCurrentHandle`; `__osPiTable`/`__Dom*SpeedParam` unreferenced → dropped; bare-include `piint.h` per epirawdma sibling. `osCreatePiManager`=0x800A3000 pre-curated; all callees + data refs placed (S84/S85). Full-make ROM SHA-1 == baserom, 0 iteration. md5-candidate 134→**135** (all .c stub-free), asm subsegs 158→157. 0 stuck-far/permuter/carried/re-opened. Retro applied 3 of 3: #1 `BACKLOG.md` Spike-guidance at-write-time drop-static-test discipline (frame the drop-def verdict not the worst-case carve) + sched.c head framing revised; #2 `pick_target.py` `carry_over_names()` region+symbol scoping — heading-anchored split + live-region bound fixes a 332→50 over-scoop that silently dropped name-dropped still-asm functions (e.g. `bcmp`/`_Litob`) from the ranker, + the by-design clarification at the exclusion site (a parked carry-over is retrieved via `--include-stuck`/BACKLOG, not smallest-first — pimgr was found that way, NOT a filter bug); #3 `docs/hazards.md#recover-extern-refs-unplaced` contiguous-`.bss`-block-sized-by-gaps recover note. suite pass, golden regen (carry_over un-suppressions). Mirror track seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 91 | libc/bcmp.s (bcmp) | 1 | mirror / **asm-mirror** (bzero/bcopy class) libc (libultra/libc/) / **decompose pts-13 pack — split off bcmp, vendor `.text`-only via VENDOR_ASM** | 2 | *2* | **The cheap libultra mirror band is exhausted — every remaining candidate is a pts-8/13 pack; banked the one GUARANTEED-clean sub-unit after a mid-gate pivot.** Verified at the gate that the top coddog "mirrors" are false/partial: `func_80050400` "llcvt.c@99.99" fn-sizes don't match (KMC compiler-runtime, false fingerprint); `func_800660A0` mtxutil tail only 2/4 fns match; `_Litob`/`_Ldtob` are the S71 <99% divergent-classical leaves. PO first approved **exceptasm.s** (8 fns, .text=0x970 EXACTLY matches the subseg) but gate investigation found `__osIntTable`/`jtbl_800D2610` — a switch jtbl the active `.text` `jr`s through, emitted SYMBOLICALLY (`.word .L800Bxxxx`) in a separate extracted rodata blob → breaks S84 strip-and-rename (vestigial `.text` labels) AND can't be carve-placed (VENDOR_ASM `.o` rodata auto-links at section end) → a genuine SPIKE, not the replay I framed → PO pivoted. Banked `libc/bcmp.s` instead: nm-u empty, only R4300/asm/regdef includes, WEAK skipped non-__sgi, NO data/rodata → cleanest asm-mirror class. Split `[0x8BE20]` pts-13 pack at 0x8BF30 (`_Printf`@0x800B0B30, 16-aligned; gate-validated green as two asm subsegs first), vendor ultralib `libc/bcmp.s` via VENDOR_ASM (8BE20:…), flip `[0x8BE20]` asm→hasm; `build/asm/8BE20.o` .text-only 0x110 exact, full-make ROM SHA-1 == baserom, 0 iteration. Bonus: isolates the `[0x8BF30]` xprintf pack (`_Printf`/`_Putfld`/`func_800B1580`) for future classical work. `bcmp`=0x800B0A20 pre-curated → zero symbol adds. Quality: 0 stuck-far/permuter/re-opened, **1 carried** (exceptasm spike, jtbl blocker). Retro applied 3 of 3: #1 `pick_target.py` has-rodata gated to the ACTIVE build (`#ifndef _FINALROM`/inactive-`BUILD_VERSION` EXPORTs dropped — exceptasm's `__osCauseTable_pt` no longer over-counted); #2 new `asm-mirror-jtbl:<head>` detector (a symbolic-pointer table → asm-mirror SPIKE, not S84 replay); #3 `docs/hazards.md#asm-mirror-vendoring` jtbl sub-case + CLAUDE.md index rows. suite 52 pass, golden-inert. Mirror track seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 92 | libc/xlitob.c (_Litob) | 1 | mirror / verbatim libc decompose (libultra/libc/) / **decompose pts-13 xldtob\|xlitob c-combined + `.data` carve** (ldigs/udigs digit tables) | 3 | *3* | **Banked the first sub-unit of the `[0x8D230]` xldtob\|xlitob c-combined pack — a clean verbatim mirror, retiring S91's "`_Litob` is a <99% divergent-classical leaf" mischaracterization** (coddog pct < 99 was a reloc-mask artifact; the named-member identity made it byte-verbatim). The pts-13 4-fn pack tripped the 8-gate → decomposed at the upstream-file boundary (xlitob HEAD / xldtob TAIL), since `c-combined:2file` blocks the verbatim-mirror exemption. Split `[0x8D230,asm]`→`[0x8D230,c,libultra/libc/xlitob]` (_Litob only, 592B/0x250) + `[0x8D480,asm]` (xldtob tail, 16-aligned). Verbatim ultralib VERSION_J `src/libc/xlitob.c`, byte-identical cp, **first-build full-make ROM SHA-1 == baserom, 0 iteration**. `jal-count-mismatch:2vs4` was a FALSE flag (4 jals = lldiv+memcpy source calls + 2 compiler-emitted u64 div/mod intrinsics `__udivdi3`/`__umoddi3` for `ullval % base` / `ullval / base`). **`.data` carve** for the two file-static digit tables `ldigs`="0123456789abcdef"@0x800C9660 + `udigs`@0x800C9674 (asm-recovered lui/addiu): `[0xA4A60,.data,libultra/libc/xlitob]` size 0x30 exact first try (segment vram→rom delta 0x80024C00, anchored off `gu/align.o(.data)`@0x800C81A0=rom 0xA35A0). Headers all resolve (xstdio.h source-private same-dir S54, stdlib.h/string.h). All 4 callees + `_Litob` pre-named → **zero symbol adds**. md5-candidate 135→**136** (all .c stub-free), asm subsegs 135→135 (xldtob tail stays asm, +1 C subseg). 0 stuck-far/permuter/carried/re-opened. Retro applied **2 of 3** (#3 reverted at apply — over-fired): #1 `coddog-fncount-mismatch` now fires on TAIL-carried identities (caught all 3 llcvt phantoms, not just the ovl8 one); #2 new `coddog-structural:<file>@<pct>` size-ratio guard (`>64 B/LOC`); (#3 a `.data`-carve detector — built, but the `addiu %lo(D_)` scan can't separate a file's own static from a cross-file extern by asm alone → over-fired on shared externs → reverted to a carry-over). suite 52 pass, golden-inert. Mirror track seed-only -- committed 3pt / banked 3pt -- `(real)` |
| 94 | audio/auxbus.c (alAuxBusPull+alAuxBusParam) | 2 | mirror / coddog @100.00 verbatim audio (libultra/audio/) / **single-file-pack:2fn 8-gate EXEMPTION (S64/S69)**, opens the audio sub-band | 13 | *13* | **The smallest-clean remaining libultra leaf — the audio sub-band's first verbatim mirror, banked first-try.** Surveying by the coddog column (the `--lib libultra` table showed only pts-8/13 spikes — the S55 caveat) found `func_800A4FC0` @ 0x803C0 (272B/2fn, coddog `src/audio/auxbus.c`@100.00, `one-tu`, ZERO other hazards). The audio header `-I` enabler was already paid (heapinit/heapalloc/copy flipped) → near-zero-enabler cp. Verified clean: no file-scope data/statics (no carve), callees are ABI macros (`aClearBuffer`) + an indirect `sources[i]->handler` (no jal to place), `switch` one real case (no jtbl), all types/macros resolve in-tree (`ALAuxBus`/`AL_AUX_*`/`AL_FILTER_ADD_SOURCE`). pts-13 tripped the 8-gate but ran under the **verbatim-mirror exemption** (regime mirror + verbatim single upstream file + decompose-blocked `one-tu` single-file-pack + no jal callees + both names curated at gate). 2 symbol adds (`alAuxBusPull`=0x800A4FC0, `alAuxBusParam`=0x800A509C), single text flip `[0x803C0,asm]`→`[0x803C0,c,libultra/audio/auxbus]`, no split/carve. Verbatim ultralib VERSION_J `src/audio/auxbus.c`, byte-identical cp, **first-build full-make ROM SHA-1 == baserom, 0 iteration**. md5-candidate 137→**138** (all .c stub-free), asm subsegs 157→156. 0 stuck-far/permuter/carried/re-opened. Retro applied **3 of 3**: #1 `pick_target.py` `--lib <scope>` now surfaces coddog-mirror rows whose matched source is in-scope (audio coddog hits stay `upstream none` since header-gated, so were invisible under `--lib libultra`; the coddog map IS the ultralib sweep → any match=libultra, + sub-path scope like `--lib audio`) + crash-guard (None `args.lib` short-circuit) + 1 unit test (suite 54 pass) + golden regen (absorbs the S94-bank tail-shift, func_800A4FC0→func_800A3C80); #2 BACKLOG audio-sub-band ordering note (next-cleanest audio mirrors + hazards); #3 pts-mirror-over-estimate decision (keep pts as-is — display-only, doesn't change the smallest-first sort, exemption absorbs the false 8-fire; seed-rubric note, no formula change). Mirror track seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 93 | libc/xldtob.c (_Ldtob+_Ldunscale+_Genld) | 3 | mirror / verbatim libc float-to-string (libultra/libc/) / **single-file-pack:3fn 8-gate EXEMPTION (S64/S69) + `.rodata` carve** (pows[] + NaN/Inf/literal pool), twin-of xlitob (S92) | 13 | *13* | **The S92 xldtob-tail carry-over banked first-try — a clean verbatim mirror, the float/long-double→string sibling of S92's xlitob.** Single text flip `[0x8D480,asm]`→`[0x8D480,c,libultra/libc/xldtob]` (S92 already split the xlitob head off `[0x8D230]`; no further split). pts-13 tripped the 8-gate but ran under the **verbatim-mirror exemption** (regime mirror + verbatim single upstream file + decompose-blocked `single-file-pack:3fn`/`one-tu` + all callees placed + all 3 names pre-curated in ghidra_symbols). Verbatim ultralib VERSION_J `src/libc/xldtob.c`, byte-identical cp, **first-build full-make ROM SHA-1 == baserom, 0 iteration**. `jal-count-mismatch:4vs3` was a FALSE flag (verbatim body matched on SHA). **`.rodata` carve** (the only data work, `.rodata`-ONLY since `pows[]` is `const` unlike xlitob's mutable `.data` ldigs/udigs): the generic `[0xADBD0,rodata]` subseg ALREADY bounded the exact 0x70 extent (vram 0x800D27D0→0x800D2840 = `static const ldouble pows[]` 0x48 + "NaN"/"Inf" strings + 1.0/1e8/"0" literals) → **1-line attribute change** `rodata`→`.rodata,libultra/libc/xldtob`, NO split. Callees memcpy/ldiv/lldiv/__udivdi3/__umoddi3 all pre-placed; xstdio.h/stdlib.h/string.h resolve (S92) → **zero symbol adds**. md5-candidate 136→**137** (all .c stub-free), asm subsegs −1 (0x8D480 flipped) + 1 rodata carve. 0 stuck-far/permuter/carried/re-opened. Retro applied **3 of 3**: #1 `pick_target.py` rodata-literal **carve-start widening** (`defines_file_static_const_array` source-gates a rodata-subseg-start carve-start — the FP scan missed the pows[] base 0x800D27D0 by 0x50 B; FP-safe via the `static const` file-private gate, unlike the S92-reverted .data addiu scan; +1 unit test, suite 53 pass, golden-inert); #2 `docs/hazards.md#rodata-sibling-yaml-pattern` carve-start-widening + carve-as-attribute-change (no-split-when-bounded) notes + S93 provenance; #3 near-free-retry checklist confirmation (the S92 checklist replayed verbatim-correct → 0 rework; no edit). Mirror track seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 95 | audio/load.c (alAdpcmPull+alRaw16Pull+alLoadParam+_decodeChunk) | 4 | mirror / coddog @99.99 verbatim audio (libultra/audio/) / **single-file-pack:4fn 8-gate EXEMPTION (S64/S69)**, 2nd audio sub-band leaf, ALL flagged hazards FALSE (AUD_PROFILE-guarded) | 13 | *13* | **The 2nd audio sub-band leaf, banked first-try — the cleanest tractable audio pack once auxbus opened the band.** The coddog map (func→fn→src, in source order) was decisive at the gate: load.c's `[0x7F8B0]` subseg (0xB10=2832B, ending exactly at auxbus 0x803C0) maps ALL 4 fns to ultralib `src/audio/load.c`@99.99 in order (alAdpcmPull/alRaw16Pull/alLoadParam/_decodeChunk) → a true single-file pack, vs mainbus (2vs4) / save (2vs6 coddog-twin) which are genuinely multi-file (structural FPs). **All 3 flagged hazards were FALSE:** `refs-unplaced:lastCnt` (the `extern u32 ...lastCnt[]` decl + the `lastCnt[++cnt_index]=osGetCount()` use + the 2 `PROFILE_AUD()` timing calls are ALL `#ifdef AUD_PROFILE`-guarded; MG64 doesn't define it → compile out → zero refs); `calls-unplaced:alLoadParam@0x800A4E3C` (a self-member, the coddog tail-carry artifact). Only active external callee `alCopy` placed S36; abi.h microcode macros (a*/aLoad*/aSetBuffer) + libaudio.h/synthInternals.h/os.h/R4300.h all vendored/in-tree; no rodata literals/jtbl (the alLoadParam `switch(paramID)` compiled to a branch chain → no carve). pts-13 tripped the 8-gate but ran under the **verbatim-mirror exemption** (regime mirror + verbatim single upstream file + decompose-blocked single-file-pack + all callees placed + 4 names curated at gate). 4 symbol adds (alAdpcmPull=0x800A44B0/alRaw16Pull=0x800A48F4/alLoadParam=0x800A4C90/_decodeChunk=0x800A4E3C), single text flip `[0x7F8B0,asm]`→`[0x7F8B0,c,libultra/audio/load]`, no split/carve. Verbatim ultralib VERSION_J `src/audio/load.c`, byte-identical cp, **first-build full-make ROM SHA-1 == baserom, 0 iteration, 0 recover-extern**. md5-candidate 138→**139** (all .c stub-free), asm subsegs 156→155. 0 stuck-far/permuter/carried/re-opened. Retro applied **3 of 3**: #1 AUD_PROFILE de-noise (`cpreprocess.py` `_strip_dead_blocks` dead-#ifdef set += AUD_PROFILE; `pick_target.py` `refs_unplaced` now also strips dead blocks (symmetric w/ calls_unplaced) + `macro_hidden_text` strips dead blocks before finding invocations so a macro invoked ONLY under a dead block — PROFILE_AUD — isn't phantom-expanded; removes the phantom lastCnt/save_min/rate_min/vol_min from reverb/env/load rows; suite 54 pass, golden-inert); #2 BACKLOG audio-sub-band ordering refresh (auxbus+load were the only near-clean leaves; drvrnew/reverb/env need header/jtbl work, mainbus/save need multi-file splits); #3 coddog-attribution log-only note (the map cleanly separated single-file from multi-file; coddog-fncount-mismatch correctly did NOT fire on load.c). Mirror track seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 96 | audio/drvrnew.c (_init_lpfilter+alFxNew+6×al*New) | 8 | mirror / coddog @99.99 verbatim audio synth driver (libultra/audio/) / **8-gate FIRED, NO exemption** (residual variance: dual `.data`+`.rodata` carve + 10 cross-file callee names + 2 header vendors) → ran enabler-forward | 13 | *13* | **First audio mirror that did NOT qualify for the verbatim exemption — the synth DRIVER, banked first-try as an enabler-forward sprint.** The 8-gate fired (seed-13) and the S64/S69 exemption FAILED condition (c) "no residual variance" (unlike S94/S95 clean audio leaves): drvrnew references 10 cross-file pull/param entry points by name (`alFilterNew`=0x800A5D80 + alMainBusPull/Param + alResamplePull/Param + alFxPull/Param/ParamHdl + alSavePull/Param), all asm-confirmed from the al*New a1/a2+jal at the gate (18 syms total = 8 drvrnew + 10 callees; no conflict/caller-evict), AND needs a **dual carve**. PO chose the full mirror over an enabler-only sprint; banked atomically anyway. Enablers: vendored `initfx.h`(→internal/) + `stdio.h`(→compiler/gcc/, same source as memory/stdlib/string.h); **`.data` carve** `[0xA32D0,.data,libultra/audio/drvrnew]` (6 `static s32 *_PARAMS[]` = 0x190, 3-way split of `main_data`) + **`.rodata` carve** `[0xAD6B0,.rodata,...]` (jtbl_800D22B8 switch + SCALE/CONVERT/2³² consts = 0x40, attribute-change). `refs-unplaced:__FILE__,__LINE__` was FALSE — `_DEBUG` off → `alHeapAlloc→alHeapDBAlloc(0,0,...)`, no string emitted. drvrnew.o sections match carve extents exactly (.text 0x830/.data 0x190/.rodata 0x40). Verbatim ultralib VERSION_J `src/audio/drvrnew.c`, byte-identical cp, **first-build full-make ROM SHA-1 == baserom, 0 iteration**. md5-candidate 139→**140** (all .c stub-free). Strategic: pre-named the reverb/mainbus/save/resample entry points + vendored the shared headers → the audio synth cluster (reverb/env/mainbus/save/resample) is the warm cheaper next band. 0 stuck-far/permuter/carried/re-opened. Retro applied **3 of 4** edits (+1 log-only): #1 `pick_target.py` `coddog-source-banked:<file>` tag (a coddog match to an ALREADY-banked mirror is structural, not a fresh attribution — func_8009F440→load.c@98.17 after S95; advisory) + #2 `refs_unplaced` skips compiler predefined macros (`__FILE__`/`__LINE__`/… never link symbols; de-noises reverb/env) + golden regen (absorbs the S96 bank: alSavePull/alMainBusPull now named, drvrnew gone); #3 `docs/hazards.md` dual-section-carve cross-ref (#defines-data ↔ #rodata-sibling, size by `%hi` address band); #4 cluster-unlock note (log-only). Mirror track seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 97 | audio/save.c (alSavePull+alSaveParam) + audio/sl.c (alInit+alClose+alLink+alUnlink) | 2+4 | mirror / `c-combined:2file` audio-synth pack decompose (libultra/audio/) / **assert-strip wrap (save) + shared-global defines-data DROP (sl)** | 5 | *5* | **Cleared the `[0x82160]` `alSavePull` pack — the smallest audio-synth-cluster unit, the first audio mirror sprint with two known-edit sub-cases.** The pts-8 `c-combined:2file[save\|sl]` pack tripped the 8-gate (NOT the verbatim exemption — `c-combined` MUST decompose) → split `[0x82160,asm]`→`[0x82160,c,libultra/audio/save]` + `[0x82230,c,libultra/audio/sl]` at the save.c/sl.c file boundary (alInit @0x800A6E30). **save.c (208B/2fn):** verbatim ultralib VERSION_J cp EXCEPT the **assert-strip wrap** — `assert(f->filter.source)` is the FIRST audio mirror to exercise assert(); `<assert.h>` resolves to `include/assert.h` (NDEBUG-keyed, both NDEBUG+_DEBUG undefined → live `__assert`) so a verbatim cp SHA-missed (compiled-in branch + `jal __assert` + 0x30 `.rodata` `__FILE__`/cond strings) → wrapped in `#ifdef _DEBUG` per the sirawread/epirawread convention → match. Names pre-curated S96, no carve. **sl.c (176B/4fn):** verbatim cp + **shared-global defines-data DROP (S42 fast path, mandatory not optional):** `ALGlobals *alGlobals=0;` (D_800C8180, 16B = 4B ptr + 12B `.data` section-align pad) is SHARED — refd by env @804D0 + fx @815C0 (still-asm) via the splat `D_` name — so a carve would orphan those refs / double-define; dropped to the `libaudio.h:388` extern + `alGlobals=0x800C8180 // size:0x4` (the `make extract` rename names the splat `.data` dlabel, all referrers resolve, 16B stays splat-side). 1 gate callee-name `alSynDelete=0x80051E54` (alClose's func_80051E54, only asm callers → no caller-evict). Both **first-build full-make ROM SHA-1 == baserom, 0 iteration**. md5-candidate 140→**142** (all .c stub-free). 0 stuck-far/permuter/carried/re-opened. Retro applied **3 of 3**: #1 `pick_target.py` `bare-assert:<n>` advisory (scans a mirror's upstream for non-`_DEBUG`-guarded `assert(` post-`_strip_dead_blocks` — now flags reverb/env/sched at the gate) + CLAUDE.md index row; #2 `docs/hazards.md#defines-data` shared-global DROP-mandatory rule; #3 `pick_target.py` unions `defines-data` over `c-combined` member upstreams (sl.c's alGlobals would now flag at the gate, not at execution); golden regen (absorbs the S97 bank + new flags), suite green. Mirror track seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 98 | audio/mainbus.c (alMainBusPull+alMainBusParam) + audio/resample.c (alResamplePull+alResampleParam) | 2+2 | mirror / `c-combined:2file` audio-synth pack decompose (libultra/audio/) / **resample `.rodata` carve (twin-of:drvrnew)**; mainbus clean | 4 | *4* | **Cleared the `[0x811A0]` audio `c-combined:2file[mainbus\|resample]` pack — two verbatim VERSION_J mirrors, the first per-member rodata-attribution case.** The pts-8 c-combined pack decomposed (NOT the verbatim exemption — `c-combined` MUST decompose) → split `[0x811A0,asm]` text at the mainbus/resample file boundary (0x81310 / `alResamplePull`@0x800A5F10, 16-aligned). **mainbus.c (368B/2fn):** verbatim cp, carve-FREE (alMainBusPull all-immediate, alMainBusParam 1-case switch→branch — asm-confirmed no 0x800D2 refs). **resample.c (688B/2fn):** verbatim cp + **`.rodata` carve** `[0xAD7E0,.rodata,libultra/audio/resample]` (MAX_RATIO double D_800D23E0 8B + jtbl_800D23E8 10w 40B = 0x30) — `alResamplePull` `ldc1 0x23e0` + `alResampleParam` 5-case switch; pre-carve build link-failed (AD6F0.rodata.o jtbl `.word .L800A6160/.L800A6188` vanish under C) → carved generic `[0xAD6F0]` 3-way (twin-of:drvrnew S96 pinned the playbook). All 4 names pre-curated S96 (drvrnew callees) → **zero symbol adds**; osVirtualToPhysical placed S7; AUD_PROFILE dead S95. Both **first-build full-make ROM SHA-1 == baserom, 0 iteration**. md5-candidate 142→**144** (all .c stub-free), asm subsegs 130→129. 0 stuck-far/permuter/carried/re-opened. Retro applied **2 of 2** (conservative form): #1 `pick_target.py` `;owner-per-member` marker on a c-combined pack's rodata-jtbl/literal (the whole-pack scan S55 over-attributed resample's carve to the mainbus primary row; fires ONLY when member_paths non-empty → no single-file-pack regression; now flags the env/filter pack 0x804D0) + CLAUDE.md index row + `docs/hazards.md#rodata-sibling-yaml-pattern` c-combined owner-per-member note; #2 carve-end upper-bound + stale-`asm/<ROM>.s` caveats captured in the same hazards section; **FULL per-member attribution** (incl. the coddog jtbl path in `_append_recover_hazards`) **+ label-bounded carve-end CARRIED** as a tooling follow-up (touches the smallest-first ranker → off-cadence golden-gated work). golden regen (post-S98 bank + owner-per-member markers), suite 54 pass. Mirror track seed-only -- committed 4pt / banked 4pt -- `(real)` |
| 99 | libnusys/mainlib/nugfxdisplayon.c + nupiinit.c + nupiinitsram.c | 1+1+1 | mirror / libnusys cold / `c-combined:3file` decompose + drop-static mirror (×2) | 3 | *3* | **Cleared the `[0x7CAD0]` libnusys `c-combined:3file[nugfxdisplayon\|nupiinit\|nupiinitsram]` pack — 3 verbatim/drop-static mirrors, banked first-try.** Split the asm subseg at the 3 file boundaries (nuPiInit@0x7CAE0, nuPiInitSram@0x7CB20, all 16-aligned). **nugfxdisplayon (16B):** trivial verbatim cp (1 store to `nuGfxDisplay`, placed S16), zero enabler. **nupiinit (64B) + nupiinitsram (176B):** S87 drop-static mirrors — file-statics `PiMesgQ`/`PiMesgBuf`/`SramHandle` + globals `nuPiCartHandle`/`nuPiSramHandle` dropped to extern; 3 statics asm-recovered (PiMesgQ=0x800F74A0/0x18, PiMesgBuf=0x800F74B8/0xC8, SramHandle=0x800F7580/0x74) + added to symbol_addrs (**no carve — `.bss` is NOBITS**). All 4 callees placed (osCreatePiManager/osCartRomInit/bzero/osEPiLinkHandle). Each **first-build full-make ROM SHA-1 == baserom, 0 iteration**. md5-candidate 144→**147** (all .c stub-free). **Seed 3 was UNDER-PRICED** (pick_target whole-pack scan saw only the clean primary nugfxdisplayon; members 2&3's drop-static load was missed — anchor-true ~6); logged at the deterministic pts=3, the under-pricing IS what retro #1 fixed. 0 stuck-far/permuter/carried/re-opened. Retro applied **3 of 3**: #1 `pick_target.py` comment-strip fix — `has_file_scope_static` + `defines_data_globals` now scan comment-STRIPPED text (a trailing `/*..*/` after `;` defeated FILE_STATIC_RE; a `Copyright (C)` banner's `(` falsely tripped the K&R guard → suppressed every depth-0 global across the whole nusys band) + `file-static` member-union over `c-combined` members; golden regen now correctly flags gfxThread/nuContMgrInit/nuGfxTaskMgr defines-data + nuContGBPakFwrite file-static+defines-data, suite 54 pass; #2 `docs/hazards.md#file-static` batch-add transient-red note (a DATA-symbol caller-evict: a recovered static added before its consumer's body lands evicts the still-asm stub's sub-field `D_` labels) + detector-sync note; #3 `.bss`-NOBITS-no-carve confirmation (log-only, 3rd after S81/S87). Mirror track seed-only -- committed 3pt / banked 3pt -- `(real)` |
| 101 | audio/env.c (alEnvmixerPull+alEnvmixerParam+_pullSubFrame+_frexpf+_ldexpf+_getRate+_getVol) + audio/filter.c (alFilterNew) | 7+1 | mirror / `c-combined:2file[env\|filter]` audio-synth decompose (libultra/audio/) / **env: 8-gate FIRED, NO exemption** (residual variance: dual `.data`+`.rodata` carve + assert-strip) → enabler-forward (S96 drvrnew / S100 reverb replay); filter: trivial clean cp | 13 | *13* | **Cleared the `[0x804D0]` `c-combined:2file[env\|filter]` pack — the LAST un-flipped audio-synth-cluster subseg; both halves banked first-build, closing the audio mirror vein.** pts-13 tripped the 8-gate → MANDATORY decompose (c-combined blocks the verbatim exemption) → split `[0x804D0,asm]`→`[0x804D0,c,libultra/audio/env]` + `[0x81180,c,libultra/audio/filter]` at the env/filter file boundary (`alFilterNew`@0x800A5D80, the 0x20 B tail). **filter.c (1fn):** trivial verbatim cp (6 struct-field writes, no data/rodata/calls), `alFilterNew` pre-curated. **env.c (7fn, the envmixer):** verbatim ultralib VERSION_J `src/audio/env.c`, the drvrnew/reverb carve-mirror class — failed the exemption condition (c) on residual variance (dual carve + assert-strip), ran enabler-forward. **Dual whole-subseg carve, NO split (S93-class, both sections):** `.data` `eqpower[128]`@`[0xA3460,.data,env]` (0x100 B, vram 0x800C8060, the file's ONLY `.data`; UN-flagged S92 class, asm-recovered s4=lui 0x800d/addiu -0x7fa0) + `.rodata` jtbl_800D22F0 + 13 FP literals@`[0xAD6F0,.rodata,env]` (0xF0 B) — BOTH exact-bound by the existing generic subseg (1-line attribute flip; pick's `carve-end=0x800D25C0` over-stated, real end 0x800D23E0). **assert-strip:** 3 asserts (105/106/370) wrapped `#ifdef _DEBUG` (NDEBUG+_DEBUG both unset → bare `assert()`→`__assert` SHA-break; save.c style). **2 calls-unplaced recovered:** `__freeParam`=0x80051E74, `_freePVoice`=0x80051E7C (alEnvmixerPull lines 292/267 jal targets, still-asm synth region, 8-byte stubs); `_frexpf`/`_ldexpf` calls-unplaced were intra-file FALSE-positives (env.c members). 4 member symbol adds at gate (`_frexpf`/`_ldexpf`/`_getRate`/`_getVol`; alEnvmixerPull/Param/_pullSubFrame placed). Both **first-build full-make ROM SHA-1 == baserom, 0 iteration**. md5-candidate 148→**150** (all .c stub-free), asm subsegs 127→126. 0 stuck-far/permuter/carried/re-opened. Retro applied **3 of 3**: #1 `docs/hazards.md#rodata-sibling-yaml-pattern` generic-subseg-bound carve = exact-extent-no-split note (advances deferred S98 carve-end work); #2 BACKLOG S92 `.data`-carve detector 2nd data point (env single-file-pack = the safe first slice, no per-member ambiguity); #3 BACKLOG new S101 #1 tooling follow-up — suppress intra-pack `calls-unplaced` (calls-side dual of S66 #2). **Cross-repo follow-up:** 6 new decomp symbols (`_frexpf`/`_ldexpf`/`_getRate`/`_getVol`/`__freeParam`/`_freePVoice`) → propagate via `sync_decomp_names.py --import-from-decomp`. Mirror track seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 100 | audio/reverb.c (alFxPull+alFxParam+alFxParamHdl+_loadOutputBuffer+_loadBuffer+_saveBuffer+_filterBuffer+_doModFunc) | 8 | mirror / named-verbatim audio synth reverb (libultra/audio/) / **8-gate FIRED, NO exemption** (residual variance: dual `.data`+`.rodata` carve + assert-strip) → enabler-forward (S96 drvrnew replay) | 13 | *13* | **Cleared the `[0x815C0]` `alFxPull` pack — the audio REVERB effect, a drvrnew-class single-file mirror banked first-try.** The pts-13 single-file pack tripped the 8-gate and FAILED the S64/S69 exemption condition (c) "no residual variance" (dual carve + assert-strip) — exactly like S96 drvrnew; PO chose the full mirror enabler-forward, banked atomically. Verbatim ultralib VERSION_J `src/audio/reverb.c` (all 8 includes kept verbatim — `ultraerror.h`+`os_internal.h` ARE vendored at `include/libultra/PR/`, no need to drop as drvrnew did) + **1 assert-strip** (`assert(source)`→`#ifdef _DEBUG`; reconciles `jal-count-mismatch:8vs7`). **Dual carve:** `.rodata` ATTRIBUTE-CHANGE `[0xAD810,.rodata,libultra/audio/reverb]` (jtbl_800D2410 8-entry + 6 FP doubles = 0x50; generic `[0xAD810,rodata]` already bounded the exact extent, NO split, S93-class; pick_target's `carve-end=0x800D25C0` was a 0x160 over-estimate) + `.data` 3-WAY SPLIT `[0xA3560,.data,libultra/audio/reverb]` (0x20: `L_INC[]`={0x10,0x10,0x20}, `val`=0.0, `lastval`=-10.0=`0xC1200000`, `blob`=0, pad). The `.data` was **UNREFERENCED dead statics** (no `.text` reloc → not asm-recoverable) → offset found by ROM byte-search (objdump `.data` → `xxd baserom | grep`), at 0xA3560 (0x100 B past drvrnew's 0xA3460 tail). `calls-unplaced:SWAP` FALSE (inline macro). reverb.o sections match carve extents (.text 0xBA0/.data 0x20/.rodata 0x50; placed .text 0x800A61C0/.data 0x800C8160/.rodata 0x800D2410). 5 helper symbol adds at gate (`_loadOutputBuffer`=0x800A6738/`_loadBuffer`=0x800A6950/`_saveBuffer`=0x800A6AC0/`_filterBuffer`=0x800A6C30/`_doModFunc`=0x800A6CCC; alFxPull/Param/ParamHdl placed S96). **First-build full-make ROM SHA-1 == baserom, 0 iteration**. md5-candidate 147→**148**, asm subsegs 128→127. 0 stuck-far/permuter/carried/re-opened. Retro applied **1 of 3** (PO): #1 `docs/hazards.md#defines-data` unreferenced-static-carve byte-search sub-case (+ deferred `pick_target` `;unref` tag, off-cadence). Mirror track seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 102 | io/motor.c (__osMotorAccess+osMotorInit) | 2 | mirror / coddog @99.99 verbatim io VERSION_J (libultra/io/) / **8-gate FIRED, decompose MECHANICALLY BLOCKED** (one-tu single-file-pack) → enabler-forward; **wrong-ghidra-name override (NO sync-names)** + drop-static-to-extern | 8 | *8* | **Banked the io/motor.c trap S75 flagged — the smallest remaining libultra target (pts-8; all else `--lib libultra` is pts-13 structural). VERSION_J verbatim mirror banked first-build.** The active `#if BUILD_VERSION >= VERSION_J` branch compiles `__osMotorAccess`@0x800AE380 + `osMotorInit`@0x800AE4C4 (`__osMakeMotorData` inlined → `pack:2fn`/`one-tu`) + the file-static `__MotorDataBuf[4]` — all verified against `build/J/libgultra_rom/motor.o` (`T __osMotorAccess`, no `osMotorStop`; `b __MotorDataBuf`). pts-8 tripped the 8-gate but decompose was MECHANICALLY BLOCKED (one-tu single-file-pack, no inter-file boundary) → ran 1-increment enabler-forward (S100/S101 precedent), failing the strict exemption on residual variance (drop-static + name override). **Headline — wrong-ghidra-name override, NO `make sync-names`:** ghidra_symbols mislabels 0x800AE380 `osMotorStop`, but os_motor.h `#define osMotorStop(x) __osMotorAccess(...)` makes that a macro; the VERSION_J fn IS `__osMotorAccess`. Corrected via a `symbol_addrs.txt` override `__osMotorAccess = 0x800AE380; // rom:0x89780 type:func` — the `rom:` qualifier dodges splat's same-rom+segment dup error (`util/symbols.py:298-309`), symbol_addrs is read first → wins the reference (still-asm contRmbControl's 4 relocs → `jal __osMotorAccess`, verified at the gate). NO `#undef` needed (body names the macro RHS, not the macro). **drop-static:** `__MotorDataBuf`=0x800FBC30 size:0x100 (lui 0x8010/addiu -0x43d0, vi/io bss after viCounterMsg) `static`→`extern`. Include adapt `PRinternal/{controller,siint}.h`→bare. pick false-flags resolved by VERSION_J analysis: `defines-data:__osMotorinitialized` + half `drop-static:2bss` (inactive `#else`), `calls-unplaced:READFORMAT` (function-like macro). **First-build full-make ROM SHA-1 == baserom, 0 iteration.** md5-candidate 150→**151** (all .c stub-free), asm subsegs 126→125. 0 stuck-far/permuter/carried/re-opened. Retro applied **4 of 4**: #1 new `docs/hazards.md#wrong-ghidra-name-override` + CLAUDE.md index row + `pick_target.py` `wrong-ghidra-name` tag (`_macro_alias_target` + `_upstream_defines_function`) + unit test `test_wrong_ghidra_name_override`; #2 `pick_target.py` `#if BUILD_VERSION` version-strip wired into `has_file_scope_static`/`defines_data_globals`/`defines_local_static_data` (suppresses inactive-`#else` defines-data) + same-file function-like macro exclusion in `calls_unplaced` (READFORMAT; also cleaned `_Printf` ATOI/LDSIGN/PAD/PUT/isdigit FPs); #3 `header_renames_symbol` gated by `_upstream_defines_function` (macro-alias false-fire suppression); #4 `docs/hazards.md#upstream-mirror-pattern` `nm build/J/libgultra_rom/*.o` authoritative-symbol-set note. suite 55 pass, golden regen (the 2 intended diffs). **Cross-repo follow-up:** rename 0x800AE380 `osMotorStop`→`__osMotorAccess` in the Ghidra workspace (the deferred reconciliation; the override coexists meanwhile). Mirror track seed-only -- committed 8pt / banked 8pt -- `(real)` |
| 103 | mgu/mtxutil.c (guMtxF2L+guMtxL2F+guMtxIdentF+guMtxIdent) | 4 | **classical/mixed** / game-region -O2 (NOT libultra -O3) → src/mgu/ / **8-gate decompose of func_800660A0 pack; mirror→classical PIVOT** | 3 | *5* | **Planned a verbatim libultra/gu mirror; it FAILED first build — two compounding errors diagnosed + fixed.** (1) WRONG PROFILE: the 4 gu fns live in the GAME region (0x80067B00, tail of the func_800660A0 pack), compiled -O2, NOT the libultra -O3 band; the src/libultra/gu placement forced -O3 → `-finline-functions` inlined guMtxIdent (240B vs ROM 60B). (2) guMtxF2L CLAMPS in place (`if(x<-32768.0f)…; if(x>32766.0f)…`, consts 0xc7000000/0x46fffc00) — a Monegi overflow-guard variant absent from ultralib gu/mtxutil.c, ultralib mgu/mtxf2l.s, AND libultra_modern monegi/mgu/mtxf2l.s (all 3 non-clamping, byte-identical). PO chose push-through-classically → re-placed at src/mgu/mtxutil.c (-O2), 3 fns verbatim + guMtxF2L = upstream body + explicit clamp; new verbatim-upstream dir src/mgu/ (+.clang-format). 1 fix-iteration: float-literal `f` suffix (bare doubles compiled c.lt.d + cvt.d.s + a rodata pair; ROM uses single c.lt.s inline). 3-leaf byte-cmp IDENTICAL + full-make SHA after the fix. 0 symbol adds (names pre-curated). md5-candidate 151→**152** (all .c stub-free), asm subsegs 125→125 (game prefix stays asm, +1 c subseg). 0 stuck-far/permuter/carried/re-opened; **1 mid-sprint re-plan (mirror→classical) + 1 fix-iteration**. Retro applied **4 of 4**: #1 `pick_target.py coddog-partial:<m>of<n>fn` (multi-twin subset guard) + unit test; #2 `pick_target.py game-region-mirror:0x<vram>` (below-libultra-band -O2 router) + unit test + `docs/hazards.md#game-region-mirror-o2-profile`; #3 float-literal single-prec note (`docs/hazards.md#mirror-cast-divergence`); #4 codify src/mgu/ no-clang-format dir (CLAUDE.md ×3 + .clang-format). suite 55→**57** pass (2 new tests). **v2 realized tier (mis-committed mirror corrected):** committed seed 3 (regime mirror — coddog-mirror over-promised verbatim) → realized **5** (+1 mid-sprint re-plan, +1 novel profile+clamp gotcha), **residual +2**; regime mixed -- `(real)` |

| 104 | libc/xprintf.c (_Printf+_Putfld) | 2 | mirror / verbatim libc VERSION_J (libultra/libc/), warm band / **8-gate FIRED → DECOMPOSE** (split off func_800B1580 foreign TU) + enabler-forward dual carve | 13 | — | **PO picked `_Printf` as a "classical spike"; the gate asm-vs-upstream check UNMASKED a verbatim MIRROR (S13 precedent).** The alarming `jal-count-mismatch:14vs3` was pure jalr-vs-jal: every `(*pfn)` output is `jalr s4`, the 3 jals = strchr×2 + _Putfld, matching upstream `_Printf` exactly. `single-file-pack:3fn` was WRONG — upstream xprintf.c defines only 2 fns; `func_800B1580` is a separate `__osDpDeviceBusy` TU, split off at the gate (`[0x8BF30,asm]`→`[0x8BF30,c,libultra/libc/xprintf]`+`[0x8C980,asm]`, 16-aligned). All 4 callees C-resolved in banked siblings (_Litob@xlitob S92, _Ldtob@xldtob S93, strchr/strlen@string.c) → zero recovery. Byte-identical cp of ultralib `src/libc/xprintf.c`; **dual carve** (env.c/xldtob class) sized by `objdump -h xprintf.o`: `.data` `[0xA48B0,.data]` 0x50 (spaces[33]+zeroes[33], the S92 un-flagged init-static-array class) + `.rodata` `[0xADA50,.rodata]` 0x178 (fchar/fbit/"hlL"/_Putfld jtbl; carve-start 0x10 past the FOREIGN leading `__libm_qnan_f`@0xADA40). **First-build full-make ROM SHA-1 == baserom, 0 iteration.** md5-candidate 152→**153** (all .c stub-free), asm subsegs ~125 (+1 func_800B1580 subseg). 0 stuck-far/permuter/carried/re-opened. regime mirror → **seed-only, banked = seed 13pt** (no v2 residual on the mirror track). Retro applied **4 of 4**: #1 jal-count drops fn-ptr-callback macros (xprintf PUT/PAD) + calls-unplaced skips fn-ptr params (pfn); #2 `upstream-fncount-mismatch:<m>vs<n>` (foreign-TU-in-single-stem-pack) on a depth-aware `_iter_upstream_functions` rewrite (counts single-token K&R `_xatan` + leading-space defs, skips protos/#define/doc-comments → `_xatan`/`_xsincos` correctly relabel single-file-pack); #3 `data-carve:<names>` .data init-static-array detector (single-file-pack subset); #4 `docs/hazards.md` carve-start-past-foreign-symbol + `.o`-section-size extent oracle. suite 57→**61** pass (4 new tests), golden regen (func_800B1580 added, _xatan/_xsincos pack→single-file-pack, _Printf banked). |

| 105 | io/dpsetnextbuf.c (osDpSetNextBuffer) | 1 | mirror / verbatim io VERSION_J (libultra/io/), warm band / clean — picked up the S104-split foreign TU, re-identified | 3 | — | **The S104 `upstream-fncount-mismatch` split-off TU (func_800B1580), picked up the very next sprint as the smallest libultra candidate — split-then-mirror pipeline validated end-to-end.** coddog@99.99 → `src/io/dpsetnextbuf.c`; the gate asm confirmed `osDpSetNextBuffer` (NOT the imprecise S104 "`__osDpDeviceBusy` TU" label — the fn *calls* __osDpDeviceBusy@0x800B2B10 S1 + osVirtualToPhysical@0x800A7720 S7 ×2). 3 jals upstream = 3 in asm, no data externs, all 4 headers in-tree → ZERO recovery, ZERO carve, ZERO mid-flight surprises. Verbatim body cp of ultralib VERSION_J `src/io/dpsetnextbuf.c`; only the include block adapted to in-tree io-band convention (dropped `#ident`, `PRinternal/osint.h`→`osint.h`); `_DEBUG` asserts kept verbatim (compile out under `_FINALROM`). 1 symbol add at gate (osDpSetNextBuffer=0x800B1580). **First-build full-make ROM SHA-1 == baserom, 0 iteration.** md5-candidate 153→**154** (all .c stub-free), asm subsegs ~125→~124. 0 stuck-far/permuter/carried/re-opened. Bonus: pre-places the osCreateScheduler (pts-13) `calls-unplaced` callee. regime mirror → **seed-only, banked = seed 3pt** (no v2 residual). Retro applied **1 of 3** (PO): #2 `docs/hazards.md#upstream-mirror-pattern` split-off-TU-label-is-a-hint note (#1 confirmatory, #3 seed-pricing nuance — NOT selected). Mirror track seed-only -- committed 3pt / banked 3pt -- `(real)` |
| 106 | sched/sched.c (osCreateScheduler + 4 API + 9 static) | 14 | mirror / verbatim sched VERSION_J (libultra/sched/) / **8-gate FIRED, decompose MECHANICALLY BLOCKED** (single-file-pack) → enabler-forward; dual carve + 2 recover + 2 caller-evict + 9 assert-strip | 13 | — | **Banked THE last real libultra source mirror — the RCP task scheduler (14 fns). All else `--lib libultra` is now non-source-mirror work (exceptasm.s jtbl spike, game-region structural phantoms, libnusys/libkmc fillers).** pts-13 tripped the 8-gate; single-file-pack (asm subseg = exactly the .text 0xA10, 0x86A50..0x87460) blocks decompose → PO-approved enabler-forward full mirror (S100 reverb precedent), banked atomically after 1 enabler fix. **Characterized up front from `ultralib/build/J/libgultra_rom/.../sched.o`** (the `_rom` = MG64 profile): .text 0xa10 / .data 0x10 / .rodata 0x20 / **.bss 0** (SC_LOGGING off → osCreateLog/osFlushLog/osLogEvent calls-unplaced were FALSE flags, the scLog/logArray statics gone). **Carves:** `.data` `[0xA3600,.data,sched]` 0x10 (count/dp_busy/dpCount/firsttime @0x800C8200, firsttime=1 nonzero; vram found via __scExec dp_busy=0x800C8204; the S104 detector misses SCALAR statics → #5 BACKLOG follow-up) + `.rodata` `[0xAD9C0,.rodata,sched]` 0x20 (the __scExec jtbl @0x800D25C0, was the whole generic `[0xAD9C0,rodata]` block — exact fit). **2 recover (S22/S24):** `osViModeTable`=0x800C8270 size:0x1180 (56×0x50 OSViMode, src/io/vitbl, shared w/ nuScCreateScheduler) + `osSpTaskYielded`=0x800AB600 — which IS the S11-banked `func_800AB600` ("classical main" leaf = the un-named osSpTaskYielded), renamed + sig→sptask.h (OSYieldResult/OSTask*) keeping the verified `(status>>8)&1` body (NOT the upstream ternary — -O2 codegen risk). **2 caller-evict:** still-asm mus_dma (`asm/78D10.s`) called the sched globals by old func_ names → named `osScAddClient`=0x800AB798 + `osScGetCmdQ`=0x800AB880 (the multi-global flip dual). **assert-strip: 9 bare** `#ifdef _DEBUG`-wrapped (pick's `bare-assert:9` authoritative; my manual `^\s*assert\(` grep missed 2 `assert (` SPACE-variants → first-build .text 0xa90/.rodata 0x70 bloat → 1 fix-iteration → exact 0xa10/0x20; 1 was an `if`-body needing whole-`if` wrap). Include adapt PRinternal/osint.h→osint.h, drop #ident. **Full-make ROM SHA-1 == baserom.** md5-candidate 154→**155**, asm subsegs ~124→~123. 0 stuck-far/permuter/carried/re-opened; 1 fix-iteration. **Cross-repo follow-up:** 4 new decomp symbols (`osSpTaskYielded`/`osScAddClient`/`osScGetCmdQ`/`osViModeTable`) → propagate via `sync_decomp_names.py --import-from-decomp`. regime mirror → **seed-only, banked = seed 13pt** (no v2 residual; the 1 fix-iteration was a mirror enabler hiccup, not classical iteration). Mirror track seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 107 | os/exceptasm.s (8-fn OS exception/dispatch TU) | 8 | mirror / asm-mirror `hasm` (vendored ultralib `os/exceptasm.s` VERSION_J) / **8-gate FIRED → satisfied by mechanism-as-goal** (the jtbl-placement spike WAS the sprint goal) | 13 | — | **SOLVED the `os/exceptasm.s` jtbl spike parked since S91 — RESOLVED, not carried — via the LABEL-EXPORT mechanism (S91's listed-but-UNTRIED option, NOT a dead-end).** S91 proved 2 paths fail (strip-and-rename of a symbolic table; carve a `hasm` `.o`'s rodata) and over-generalized "both dead-ends proven" to the whole class; the 3rd option works first-try. The OS exception/thread-dispatch core (`__osExceptionPreamble`/`__osException`/`send_mesg`/`__osEnqueueAndYield`/`__osEnqueueThread`/`__osPopThread`/`__osDispatchThread`/`__osCleanupThread`). **Mechanism:** the `__osIntTable` switch jtbl lives in its OWN already-address-placed rodata blob (`asm/data/AD9F0.rodata.s`) that survives the `.text` flip and keeps SYMBOLIC `.word .L800Bxxxx` refs into the `.text`. **Phase 1** (subseg still `asm`): `symbol_addrs` strip-rename the 5 D_/jtbl_ tables (`__osHwIntTable`=0x800C9480/0x28, `__osPiIntTable`=0x800C94A8/0x8, `__osIntOffTable`=0x800D25F0/0x20, `__osIntTable`=0x800D2610/0x30, `__osThreadSave`=0x800FC6A8/0x1B0=sizeof(OSThread) bss) → `make extract && make` GREEN (isolates rename risk; the `__osIntTable` rename PRESERVED the symbolic jtbl). **Phase 2:** vendor `os/exceptasm.s` `.text`-only (strip `.rdata`/`.data`; adapt 2 source-private includes → `internal/…`; vendor `exceptasm.h`→`include/libultra/internal/`), `.globl` the stripped tables, and **export the 9 jtbl-target labels under the `.L800Bxxxx` names the blob references** (mapped by instruction: `counter`→`.L800AFDFC`, `redispatch`→`.L800B00A0`, …). VENDOR_ASM pair 8AF90 + flip `[0x8AF90,asm]`→`hasm`, `make clean && make extract && make`. Vendored `.o` = `.text`-only `0x970` exact (objdump confirmed empty data sections → 0-byte auto-link lines); blob stayed symbolic → the 9 exports are LOAD-BEARING. All 8 fns pre-curated in ghidra_symbols (no fn-add, no caller-evict). 0 extract-artifact changes (`.ld` already named `build/asm/8AF90.o`; renamed syms are blob-defined). **Full-make ROM SHA-1 == baserom**; asm subsegs ~123→**122**, md5-candidate unchanged at 155 (asm-mirror banks as `hasm`, not a `.c`). 0 stuck-far/permuter/carried/re-opened; 0 iteration after the mechanism. **Cross-repo follow-up:** 5 new decomp data symbols → propagate via `sync_decomp_names.py --import-from-decomp`. Retro applied **3 of 3**: #1 `docs/hazards.md#asm-mirror-vendoring` asm-mirror-jtbl sub-case rewritten spike→proven label-export procedure + `pick_target.py` comment + CLAUDE.md index row; #2 untried-mechanism-before-dead-end lesson (BACKLOG carry-over header); #3 Phase-1 rename-isolation codified as step 1. regime mirror → **seed-only, banked = seed 13pt** (asm-mirror, no v2 residual). Mirror track seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 108 | os/interrupt.s (__osDisableInt + __osRestoreInt) | 2 | mirror / asm-mirror `hasm` (vendored ultralib `os/interrupt.s` VERSION_J) / clean — no jtbl/rodata, internal-header include rewrite, 0 symbol/header adds | 2 | — | **Banked the clean no-jtbl sibling of S107 exceptasm — the cleanest remaining libultra-region asm TU. + Definitively ruled out `llcvt.c` (the scope's other half).** `__osDisableInt`(0x70)+`__osRestoreInt`(0x1C) @0x8B900 vendored verbatim from ultralib `src/os/interrupt.s` (the active `#if BUILD_VERSION >= VERSION_J` branch — `la __OSGlobalIntMask`/`mfc0 C0_SR`/global-mask reconcile — matched the disassembly). ZERO complications: no `.data`/`.rodata`/jtbl (unlike S107), all 5 headers (`PR/R4300.h`, `sys/asm.h`, `sys/regdef.h`, `PR/os_version.h`, `internal/threadasm.h`) already vendored, all 4 syms (`__osDisableInt`/`__osRestoreInt`/`__OSGlobalIntMask`/`__osRunningThread`) already named → 0 `symbol_addrs` adds, 0 extract-artifact changes. **One verbatim deviation:** the lone bare internal-header include `"threadasm.h"`→`"internal/threadasm.h"` (LIBULTRA_ASFLAGS `-I` set reaches it only via `-I include/libultra`; .text byte-unaffected, SHA-1 stays the proof — the S107 convention, now codified in `docs/hazards.md#asm-mirror-vendoring` step 1). yaml `[0x8B900,asm]`→`hasm` + VENDOR_ASM `8B900:src/libultra/os/interrupt.s`. **Full-make ROM SHA-1 == baserom** (one first-build parse-error from the bare include, fixed via the documented precedent — NOT a stuck-far/re-attempt). asm subsegs 122→**121**, hasm 24→**25**, md5-candidate unchanged at 155 (asm-mirror banks as `hasm`). 0 stuck-far/permuter/carried/re-opened. **llcvt.c closed as not-linked:** the libgcc workhorses it would `jal` are present (`__floatdidf`@0x800B3D40, `__fixunsdfdi`@0x800B3C20, libkmc/libgcc band, named) but the cast wrappers (`__d_to_ll`…`__ull_to_f`) are absent everywhere; the 8 coddog `__d_to_ll`@99.99 rows are reloc-masked structural FPs. Retro applied **4 of 4**: #1 `docs/hazards.md#asm-mirror-vendoring` internal-header-include-rewrite step-1 sub-bullet; #2 BACKLOG S108 #2 tooling follow-up (multi-root libkmc asm-TU index — deferred, not rushed into the ranker: the existing intrinsic-likely path already tags ultralib hand-asm; the remaining inventory is libkmc); #3 BACKLOG `## Active phase` S108 BANKED + remaining-asm-inventory note; #4 `docs/hazards.md#coddog-cross-ref` S108 workhorse-linked/wrapper-absent llcvt-FP example. **Cross-repo follow-up:** none (both fns already curated). regime mirror → **seed-only, banked = seed 2pt** (asm-mirror, no v2 residual). Mirror track seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 109 | mcvtld.s (__fixdfdi/__fixunsdfdi + __floatdidf) | 2 | mirror / asm-mirror `hasm` **KMC-as sub-lane** (vendored libkmc `mcvtld.s` soft-float double↔long-long cvt) / clean — no rodata/relocs, `li 0xffffffff`→`addiu` encoding edit ×2, multi-subseg merge | 2 | — | **First documented KMC-as sub-lane TU — the cleanest remaining asm in the libultra-region band (S108 note), PO-picked over the atan.c C-mirror.** libkmc soft-float (`__fixdfdi`/`__fixunsdfdi`@0x8F020 + `__floatdidf`@0x8F140) vendored verbatim via the KMC-assembler explicit-rule path (the `mmuldi3.s` precedent), NOT ultralib `VENDOR_ASM`. Two KMC encoding edits: `li $X,0xffffffff` expands to 2-insn `lui+ori`; the ROM uses 1-insn `addiu $X,$0,-1` (`2403ffff`/`2407ffff`) — same divergence as the vendored `mmuldi3.s`; diagnosed by Python byte-slice `cmp` of the assembled `.o` vs baserom (one-word downstream shift at a `lui…ffff`/`ori…ffff` pair). `.include "mips_as.h"` (`.equ FPU,1`) vendored to `src/libkmc/mips_as.h` + `-I src/libkmc`. Multi-fn TU spanning 2 adjacent splat subsegs → merged to ONE `[0x8F020,hasm]` (one source→one `.o`, the asm analog of single-file-pack). 0 `symbol_addrs` adds (both fns named). **Full-make ROM SHA-1 == baserom.** asm subsegs 121→**119**, hasm 25→**26**, md5-candidate unchanged at 155 (asm-mirror banks as `hasm`). 0 stuck-far/permuter/carried/re-opened. **Cross-repo follow-up:** none (both fns already curated). Retro applied **3 of 3**: #1 `pick_target.py` `intrinsic-likely:<tu>.s(kmc-as)` hazard tag (libkmc `.globl` asm-TU index, fires on asm-only primary w/ no C upstream; behavior-neutral on current ROM — 0 tags, mcvtld now hasm; golden regen absorbs the S108+S109 bank drift) + new `build_kmc_asm_tu_index` + `KMC_ASM_TU_DEF_RE` + 61-pass suite; #2 `docs/hazards.md#asm-mirror-vendoring` KMC-as sub-lane (recipe + `li`→`addiu` edit + merge note) + CLAUDE.md index row; #3 multi-subseg-merge note (folded into #2). regime mirror → **seed-only, banked = seed 2pt** (asm-mirror, no v2 residual). Mirror track seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 110 | re-home osSpTaskYielded + __osGetCurrFaultedThread to src/libultra/ + vendor os/parameters.s (0x8AC20) | 2+1 | mirror / re-home (-O2 src/main → -O3 src/libultra verbatim) + asm-mirror `hasm` / clean | 4 | — | **Re-homed two trivial libultra leaf fns mislocated in src/main/ to their correct src/libultra/{io,os} subdirs as verbatim upstream mirrors, and resolved the 0x8AC20 "remaining file".** func_800AB600.c→io/sptaskyielded.c (osSpTaskYielded) + __osGetCurrFaultedThread.c→os/getcurrfaultthread.c, both verbatim upstream, matched at the -O3 libultra profile (they'd matched at -O2 only by coincidence). C_FILES is a find-glob → `git rm` the old src/main files (an orphan double-defines). The 0x8AC20 asm subseg is NOT padding: it is libultra `os/parameters.s` (.text = `.space 0x60`, the ABS() N64 OS globals leoBootID/osTvType/…/osAppNMIBuffer); vendored verbatim as `hasm` (ABS macro from sys/asm.h, .text 0x60 exact). Full-make ROM SHA-1 == baserom. 0 stuck-far/permuter/carried/re-opened. Mirror track seed-only -- committed 4pt / banked 4pt -- `(real)` |
| 111 | libultra `.data`/`.rodata` resolution sweep (≈18 TUs, whole 0xA32D0–0xADCA0 region) | 18 | **classical/mixed** / whole-region carve: 12 placed drop-def restores + 6 data-TU vendors (thread/vitbl/vi/3×vimodes) + exceptasm un-strip + gu/libm_vals / **new `#data-rodata-carve` playbook** | 8 | *13* | **Resolved every attributable anonymous `.data`/`.rodata` block in the libultra region into named `libultra/<tu>` subsegs.** Three carve kinds: drop-def restore (extern→def, upstream initializer in ROM-addr order, 0x10 `.data`-size-pad boundary rule), data-only TU vendor (thread.c/vi.c functions stay asm; vitbl 56-entry osViModeTable + vimodes verbatim), and asm-mirror UN-STRIP (exceptasm `__osIntOffTable`/`__osIntTable`/`__osHwIntTable`/`__osPiIntTable` re-emitted from the `.s` — the S107 strip was REVERSIBLE once `.text` re-exports the `.L<addr>` labels). `__libm_qnan_f`=its own gu/libm_vals.s data TU. Carried: ADC40/ADCA0 (atan/_xsincos rodata → need those asm fns vendored first), A4A90 (unattributable, drmario64 also generic). **Major re-open:** a hand-rolled ungated `sha1sum` read a coincidentally-green stale ROM and reported MATCH across 3 commits that NEVER built (vitbl missing `VI_CTRL_ANTIALIAS_MODE_0`; timerintr `&__osBaseTimer` unplaced bss reloc; `vi` static vs split viinit.c) — caught at the review gate, root-caused + fixed, genuinely re-verified. Retro applied **3 of 3**: #1 `tools/verify-rom.sh` gated-verify helper (+ CLAUDE.md mandate); #2 `docs/hazards.md#data-rodata-carve` playbook (4 gotchas) + CLAUDE.md index row; #3 splat attribution-oracle + asm-mirror-jtbl-reversible notes. v2 realized: seed 8 → realized **13** (+1 masked-failure re-open, +3 mid-sprint link/header debugs, +1 large multi-TU scope), **residual +5**; regime mixed -- `(real)` |
| 112 | atan.c (libkmc C-mirror: _xatan/atan/atan2) | 3 | mirror / verbatim C-mirror + recover-extern + `.rodata` carve / single-file-pack (8-gate → **verbatim-mirror exemption**) | 13 | — | **First libkmc C-mirror banked.** Verbatim cp of `libkmc/src/atan.c` (CORDIC fixed-point on `long long` XLONG + doubles) at the libkmc `-O` profile (`LIBKMC_CFLAGS` = `-O2`→`-O`). All 4 flagged risks (rodata literal-pool order / `long long` shift codegen / `-O` fp scheduling / K&R protos) held FIRST BUILD, 0 iteration. Enablers: place `_atbl`=0x800C9690 (shared atbl.c CORDIC table, extern from atan.c/sin.c; the whole `[0xA4A90,data]` blob = 0xBD8 = the table → symbol-place, NOT a carve), vendor `cordic.h`, carve `[0xADC40,rodata]`→`[0xADC40,.rodata,libkmc/atan]` (generic-subseg-bound 1-line flip, 0x60 B). All callees pre-placed (`__floatdidf`/`__fixunsdfdi`/`__matherr`); KMC GCC inlines the `long long` shifts (0 shift-helper jal). **Full-make ROM SHA-1 == baserom (clean rebuild).** md5-candidate 161→**162**. 0 stuck-far/permuter/carried/re-opened. Cross-repo follow-up: import `_atbl` to the Ghidra workspace. Retro applied **3 of 3**: #1 `docs/hazards.md#.rodata sibling` stale-orphan-after-retype-carve note + S112 provenance; #2 `#recover-extern` unindexed-upstream-mirror no-scan note (root cause: atan/sin upstream `none`, NOT a regex gap — `EXTERN_DATA_DECL_RE` already matches `extern XLONG _atbl[];`); #3 BACKLOG band-exhaustion paragraph (sin.c = last libkmc non-hasm). **Verbatim-mirror exemption confirmed for a pts-13 single-file-pack carrying recover-extern + rodata-carve (not just a clean cp).** regime mirror → **seed-only, banked = seed 13pt**. Mirror track seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 113 | sin.c (libkmc C-mirror: _xsincos/sin/cos/tan) | 4 | mirror / verbatim C-mirror + `.rodata` carve / single-file-pack (8-gate → **verbatim-mirror exemption**) | 13 | — | **libkmc C-band COMPLETE — sin.c was the last non-`hasm` unit.** Near-free retry of S112 atan.c: the S112 carry-over's 5-point checklist replayed verbatim-correct, **0 rework, 0 iteration**. Verbatim cp of `libkmc/src/sin.c` (CORDIC fixed-point, same XLONG/MBIT/CBIT idioms as atan.c) at the libkmc `-O` profile. Gate enabler = ONE text flip `[0x8E660,asm]`→`[0x8E660,c,libkmc/sin]`; execution = verbatim cp + carve `[0xADCA0,rodata]`→`[0xADCA0,.rodata,libkmc/sin]` (whole generic subseg = 0x90 B = 18 doubles, exact-bound, the atan.c [0xADC40] precedent). **ZERO new symbols** — all 4 names curated, `_atbl`/`__fixunsdfdi`/`__floatdidf` placed (S112/S109); asm-confirmed (asm/8E660.s) the only jals are those 3 + `_atbl` + the FP pool. No `__matherr` dep (unlike atan). KMC GCC emits `__fixunsdfdi` (NOT `__fixdfdi`) for the signed `XLONG=double*MBIT` idiom, same as atan.c. **Full-make ROM SHA-1 == baserom (first build).** md5-candidate 162→**163**. 0 stuck-far/permuter/carried/re-opened. After this libkmc is fully mined (only mmuldi3/mcvtld `hasm` remain). Retro applied **3 of 3**: #1 BACKLOG libkmc-fully-mined epic-status; #2 near-free-retry checklist 3rd-clean-hit doctrine data-point (S75 contquery / S93 xldtob / S113 sin.c); #3 `docs/hazards.md#compile-profiles` KMC `__fixunsdfdi`-not-`__fixdfdi` codegen note. **3rd verbatim-mirror-exemption pts-13 single-file-pack** (S64/S69/S112/S113). regime mirror → **seed-only, banked = seed 13pt**. Mirror track seed-only -- committed 13pt / banked 13pt -- `(real)` |
| 114 | nusicallbackadd.c + nusicallbackremove.c (nuSiCallBackAdd + nuSiCallBackRemove) | 1+1 | mirror / cold libnusys / c-combined 2-file split + recover-extern (coddog 99.99 each) | 4 *(reconstructed)* | — | **BACKFILLED at the S115 review — commit 9690ad8 was banked but its `/sprint-review` never ran, so VELOCITY/RETRO/BACKLOG had no row and the SPRINT.md suggestion buffer was lost.** Verbatim nusys-2.07 mirror pair: `[0x7DE70,asm]` split at the 0x7DF10 file boundary into `nusicallbackadd` + `nusicallbackremove`; 1 recover-extern `nuSiCallBackList`=0x800C7E30 (defined by the un-decompiled `nusimgr.c`). Full-make ROM SHA-1 == baserom. md5-candidate 163→**165**. Quality 0/0/0/0 (reconstructed from the clean commit; no standup survives). Seed reconstructed ~4pt (S16 2-file-libnusys-recover-extern precedent); suggestion buffer unrecoverable. regime mirror → **seed-only, banked = seed ~4pt**. Mirror track seed-only -- committed ~4pt / banked ~4pt -- `(real, reconstructed)` |
| 115 | nugfxthread.c (gfxThread + nuGfxThreadStart) | 2 | mirror / cold libnusys / drop-static-mirror (file-static + defines-data, single-file-pack:2fn, coddog 99.99) | 5 | — | **Banked the NuSYS graphics thread + its starter as a clean drop-static mirror — the proven S87/S90 pattern, first-build SHA, 0 iteration.** Body byte-verbatim; the 4 header-declared globals drop-def (nusys.h `extern`) + the 2 file-statics drop-static-to-extern, all placed at asm-recovered `main_bss` vrams: `nuGfxPreNMIFunc`=0x800C7DC4 (adjacent to placed `nuGfxFunc`@0x800C7DC0), `nuGfxMesgBuf`=0x800E72D0/0x20, `nuGfxThread`=0x800E72F0/0x1B0 (contiguous run), `GfxStack`=0x800F54A0/0x2000, `nuGfxMesgQ`=0x801052D0/0x18; all 5 callees pre-placed. **GfxStack tell:** the stack-top arg `Stack + N` resolved in asm to `%lo(PiMesgQ)`=0x800F74A0 (PiMesgQ placed S99/nupiinit.c, a NAMED cross-TU symbol) → base = 0x800F74A0 − `NU_GFX_STACK_SIZE`(0x2000) = 0x800F54A0, zero gap arithmetic. No rodata carve (2-case switch, no jtbl). **Full-make ROM SHA-1 == baserom (first build).** md5-candidate 165→**166** (all 166 src .c stub-free). 0 stuck-far/permuter/carried/re-opened. Both fn names pre-curated → gate = 1 yaml flip. **Cross-repo follow-up:** 5 new decomp data symbols (`nuGfxPreNMIFunc`/`nuGfxMesgBuf`/`nuGfxThread`/`GfxStack`/`nuGfxMesgQ`) → propagate via `sync_decomp_names.py --import-from-decomp`. Retro applied **2 of 2**: #1 `docs/hazards.md` drop-static fast-path stack-top-equals-named-adjacent-symbol tell; #2 `docs/hazards.md` 2nd `drop-static-mirror:<n>bss` under-count class (header-declared uninit global). regime mirror → **seed-only, banked = seed 5pt**. Mirror track seed-only -- committed 5pt / banked 5pt -- `(real)` |
| 116 | nucontgbpakmgr.c (nuContGBPakMgrInit + 7 GBPak mgr fns) | 8 | mirror / cold libnusys / **.data-carve mirror** (single-file-pack:8fn, coddog 99.99, 8-gate → **verbatim-mirror exemption**) | 8 | — | **Banked the 64GB Pak Manager (nuContGBPakMgrInit/Remove + 6 static contGBPak* dispatch leaves) — the FIRST libnusys `.data` carve, first-build SHA, 0 iteration.** Verbatim nusys-2.07 cp. The file DEFINES initialized `.data` (`funcList[8]`@0x800C7E00 = NULL+6 contGBPak*+NULL 0x20 + `nuContGBPakCallBack`@0x800C7E20 = {NULL, funcList, 0x0500} 0xC → `.o` `.data` 0x30 w/ 4B align pad), carved **mid-`main_data`** via a 3-way split (`main_data | [0xA3200,.data,nucontgbpakmgr] | main_data_2`). **Shared-at-a-field gotcha (resolved in-sprint, no carry):** the gate base-only share-check grepped only `D_800C7E20`/`D_800C7E00` → wrongly read "sole-referrer → carve"; the still-asm sibling `nuContGBPakFwrite` (0x7D970) actually reads `nuContGBPakCallBack.func` at **base+4** (`D_800C7E24`), surfaced ONLY by the link error. DROP impossible (the `callback→funcList→6 statics` ref-chain orphans the statics → dead-strip risk), so carve forced even though shared. Resolved by naming the base **canonical + sized** `nuContGBPakCallBack=0x800C7E20 // size:0xC` → splat renders the sibling as `nuContGBPakCallBack + 0x4` (identical bytes, binds the C export; NOT a `D_` placeholder, NOT `defined:False`). All 8 names pre-curated, all 9 callees placed (nuSiCallBackAdd/Remove S114, 6× osGbpak*, nuSiMesgQ). **Full-make ROM SHA-1 == baserom (first build).** md5-candidate 166→**167** (all 167 src .c stub-free). 0 stuck-far/permuter/carried/re-opened (1 link-error gotcha, mirror enabler hiccup not classical iteration). **Cross-repo follow-up:** 1 new decomp data symbol (`nuContGBPakCallBack`) → propagate via `sync_decomp_names.py --import-from-decomp`. Retro applied **4 of 4**: #1 whole-object share-check (sweep base..base+size) + #2 mid-struct canonical-addend pattern + #3 S97 "AND droppable" qualifier → `docs/hazards.md#defines-data` S116 paragraph; #4 verbatim-mirror exemption .data-carve clarification → CLAUDE.md ## Story points. **4th verbatim-mirror-exemption pts-8/13 single-file-pack** (S64/S69/S86/S112/S113/S116; first `.data`-carve variant). regime mirror → **seed-only, banked = seed 8pt**. Mirror track seed-only -- committed 8pt / banked 8pt -- `(real)` |
| 117 | nucontmgr.c (nuContMgrInit + 8 Controller Mgr fns) | 9 | mirror / cold libnusys / **.data-carve + drop-def hybrid** (one-tu 9fn, coddog 99.99 [2.07 STRUCTURAL only], 8-gate → **verbatim-mirror exemption**) | 8 | — | **Banked the NuSYS Controller Manager (nuContMgrInit/Remove + DataClose/Open + 5 static dispatch leaves contReadData/contQuery/contRetrace/contRead/contReadNW) — the S116 sibling, banked atomically BUT with 3 in-sprint gotchas (first mirror-track sprint with real diagnostic friction, no carry).** **HEADLINE: source is nusys-2.05, NOT the pinned 2.07.** coddog-sweep-nusys (2.07 ref) matched @99.99 STRUCTURALLY but the 2.06/2.07 rewrite changed nuContMgrInit's loop `for(...;cnt++){ ...; bitmask<<=1; }` → comma-operator `for(...; bitmask<<=1, cnt++)`, which KMC emits 2 instrs shorter (`.text` 0x330 vs 0x340), cascading a ROM-wide `-0x10` shift (100740 diff bytes). Diagnosed by compiling every nusys revision: 1.10/1.20/2.06/2.07 give 0x330, **2.00≡2.05 give 0x340** (code-identical, only JP/EN comments) → used English 2.05. **Hybrid carve:** `.data` (nuContReadFunc 0x4 + funcList 0x14 + nuContCallBack 0xC, but `.o` `.data` SECTION = **0x30** w/ 0xC 16-align END pad → carve 0xA3240..0xA3270, NOT the 0x24 content I first tried → overlap) out of `main_data` 3-way split; **BSS drop-def** the 6 scattered-COMMON globals (nuContWaitMesgQ/DataMutexQ/Status/Num + 2 statics WaitMesgBuf/DataMutexBuf) → extern (3 explicit, 3 from nusys.h). **entry.s hasm sync:** naming nuContNum=0x8010C2D0 evicted `D_8010C2D0` that `_start` (hasm) uses as the initial `$sp` (boot stack top IS &nuContNum, highest BSS) → byte-neutral D_→name edit. **Full-make ROM SHA-1 == baserom.** md5-candidate 167→**168** (all 168 src .c stub-free). 0 stuck-far/permuter/carried/re-opened (3 mirror-enabler gotchas, not classical iteration). **Cross-repo follow-up:** 13 new decomp symbols (5 fn nuContMgrRemove/contQuery/contRetrace/contRead/contReadNW + 8 data nuContWaitMesgQ/WaitMesgBuf/DataMutexQ/DataMutexBuf/Status/Num/ReadFunc/CallBack) → `sync_decomp_names.py --import-from-decomp`. Retro applied **3 of 3**: #1 nusys-version-divergence + version-hunt playbook (`docs/hazards.md#coddog-cross-ref`) + #2 carve-extent = `.o .data` section size 16-aligned (`#defines-data`) + #3 hasm-referrer byte-neutral sync at gate (`#file-static`). regime mirror → **seed-only, banked = seed 8pt**. Mirror track seed-only -- committed 8pt / banked 8pt -- `(real; atypical mirror friction)` |
| 118 | nucontrmbmodeset.c + nucontrmbforcestop.c (nuContRmbModeSet + nuContRmbForceStop) | 1+1 | mirror / cold libnusys / c-combined 3-way split + **per-file version near-verbatim int-mask drop** (modeset=nusys-2.00, forcestop=2.07) | 2 | — | **Banked the RMB pair off the smallest remaining libnusys block (`[0x7D3B0,asm]`, 240B), both first-build SHA, 0 iteration.** Split the c-combined pack 3-way at file boundaries (`[0x7D3B0,c,nucontrmbmodeset] + [0x7D470,c,nucontrmbforcestop] + [0x7D490,asm]`); zero symbol adds (both names pre-curated in ghidra_symbols.txt; `nuContRmbCtl`@0x80104F50 + `nuSiSendMesg`@0x800A2824 placed). **HEADLINE (extends S117): nusys version is per-file, and per-file *within* the RMB family.** `nuContRmbForceStop` is code-identical 2.00..2.07 → verbatim 2.07-sdk cp. `nuContRmbModeSet` is NOT: nusys-2.05 wrapped the body in an `osSetIntMask(OS_IM_NONE)`/restore pair (2 jals, non-leaf), kept through 2.07; MG64's asm is a **0-jal leaf** = the pre-2.05 (2.00) variant. Mirror = 2.07-sdk verbatim **minus the 3-line int-mask wrapper** (the near-verbatim drop; keeps English comments, IS the 2.00 leaf code). This resolved pick_target's `jal-count-mismatch:2vs0` (it compared the 2.05+ source's 2 osSetIntMask jals vs the 0-jal asm — a pure version artifact). **Full-make ROM SHA-1 == baserom.** md5-candidate 168→**170** (all 170 src .c stub-free). 0 stuck-far/permuter/carried/re-opened. `func_800A2090` (8B empty stub, NOT nuContRmbForceStopEnd) left as `[0x7D490,asm]` per PO scope — a near-free follow-up. **Cross-repo follow-up:** none (0 new symbols, both names pre-curated). Retro applied **3 of 3**: #1 `docs/hazards.md#near-verbatim-mirror-jal-count-mismatch` nusys int-mask-wrapper anchor + provenance; #2 `pick_target.py` libnusys `(version-artifact?)` annotation on the osSetIntMask-wrapper jal-count-mismatch (+unit test, golden catch-up S114-S118); #3 BACKLOG func_800A2090 near-free-retry note. regime mirror → **seed-only, banked = seed 2pt**. Mirror track seed-only -- committed 2pt / banked 2pt -- `(real)` |
| 119 | func_800A2090 + nucontgbpakfread.c (func_800A2090 + nuContGBPakFread) | 1+1 | **mixed** / classical(trivial leaf) + **near-verbatim block-reorder** (libnusys) / trailing-pad:8B@16 + jal-mismatch-macro-artifact + no-coddog | 3 | **4** | **Cleared the 0x7D490–0x7D7A0 GBPak/RMB region: the S118 near-free carry-over + the GBPak file-read leaf.** `func_800A2090` (0x7D490): the deferred carry-over, a trivial classical 8B empty leaf `void func_800A2090(void){}` (KMC -O → `jr $ra;nop`); the 8B subseg tail is the `trailing-pad:8B@16` to nucontgbpakmgr@0x7D4A0, landed clean (ld fills from the next section); seed-only, 0 iter, kept `func_` name. `nuContGBPakFread` (0x7D7A0): **planned a clean verbatim 2.07 mirror, banked a near-verbatim block-reorder** — the verbatim cp built+linked clean but SHA-MISSED; in-tree vs asm objdump showed the SAME 117 insns REORDERED. Root cause: MG64's per-file nusys rev runs the RAM-enable block (`bzero`/`data[31]=…`/`nuContGBPakWrite`) BEFORE `nuContGBPakCheckConnector` (`ram=0` in the range-check `beqz` delay slot, `jal CheckConnector` at the skip-label), but EVERY archived 1.20/2.00/2.05/2.06/2.07 is CheckConnector-first; hand-swapped the two source blocks → insn-identical, full-make ROM SHA-1 == baserom. The `jal-count-mismatch:5vs9` was an UNRELATED macro artifact (nusys.h `nuContGBPakRead`/`Write` → `nuContGBPakReadWrite`; 9 asm jals == 9 expanded call sites). The un-refuted tell at the gate was the "no coddog" flag (a reorder breaks the structural fingerprint). **Zero symbol adds** (func_ name kept; nuContGBPakFread=0x800A23A0 + all callees pre-curated). md5-candidate 170→**172** (all 172 src .c stub-free); asm subsegs 112→110. Quality 0/0/0/0 (1 mid-sprint re-attempt: verbatim cp → block-reorder fix). **Cross-repo follow-up:** none. **First near-verbatim-reclassification (S119 #3): a nominal mirror that SHA-misses + needs a diagnosis+edit scores on the classical track, not the mirror point mass.** Retro applied **3 of 3**: #1 `pick_target.py seed_points` prices jal-mismatch + no-`coddog-mirror` at mirror-floor +1 (near-verbatim risk) + unit test + golden regen; #2 `docs/hazards.md#near-verbatim-mirror-jal-count-mismatch` block-reorder sub-case + CLAUDE.md index row; #3 `VELOCITY.md` near-verbatim-reclassification rule. regime mixed → **seed 3; realized 4; residual +1** (func_800A2090 seed 1 / realized 1; nuContGBPakFread seed 2 / realized 3, +1 re-attempt/version-divergence). Classical/near-verbatim track -- committed 3pt / banked 4pt realized -- `(real)` |
| 120 | nucontgbpakfwrite.c (nuContGBPakFwrite) | 1 | **near-verbatim block-reorder** (libnusys) / c-combined 2-file split + jal-mismatch-macro-artifact + no-coddog + **sibling-known swap** | 3 | **2** | **Banked the LAST GBPak-family asm leaf, the direct sibling of S119's nuContGBPakFread — first-build, 0 iteration.** Decomposed the pts-8 c-combined:2file `[0x7D970]` pack at the file boundary (rom 0x7DB80): flipped the `nucontgbpakfwrite` singleton, left `func_800A2780` leaf + `nusimgr.c` in the trailing `[0x7DB80,asm]` (carry). **Same block-reorder as S119, applied UP-FRONT:** the asm runs the RAM-enable block (`bzero`/`data[31]=0xa`/`nuContGBPakWrite`) BEFORE `nuContGBPakCheckConnector` (`ram=0` in the range-check `beqz` delay slot, `jal CheckConnector` at the skip-label 0x800a2610), so the verbatim 2.07 cp got its CheckConnector + RAM-enable blocks hand-swapped before the first build → ROM SHA-1 == baserom, 0 iteration (vs S119's discover-via-re-attempt). `jal-count-mismatch:5vs10` was the benign macro artifact (`nuContGBPakRead`/`Write` → `nuContGBPakReadWrite`, confirmed by callee 0x800A2310). **Zero symbol adds** (name curated; all 5 callees placed). md5-candidate 172→**173** (all 173 src .c stub-free); asm subsegs 110→110 (the split is net-zero: one asm subseg flipped to c, one new trailing asm subseg created). Quality 0/0/0/0, **0 re-attempt**. **Cross-repo follow-up:** none. Retro applied **3 of 3**: #1 `pick_target.py` `block-reorder-sibling:<file>` tag (BLOCK_REORDER_FAMILIES + `_block_reorder_sibling`, unit test + golden regen); #2 `unattrib-leaf:0x<vram>` lone-straddler flag on a c-combined split (`_straddling_unattrib`, unit test); #3 this VELOCITY sibling-known refinement (per-fn provenance: a sibling-known swap banked first-try scores realized −1). **First sibling-known near-verbatim (S120 #3): seed keeps +1 near-verbatim risk, realized −1 verbatim-first-try.** regime near-verbatim (classical track) → **seed 3; realized 2; residual −1**. Classical/near-verbatim track -- committed 3pt / banked 2pt realized -- `(real)` |
| 121 | nucontrmbmgr.c (8/9 banked; contRmbControl carried) | 8 | **verbatim mirror → cross-jump-tail-merge WALL** (libnusys) / `pack:9fn` single-file-pack + .data-carve + drop-to-extern + coddog@99.99 | 8 | **0** | **The verbatim-mirror exemption's first PARTIAL bank: a sub-100 coddog single-file-pack that was NOT atomic.** Planned a clean 9-fn pure-C md5-candidate (S116 .data-carve template); 8/9 fns verbatim-matched but `contRmbControl` hit a `#cross-jump-tail-merge` WALL — the project gcc 2.7.2 merges two byte-identical `j .L_ret; sh v0,4(s0)` counter-store tails (STOPPING `counter--` + FORCESTOP `counter=2`) that MG64 keeps UNMERGED (build 0x1EC < target 0x1F8). **Exhaustive multi-session RE (user-directed, out-of-band): byte-identical tails ⇒ NOT permuter-fixable** (145k iters plateaued at 220; the merge only breaks by adding instrs → can't byte-match); **no available compiler binary reproduces it** — real-KMC (drmario64) + decompals FSF-2.7.2 (== project tools/cc/gcc, byte-identical) BOTH merge; SN 2.7.2 (all variants) + gcc 2.8.1 (papermario) mis-allocate (5 regs/40B frame/$19-hoist) AND merge; the target's selective pattern (osMotor merged, counter+FORCESTOP not) is NON-MONOTONIC in any cross-jump knob (instrumented find_cross_jump: call_min-indistinguishable). **PO chose option B:** banked 8/9 as verbatim C, `contRmbControl` carried as INCLUDE_ASM, ROM green. Enablers: subseg 0x7CD60 asm→c; .data carve 0xA31D0 (0x30B); nuContRmbCtl drop-to-extern (0x80104F50); 4 msg-handler names to symbol_addrs. matched-fn +8; md5-candidate 173→**173** (file NOT candidate, 1 stub); asm subsegs 110→**109**. Quality: spike 1 / permuter 1 / carried 1 / re-opened 1. **Cross-repo follow-up:** 4 new decomp-side symbols (contRmbForceStop/ForceStopEnd/Start/StopMesg) → `sync_decomp_names.py --import-from-decomp`. Retro applied 3 of 4 (#2 `docs/hazards.md#cross-jump-tail-merge` + #4 `#permuter-setup-for-kmc-toolchain-mirrors` + CLAUDE.md index rows; #3 folded the downgraded compiler-fix conclusion into #2; #5 CLAUDE.md sub-100-coddog exemption hedge; #1 coddog-near-verbatim flag NOT selected). **regime mirror → seed 8; banked 0pt (per-file all-or-nothing: file partial/carried). The +8 matched-fn count is the value signal, not the file point.** Mirror track seed-only → no realized tier (the partial-bank wall is logged as the quality counter-metric + carry, not a residual). |
| 122 | nusimgr.c (nuSiMgrInit + nuSiSendMesg + nuSiMgrStop/Restart/Thread + func_800A2780 leaf) | 6 | mirror / cold libnusys / drop-static + drop-def mirror + same-TU leaf + needs-define version-rev fix (effective single-file, 8-gate clear at 5<8) | 5 | — | **Banked the S120-split carry-over `[0x7DB80]` subseg (6 fns) into ONE md5-candidate — clean first-build SHA after a single one-byte header fix.** Verbatim nusys-2.07 `nusimgr.c` drop-static (`nuSiMesgBuf`=0x800F7600 / `siMgrThread`=0x800F7620 / `siMgrStack`=0x800F77D0, +symbol_addrs) + drop-def (`nuSiMgrMesgQ`=0x8012D408 +symbol_addrs; `nuSiMesgQ`@0x801B93C0 / `nuSiCallBackList`@0x800C7E30 already placed). No carve (all bss → SHA-neutral), no trailing-pad (`.o` `.text` == 0x2F0 exactly). **HEADLINE 1 — the leaf is SAME-TU, not foreign:** `func_800A2780` (0xC) is `return siMgrStack;` — its 0x800F77D0 target is the base of nusimgr.c's own file-static `siMgrStack` (siMgrThread 0x800F7620 + 0x1B0), and a static is file-local, so the leaf MUST be nusimgr.c (an MG64-added leading accessor the 2.07 upstream lacks) → banked with it, resolving the S120 carry-over's foreign-vs-same-TU question. **HEADLINE 2 — version-rev `needs-define`:** the byte-verbatim mirror SHA-missed by exactly ONE byte (vram 0x800A27E0 `li a1,6`→`5`, the `osCreateThread` thread-id), because vendored nusys-2.07 `NU_CONT_THREAD_ID=6` but MG64's rev compacts the controller/SI thread to slot 5; fixed in `include/libnusys/nusys.h` (grep-confirmed nusimgr.c is the SOLE tree consumer → collateral-free; clean-rebuild). md5-candidate 173→**174** (all 174 src .c stub-free); asm subsegs 109→**108**. Quality 0/0/0/0 (one normal mirror fix-iteration, not a counter event). **Cross-repo follow-up:** 7 new decomp symbols (3 fn nuSiMgrStop/Restart/Thread + 4 data nuSiMesgBuf/siMgrThread/siMgrStack/nuSiMgrMesgQ) → `sync_decomp_names.py --import-from-decomp`. Retro applied **3 of 3**: #1 `docs/hazards.md#needs-define` version-rev single-immediate-byte sub-case + CLAUDE.md index row; #2 `docs/hazards.md` leaf-returns-static SAME-TU rule (refines the S120 `unattrib-leaf` foreign framing); #3 `BACKLOG.md` near-free-retry checklist 6th item (nusys header-value reconciliation). regime mirror → **seed-only, banked = seed 5pt**. Mirror track seed-only -- committed 5pt / banked 5pt -- `(real)` |

**Seed-velocity = 2.0 pt/sprint** (bootstrap anchors S1–5, sum seed 10 / 5 sprints). With the
live-logged mirror sprints S6 (seed 1) + S7 (seed 2) + S8 (seed 1) + S12 (seed 2) + S13 (seed 1)
+ S14 (seed 1) + S15 (seed 3) + S16 (seed 4) + S17 (seed 6) + S18 (seed 2) + S19 (seed 9)
+ S20 (seed 6) + S21 (seed 2) + S22 (seed 3) + S23 (seed 2) + S24 (seed 3) + S25 (seed 5)
+ S26 (seed 5) + S27 (seed 8) + S28 (seed 6) + S29 (seed 6) + S30-mirror (seed 1) + S32 (seed 10) + S34-mirror (seed 3) + S35 (seed 16) + S36 (seed 6) + S37-mirror (seed 4) + S38 (committed 2, banked 2 — retroactively resolved) + S47 (seed 3), the
running mirror-regime committed-seed-velocity is 133 pt / 34 sprints = **3.91 pt/sprint**; banked-velocity is 133 pt / 34 sprints = **3.91 pt/sprint** (S38 retroactively banked — .rodata sibling yaml fix resolved same session) (S31 + S33 were pure classical, 0 mirror pts; S32 banked 10 mirror pts; S35 banked 16 mirror pts; S36 opened the audio band, 6 mirror pts; S37 mixed: 4 mirror pts) — the warm-pool
+ S48 (seed 5) + S49 (seed 2) + S50 (seed 2), the
downward drift reverses as a *new* cold band opens (S6 last clean vi leaf; S7 opened the *cold*
convert band at the 2 pt floor; S8 the warm-1 thread leaf; S12 the warm-2 recover-extern thread
leaf; S13 6th vi mirror; S14 3rd thread mirror — note S14 was a **zero-enabler warm singleton**
(one yaml flip), so the "warm clean-singleton pool mined out" claim is **vi-band-specific**, not
project-wide: the thread band still yields zero-enabler clean leaves; S15 paid a one-time
+1 enabler to open the *cold* libnusys band — a fresh deep pool of pts-2 cold mirrors; S16
sibling-batched 2 of them, seed 4, each a recover-extern over a non-`__` library global that
pick_target had mis-reported clean — the S16 retro broadened the refs-unplaced grep to close
that false-clean); S17 trio-batched the nuContGBPak siblings (seed 6) and S19 the nuGfx\*FuncSet
trio (seed 9). **The post-S16 rise to 2.63 is sibling-batch-driven, not per-leaf throughput** —
S17/S19 banked 3 files each in one sprint, so points/sprint reflects how many homogeneous
siblings were committed (the "commitment-shaped, not pure throughput" caveat below), not faster
matching; per-leaf effort is still the flat cold-mirror 2–3 pt. **S9 is the first classical-track row** (seed 5,
banked 5, first-pass clean) — logged separately, NOT folded into the mirror 3.30 average
(never compare regimes). Classical track: **n=4** (S9 seed 5 / realized
illustrative; S11 seed 5 / **realized 5 logged**; S30 (mixed) seed 5 / **realized 5 logged**;
S31 seed 8 / **realized 10 logged**; S33 seed 5 / **realized 5 logged**), seed-velocity 5.60 pt/sprint (n=5: 5+5+5+8+5=28/5). S9 proved the loop
mechanically but produced **zero residual variance** (clean wrapper); **S11 produced the first
real variance** (non-trivial leaf, seed compiled 0.80, matched after 1 fix-iteration), so the
PO **activated v2** at the S11 review. The realized tier is live on this track. **S31 is the first positive residual (+2 net: func_800A2F50 residual −1, nuGfxInit residual +3)** — the seed rubric UNDER-priced nuGfxInit's novel-gotcha complexity (GBI macro frame requirement + absolute-physical-address linker symbol). The +1 Fibonacci snap for novel-bank-gotcha is validated; consider +2 for libnusys classical with two distinct novel patterns next occurrence. **S33 residual 0** (seed priced correctly; first-pass clean across all 3 fns; compile-flag discovery overhead absorbed — not counted as classical iteration since it required changing global tooling, not iterating on C).
Three honest caveats:
- **Fitted, not validated.** The bands were chosen so this history reads ~2 pt/sprint; the
  first out-of-sample sprint is the real test. The byte gates (768/1536) and the classical
  5/8/13 tiers have **zero** supporting data — provisional, corrected by v2's residual loop.
- **Non-stationary.** The recent ~2 came from *warm* sibling pairs, but of the ~56 remaining
  mirror candidates only ~18 are warm and ~38 are cold-band (the 2–3 pt tier) — expect velocity
  to **drift down within the mirror regime** as the warm pool depletes, then a discontinuity at
  the classical wall.
- **Commitment-shaped, not pure throughput.** Sprints are PO-scope-boxed, so points/sprint
  partly measures how much was committed, not raw output — read it as a **regime indicator**,
  not a productivity score.
- **S0 excluded.** Sprint 0 (7 files, pre-Scrum) had no DoD discipline and was batch-extracted
  before the overlay; its 7-files/sprint outlier would corrupt the per-sprint baseline. It
  stays in `BACKLOG.md` as historical record, out of this calibration.

## Velocity (headline — v1 mirror track + v2 classical track active)
- **Mirror track (v1): seed-velocity 2.0 pt/sprint** (n=5 anchors, all verbatim; seed-only —
  the mirror band is a point mass, no realized tier).
- **Classical track (v2 ACTIVE since S11): n=9, seed 4.44 pt/sprint (S11+S30+S31+S33+S34+S37+S39+S42+S43: (5+5+8+5+2+3+3+3+6)/9=40/9), realized 4.33 pt/sprint ((5+5+10+5+2+3+3+2+4)/9=39/9), net residual −1 (S31 first positive +3; S34/S37/S39 residual 0; S42 −1; S43 −2, most-negative).**
  Rolling-5 (S34+S37+S39+S42+S43): seed 3.4 pt/sprint (17/5), realized 2.8 pt/sprint (14/5). S31 nuGfxInit is the first over-seed classical fn (+3 residual); rubric needs +1 for double-novel-gotcha libnusys classical targets. S42 defines-data verbatim-body drop: realized 2 = seed 3 − 1 (the fast path makes a defines-data leaf bank like a mirror) — confirms the −1 verbatim-first-try rule extends to known-edit-mirror defines-data, not just classical seeds. S43 is the new residual floor (−2): both gbpak fns were classical-FLAGGED (jal-count-mismatch) but proved pure verbatim mirrors once the macro-FP was stripped — the seed over-priced them because the hazard was a tooling artifact, now fixed in `_c_jal_count`, so future macro-heavy libultra leaves should seed lower.
- **Regime:** `mirror` is a depleting minority (~22 % of ranked candidates: 56 mirror vs 194
  classical; 18 warm / 38 cold). The warm clean-singleton mirror pool is now **mined out** (S11
  plan gate: every top mirror candidate carries a blocking hazard), pushing the project onto the
  classical track — which is exactly what tripped v2.
- **S7 note:** vi band exhausted of clean leaves (S6), so S7 opened the **cold convert band**
  (`virtualtophysical`, seed 2 = the cold-mirror floor). The S7 retro added a **`refs-unplaced`
  hazard** to `pick_target.py` (a referenced data extern absent from both name files — the dual
  of `defines-data`); it re-priced `osYieldThread` 1→2 and `osEPiLinkHandle` 2→3, and confirmed
  `osStopThread` is the smallest *clean* remaining leaf (seed 1, warm, no unplaced ref). Still
  7-straight first-pass-clean mirrors → zero residual variance → v2 trigger remains unmet.
