# func_80074500 near-match (S243)

**Status:** CARRY.

**SHARED FINAL VERDICT (S243, supersedes the giv-form framing below):** the store-giv wall IS SOLVED via
the per-command post-increment idiom (see func_80074230.near-match.md). The irreducible residual is the
shared gcc-2.7.2 sched.c **schedule-order coin** (`rank_for_schedule:2428` class-then-LUID defers the
class-1 atlas compute behind class-3 stores) — see docs/wip/func_80073F24.near-match.md ## SHARED ROOT
CAUSE + memory [[sched-class-tiebreak-order-coin]]. TERMINAL for hand-source; permuter-responsive but not
byte-exact in 2 rounds (S243, PO gate-override). Retry this seed with the post-inc idiom (`{ Gfx* g =
gfx++; g->words.w0=..; g->words.w1=..; }`) before any future permuter run.

--- original S243 first-pass framing (pre-convergence) ---
Fully RE'd, ~174/194 rows byte-identical, sub-0.97 (asm-differ `percent` reads
negative: a dual-pointer store-giv structural misalignment inflates the insertion penalty; the
instruction multiset is ~correct). NOT permuter-attempted (below the 0.97 gate; residual is a
non-permuter-reachable loop.c strength_reduce giv-form + `beql` coin).

**What it is:** raw-DL-word glyph-string emitter, `func_80071370.c` family, 16-col hi/lo-nibble atlas,
glyph advance width 8. `void func_80074500(Gfx** dst, s16 x, s16 y, u8* str)`. `gDPPipeSync` bookends;
per-char loop emits an 11-command hand-rolled per-glyph DL block (SetTImg/SetTile/RDPLoadSync/LoadTile/
PipeSync/SetTile/SetTileSize/PipeSync + hand-rolled TexRect/RDPHalf1/RDPHalf2). Does NOT map to a clean
`gDPLoadTextureTile` (LoadTile coord frac ×2 vs SetTileSize ×4; render SetTile 4b vs load 8b). Space
(0x20) advances x+=8. Lowercase remap `(u32)(ch-0x61)<0x1A -> ch+=0x80`. Punct-narrow (cx-=6,cy-=6,x-=8)
fires only for code in {0x9E,0x9F,0xDE,0xDF}; 0xB0 special-cases to 0x70 with no narrow. row=code/16
(signed floor), col=code-row*16.

**Extern needed:** `extern void* D_800E1C30;` (texture base, used as `((u32)D_800E1C30 + 8) & ~7`).

**Landed levers (transferable, confirmed necessary):**
- `s32 cx/cy` (sign-extended from s16 x/y), NOT s16 — s16 mis-strength-reduces the glyph block to
  col*5/row*5/col*20 garbage under in-loop register pressure.
- flag+goto punctuation classify (`narrow = code<0x9E; goto block_14;`) beats structured nested-if (~660)
  — matches the ROM shared-narrow block via the goto (S237 fold_range_test-defeat pattern).
- Punct branch-chain order: `==0xB0`, `<0xB1`, `<0xA0`, `<0x9E` / else `<0xE0`, `<0xDE`.

**The wall (shared family coin, see also func_80074230/func_80073F24):**
1. **Store-block induction form.** ROM uses a WALKING store giv `t1` hoisted to the preheader
   (`addiu t1,t2,-4`, anchored at the w1 field so w0 = `-4(t1)`, w1 = `0(t1)`, `addiu t1,t1,8`/command)
   running PARALLEL to a value pointer `t2` bumped `+0x58`/glyph. No source form reproduced the
   hoisted-parallel-giv (tried `gfx++`×11, `gfx[k]`+bump, backward-index, separate walker + `gfx+=11`
   which gcc collapses). loop.c `strength_reduce` biased-pointer canonicalization coin
   (`#indexed-vs-pointer-loop-strength-reduction`, 0 project cracks).
   - NOTE: this seed used indexed `gfx[k]` + `gfx += 11`; the sibling crack-74230 later found the
     per-command post-increment idiom `{ Gfx* g = gfx++; g->words.w0=..; g->words.w1=..; }` (gbi-macro
     expansion) gives the two-pointer walking form and suppresses a hoist — retry this seed with that
     idiom before any future crack-dive (still expected sub-gate, but closer).
2. `beql` vs `bne` on `if(code==0xB0)` (branch-likely with `li code,0x70` in the delay slot); a gcc
   delay-slot/branch-prob heuristic, source-invariant.

Working seed: `nonmatchings/func_80074500/base.c` (also scratch-stashed).

## Final near-match C
```c
void func_80074500(Gfx** dst, s16 x, s16 y, u8* str) {
    Gfx* gfx = *dst;
    s32 cx, cy, code, row, col, narrow;
    u8 c;

    gfx->words.w0 = 0xE7000000;
    gfx->words.w1 = 0;
    gfx++;
    c = *str;
    if (c != 0) {
        code = c & 0xFF;
        do {
            if (code == 0x20) {
                str++;
                x += 8;
            } else {
                cx = x;
                cy = y;
                if ((u32)(code - 0x61) < 0x1A) {
                    code += 0x80;
                }
                if (code == 0xB0) {
                    code = 0x70;
                } else if (code < 0xB1) {
                    if (code < 0xA0) {
                        narrow = code < 0x9E;
                        goto block_14;
                    }
                } else if (code < 0xE0) {
                    narrow = code < 0xDE;
block_14:
                    if (narrow == 0) {
                        cx -= 6;
                        cy -= 6;
                        x -= 8;
                    }
                }
                row = code / 16;
                col = code - row * 16;
                str++;
                x += 8;
                gfx[0].words.w0 = 0xFD68003F;
                gfx[0].words.w1 = ((u32)D_800E1C30 + 8) & ~7;
                gfx[1].words.w0 = 0xF5680200;
                gfx[1].words.w1 = 0x07000000;
                gfx[2].words.w0 = 0xE6000000;
                gfx[2].words.w1 = 0;
                gfx[3].words.w0 = (((col << 4) & 0xFFF) << 12) | (((row << 5) & 0xFFF) | 0xF4000000);
                gfx[3].words.w1 = ((((col * 8 + 7) * 2) & 0xFFF) << 12) | ((((row << 5) | 0x1C) & 0xFFF) | 0x07000000);
                gfx[4].words.w0 = 0xE7000000;
                gfx[4].words.w1 = 0;
                gfx[5].words.w0 = 0xF5600200;
                gfx[5].words.w1 = 0;
                gfx[6].words.w0 = (((col << 5) & 0xFFF) << 12) | (((row << 5) & 0xFFF) | 0xF2000000);
                gfx[6].words.w1 = ((((col * 8 + 7) * 4) & 0xFFF) << 12) | (((row << 5) | 0x1C) & 0xFFF);
                gfx[7].words.w0 = 0xE7000000;
                gfx[7].words.w1 = 0;
                gfx[8].words.w0 = ((((cx + 8) * 4) & 0xFFF) << 12) | ((((cy + 8) * 4) & 0xFFF) | 0xE4000000);
                gfx[8].words.w1 = (((cx * 4) & 0xFFF) << 12) | ((cy * 4) & 0xFFF);
                gfx[9].words.w0 = 0xE1000000;
                gfx[9].words.w1 = (col << 24) | ((row << 8) & 0xFFFF);
                gfx[10].words.w0 = 0xF1000000;
                gfx[10].words.w1 = 0x04000400;
                gfx += 11;
            }
            c = *str;
            code = c & 0xFF;
        } while (c != 0);
    }
    gfx->words.w0 = 0xE7000000;
    gfx->words.w1 = 0;
    gfx++;
    *dst = gfx;
}
```
