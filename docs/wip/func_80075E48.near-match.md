# func_80075E48 (src/main/func_80071370.c) — unrolled raw-DL-word emitter CARRY class

**Status:** CARRIED at seed triage (S275). Not attempted; classified from `.s` tells.

Takes `a0` (a Gfx** / list ptr); reads `glistp = *a0`, precomputes ~16 per-entry buffer pointers
(base+k*8) into registers (v1,a3,t0-t7,s0-s7,fp all live) and stages ~11 raw Gfx command words
(0xE3xxxxxx othermode_h variants, 0xE2 othermode_l, 0xD7 texture, 0xFCFFFFFF/0xFFFDF2F9 combine,
0xE7...) from a stack scratch area (sp+0x4,0xC,0x14,...) into each entry. 9 callee-saved regs
(s0-s7,fp) = extreme register pressure. Unrolled raw-DL-word emitter (no glistp spill, but 16 live
entry pointers) -> reg-permutation + store/sched-order coin class. Reconstruct as the fixed Gfx[]
sequence via gDP macros when taken as a dedicated slice; not a smallest-first leaf.
