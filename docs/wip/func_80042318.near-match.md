# func_80042318 (src/main/get_tile_attribute.c) — reorg delay-slot coin near-match

**Status:** CARRIED (S275). Fully reverse-engineered, register/structure/signedness-exact at
**171/172** instructions. Sole residual = two gcc `reorg` (delay-slot) coins in the dispatch tail;
source-invariant, permuter-plateaus (base score 300, no improvement in 500+ iters with
`--settings permuter_settings_main.toml`). Behavior is 100% correct.

## Function
Menu/HUD input handler (course/club select grid). Verified matching C body:
```c
void func_80042318(void) {
  u32 sel;
  u32 i;
  u16* input;

  D_800FBE70 = 0;
  flag_clear(7);
  D_800C30D1 = 0;
  sel = 0;
  if (D_800FED18 & 0x1000) {                         // cancel
    clear_text_grid();
    D_800BB08C = 0; D_800BB09C = 2; D_800BB0A0 = 0;
    play_sound_effect(0x5F, 0xF, 0x7F);
    return;
  }
  for (sel = 0; sel < 16; sel++) {                   // find current selection index
    if (D_800BB0A4[sel] == D_800DAED0) break;
  }
  for (i = 0; i < 16; i++) {                          // render 16 rows
    if (i == sel) {
      set_flag_based_on_param(1);
      check_and_print_grid(D_800CAB94, 10, sel + 5);  // "->"
    } else {
      check_and_print_grid(D_800CAB98, 10, i + 5);    // "  "
    }
    check_and_print_grid(func_80042228(D_800BB0A4[i]), 12, i + 5);
  }
  input = &D_800FED18;
  if (*input & 0x800)  { sel = (sel + 0xF)  & 0xF; D_800DAED0 = D_800BB0A4[sel]; play_sound_effect(0x5C,0xF,0x7F); } // up
  if (*input & 0x400)  { sel = (sel + 0x11) & 0xF; D_800DAED0 = D_800BB0A4[sel]; play_sound_effect(0x5C,0xF,0x7F); } // down
  if (*input & 0x8000) {                             // confirm
    clear_text_grid();
    D_800BB08C = 0; D_800BB09C = 2; D_800BB0A0 = 0;
    D_800BB020 = D_800DAED0;
    play_sound_effect(0x5D, 0xF, 0x7F);
    if (D_800DAED0 == 0xE) func_8005D9A0(1); else play_bgm_by_id(1, 0);
    switch (D_800BB020) {                            // u32 -> sltiu range split
      case 9: break;
      case 7:   func_8005F838(0); func_80069CE4(); init_sky_pool_and_world_state(); break;
      case 0xC: func_8005F838(1); init_sky_pool_and_world_state(); break;
      case 0xD: func_8005F838(2); init_sky_pool_and_world_state(); break;
    }
  }
}
```

## Load-bearing levers (all applied, all correct)
- **Search loop = `for` (peels first iteration + `bnel`)**; hoists `D_800DAED0` invariant to a0.
- **Render `row` is a giv, NOT a variable:** write `i + 5` inline (used in 2 print args) so loop.c
  strength-reduces it to its own incrementing register — biv `i`->$s2, giv->$s1. A separate `row`
  local reversed the allocation ($s1/$s2 swap). [[loop-invariant-hoist-order-preheader-regalloc]]
- **`u32` loop counters** (`sel`,`i`) -> `sltiu` loop bounds (s32 gave signed `slti`).
- **`u32 D_800BB020`** -> switch uses unsigned `sltiu` range split (s32 gave `slti`).
- **`u16* input = &D_800FED18`** -> caches the base in a callee-saved reg across the 3 reads +
  intervening calls (plain global reloaded %hi/%lo each time). [[per-region-cse-slot-base-lever]]
- **Dispatch = `switch`, not an if-else chain** -> beq-to-out-of-line-case bodies + `sltiu` split
  (if-else gave inline `bne`-skip). Cases must include `case 9: break;` (empty) to make the tree
  split at 0xA (10), matching the ROM; a {7,12,13}-only switch splits at 0xC.
- **`func_800660A0` returning a pointer** is NOT this fn (that's func_800985B4). Here callees are
  straightforward.

## The terminal residual (2 reorg coins, count 171 vs 172)
1. **`beq $v1,9` root-split delay slot (the -1):** ROM leaves it `nop`; mine fills it with the
   following `sltiu $v0,$v1,0xA` range check. Every OTHER switch comparison matches (beq==7 nop both,
   beq==0xC/0xD filled with the next const both). Only the ROOT equality test's slot differs. gcc
   reorg deems the sltiu safe to hoist (v0 dead on the taken path -> void return), and fills it; the
   ROM's reorg left it empty. Source-invariant: switch structure, cases, and value order all match.
2. **`jal func_8005D9A0` delay slot (content-only, same count):** ROM shares one `li a0,1` in the
   `bne ...0xE` delay slot for BOTH the func_8005D9A0(1) fall-through and play_bgm_by_id(1,0) taken
   paths, leaving the jal delay `nop`; mine redundantly re-emits `li a0,1` before the jal (reorg did
   not delete the redundant copy).

Both are gcc-2.7.2 `reorg.c` delay-slot fill/redundancy-elimination decisions with no C-source lever
(permuter randomization can't reach them; 500+ iters, base 300, zero improvement). Distinct from the
S233 mine-1-SHORTER scheduler-coin class — here the divergence is which delay slot carries the split
test, not a load-latency hazard. Kin: [[sched-class-tiebreak-order-coin]],
[[sched-coin-loop-preheader-order-lever]].

## Unblock path
If a future gcc-2.7.2 `reorg.c` dive finds what tips the root-equality-branch delay-slot fill (why the
ROM leaves the split test out of the `beq==9` slot), this banks immediately — it is otherwise byte-exact.
The imported permuter tree is at `nonmatchings/func_80042318/` (main settings).
