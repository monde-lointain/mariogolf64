# func_8008E164 near-match (S247)

**Semantics (100% RE'd):** integer lerp, truncated.
```c
s32 func_8008E164(s32 a0, s32 a1, f32 t) {
    return a0 * (1.0f - t) + a1 * t;   // t in $a2 (GPR) per o32: float arg after 2 int args
}
```

**Status:** FP body BYTE-EXACT. Built object's 16 FP instrs (lui/mtc1/sub.s/cvt.s.w/mul.s x2/
add.s/trunc.w.s/mfc1) match the ROM register-for-register and in order.

**Residual (the only diff):** ROM wraps the return in a dead 8-byte frame:
```
addiu sp,sp,-8
sw    v0,0(sp)        ; stores INCOMING (uninitialized) $v0, never reloaded
trunc.w.s $f0,$f2
mfc1  v0,$f0
jr    ra
 addiu sp,sp,8        ; sp-restore fills the jr delay slot
```
My leaf build emits no frame (`jr ra; nop`) because the body needs no stack. The conversion path is
identical (trunc.w.s + mfc1, register, NOT memory) in both, so the frame is NOT from the float->int
cast. The `sw v0` stores an uninitialized return register — a dead store of a value the source never
computes. Not reproducible from clean C: reads as a DCE-leftover / frame-allocation coin (a path that
set+stored v0 was eliminated, leaving the store + its frame). gcc-2.7.2, leaf, no source lever found.

**Class:** frame/dead-store scheduling coin (kin to S243 sched-order + the dead-frame family).
Permuter unlikely (no reg/instr variance to search; it is a frame presence/absence + dead-store).
Carry.
