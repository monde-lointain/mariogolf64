# func_80098C6C near-match (TERMINAL sched.c coin; barrier-only crack, PO-declined)

**Fn:** `func_80098C6C` in `src/main/func_80095A10.c` (0x80098C6C, 13 instr, 0x34 B, leaf).
**Status:** CARRIED terminal. Re-opened S282 (compiler-source fan-out). Semantics 100% RE'd.
A byte-exact 13/13 exists ONLY with two empty `__asm__ __volatile__("")` scheduling barriers;
**the PO declined banking with empty barriers (S282)**, so it carries as pure C (12 instrs, 1 short).

## Semantics (fully decoded)
```c
s32 func_80098C6C(void) {
  s32 ret = 0;
  if (fabsf(D_800E4C7C) == 0.0f) { ret = 1; }
  return ret;
}
```
`fabsf` is a gcc-2.7.2 builtin (`abssf2` = inline `abs.s`, no jal). `D_800E4C7C` is `extern f32`.
Declare `extern f32 fabsf(f32);` (missing prototype flips regalloc/sched, S225). The ret-accumulator
form is required (the `return fabsf(x)==0.0f;` and `if(x)return 1;return 0;` forms invert to `bc1t`).

## S282 FRESH RE-DERIVE (supersedes the stale S222 verdict)
The S222 doc claimed "exact 13/13 #local-alloc-qty-permutation." STALE (S268): pure C is **12 instrs,
1 SHORT**, not 13, and the residual is a scheduler ORDER coin, not just a qty permutation.
- ROM   : `l.s $f4 ; abs.s $f2,$f4 ; mtc1 $0,$f0 ; c.eq.s $f2,$f0`   (13; a nop fills the mtc1->c.eq slot)
- pure C : `l.s $f0 ; mtc1 $0,$f2 ; abs.s $f0,$f0 ; c.eq.s $f0,$f2`   (12)

## Root cause (gcc-2.7.2, pass-cited; assembler ruled out)
Binutils rule-out (S282) proved by byte-exact KMC-as reconstruction that ALL nops are as-inserted and
deterministic — 100% of the residual is gcc codegen (see `docs/levers.md` (kmc as noreorder not global nop oracle)).
1. **ORDER (the missing 13th word).** `abs` (adder, ready-delay 2) and the `0.0f` `mtc1` (memory/xfer,
   ready-delay 2) feed the compare with equal `insn_cost`, so the BACKWARD list scheduler has them ready
   the same cycle. sched1: `schedule_select` (sched.c:2615) breaks the tie with `potential_hazard`
   (sched.c:1317) — adder SIMULTANEITY 1 -> blockage 2 (nonzero) beats memory SIMULTANEITY 0 -> blockage
   1 (0); abs wins -> placed LAST (after mtc1). sched2: `priority()` (sched.c:1424) abs=3 vs mtc1=1 ->
   abs first again. Putting `abs.s` BETWEEN the mtc1 and the c.eq.s is what earns the ROM's nop
   (mtc1->cop1 COPROC_MOVE_DELAY, gas tc-mips.c:1312-1339). Costs come from mips.md's
   `define_function_unit`; `ADJUST_COST` (mips.h:2946) only zeroes anti/output deps. Source-invariant
   across 12 spellings (operand swap, hoisted temps, `!(x!=0)`, volatile global, array form, manual
   `x<0?-x:x`, extra block insns, deleted goto). `-fno-schedule-insns -fno-schedule-insns2` reproduces
   the ROM order exactly -> the ROM order is the UNSCHEDULED one.
2. **REGALLOC ($f4/$f2/$f0 distinct vs in-place $f0/$f0/$f2).** local-alloc qty order
   `floor_log2(n_refs)*n_refs*size/(death-birth)` (local-alloc.c:1579) needs the loaded value
   longest-lived (allocated last, conflicting with $f0 and $f2); nothing in a 13-instr fn consumes it
   past the mtc1.

## The only crack = empty asm-volatile barriers (PO-DECLINED)
`__asm__ __volatile__("")` emits zero bytes but `sched_analyze` treats it as a full dependency barrier
(the only zero-byte way to end a scheduling region — CODE_LABEL needs a live branch ref, a call emits
`jal`, a `volatile` MEM only orders memory). One plain barrier between abs and compare fixes ORDER; one
operand barrier `("" : : "f"(x))` after the `if` holds `x` live -> fixes REGALLOC. With both, 13/13
byte-exact. See `docs/levers.md` (empty asm volatile sched barrier). **PO S282: empty barriers are NOT sanctioned for
banking** (no in-tree precedent; all existing inline asm emits real instructions). Fallback pure C is a
HARD terminal (1 short, no priority tie -> permuter cannot score it).

## Verdict
TERMINAL sched.c `schedule_select` potential_hazard coin + local-alloc qty. Not permuter-reachable
(1-short, no tie to permute). Do NOT re-grind. If the empty-barrier policy is ever revisited, the crack
is in `nonmatchings/func_80098C6C/base.c` git history / the barrier form above.

## ROM-wide context (backlog)
This exact tie-class is unmatched tree-wide: same divergence in still-asm code at 0x8005958C
(`lwc1; abs.s; li.s; c.lt.s`) and 0x80058DBC; sibling wall func_80059BA0 is the same
`schedule_select` mechanism. The matched `clamp_min_distance_from_target` @0x8003A3DC does NOT
contradict (its `cvt.s.w` chain has unequal cost -> no tie).
