# func_80098C6C near-match (`#local-alloc-qty-permutation` + scheduler tie-break)

**Fn:** `func_80098C6C` in `src/main/func_80095A10.c` (0x80098C6C, 13 instr, 0x34 B, leaf).
**Status:** CARRIED near-match. Semantics 100% RE'd; byte-diff = 4 FP-setup instrs only
(register quantity-ordering + one schedule swap). Below 0.97 (permuter N/A). Sprint 222.

## Semantics (fully decoded)
```c
s32 func_80098C6C(void) {
  s32 ret = 0;
  if (fabsf(D_800E4C7C) == 0.0f) {
    ret = 1;
  }
  return ret;
}
```
`fabsf` is a gcc-2.7.2 builtin (`c-decl.c:3217`, `BUILT_IN_FABS` -> `abssf2` = inline `abs.s`,
no jal). `D_800E4C7C` is an `extern f32`. Return-select half (bc1f, `v0=0` in delay /
`v0=1` fall-through) matches the ROM byte-for-byte after the `ret`-accumulator form
(`s32 ret=0; if(...) ret=1; return ret;` — the direct `return fabsf(..)==0.0f;` and the
`if(..) return 1; return 0;` forms both invert to `bc1t`/`v0=1`-in-delay).

## Residual diff (4 FP instrs)
| slot | ROM (target)                | my build                    |
|------|-----------------------------|-----------------------------|
| load | `lwc1 $ft0(f4)`             | `lwc1 $f0`                  |
| abs  | `abs.s $fv1(f2), $ft0(f4)`  | `abs.s $f0,$f0` (in-place)  |
| zero | `mtc1 $zero, $fv0(f0)` (AFTER abs) | `mtc1 zero,$f2` (BEFORE abs, fills load-delay) |
| cmp  | `c.eq.s $fv1(f2), $fv0(f0)` | `c.eq.s $f0,$f2`            |

Two coupled divergences:
1. **Local-alloc quantity ordering.** ROM: zero->f0, abs_result->f2, loaded_val->f4
   (loaded_val distinct, abs NOT in-place). Mine: loaded_val->f0 (abs reuses it in-place),
   zero->f2. The loaded-value pseudo dies at the abs in both; the ROM allocator nonetheless
   colored it to a distinct higher reg (f4) with zero taking f0. `local-alloc.c` quantity
   priority (live-length weighted) is the driver; source-invariant here.
2. **Scheduler load-delay fill.** Mine hoists `mtc1 zero` into the `lwc1`->`abs` load-use
   delay slot (aggressive); ROM leaves the stall and emits `mtc1` after `abs`. Same
   pre-reload scheduler (INSN_SCHEDULING active, see memory `kmc-cc1-no-instruction-scheduler`),
   different tie-break from the pseudo numbering.

## Source levers tried (all produce IDENTICAL f0/f2/in-place/mtc1-first output)
- `return fabsf(x)==0.0f;` / `if(..) return 1; return 0;` -> `bc1t` reversed (WORSE; the
  `ret`-accumulator form fixes the branch half but NOT the FP setup).
- `0.0f == fabsf(x)` (reversed compare operands) — no change (c.eq canonicalized).
- `f32 x = D_800E4C7C; fabsf(x)` (live temp) — coalesced away, no change.
- `f32 ax = fabsf(D_800E4C7C); if(ax==0.0f)` (two temps) — no change.

## Verdict
`#local-alloc-qty-permutation` class (kin to S221 `func_80098CD8`, S213/S210 walls). Permuter
skipped by rule (project cracks on this class = 0; a 13-instr leaf gives it almost nothing to
permute). Escalation = corpus-mining, not the permuter, not more source shapes. ROM stays green
off the still-`INCLUDE_ASM` stub.
