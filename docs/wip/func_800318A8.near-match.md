# func_800318A8 (src/main/func_8002A640.c) — raw-DL glistp++ store-giv CARRY class

**Status:** CARRIED at seed triage (S275). Not attempted; classified from `.s` tells against the
documented S243 raw-DL-word CARRY class.

Frame-render DL builder. `v1 = D_800B7780` (0/1 buffer toggle); `base = D_801B94A0 + v1*0x1B580`
(double-buffered command list); runs `func_800328E0`/`func_80032B78` glue, emits ~11 raw Gfx command
words (0xE7 pipesync, 0xED setscissor, 0xE3 othermode_h, 0xFC61FEC3 combine, 0xE2 othermode_l,
0xE1/0xF1 rdphalf, 0xE9/0xDF, 0xFA000000, 0x4000400...) into the buffer via a glistp pointer SPILLED
to `sp+0x10` and reloaded 16x (the store-giv tell), then `func_80031450`, `nuGfxTaskStart(base,
size,...)`, `osGetTime`/`__udivdi3` frame-timing -> D_800B7738, then `D_800B7780 ^= 1`.

16 glistp-spill `sw ...(sp)` + 11 DL-cmd `lui` = the S243 raw-DL-word emitter (memory
`docs/levers.md` (mg64 glyph emitter dl family): "post-inc solves store-giv, sched-order coin terminal"). High carry
risk. Reconstruct via glistp++ raw words or gDP macros (`docs/levers.md` (gfxdis dl emitter reconstruction crack))
when taken as a dedicated slice; not a smallest-first leaf.
