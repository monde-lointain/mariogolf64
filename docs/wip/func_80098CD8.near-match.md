# func_80098CD8 near-match (TERMINAL uniform reg-rotation; exact-count 38/38)

**Fn:** `func_80098CD8` in `src/main/func_80095A10.c` (0x80098CD8, 38 instr, 0-jal/0-fp).
**Status:** CARRIED terminal. Re-opened S282 (compiler-source fan-out). NOW EXACT-COUNT 38/38.
3-arg byte-mapper. Callers: `func_800990D0` (itself a carry) @0x80099434 sets only a0/a1/a2.

## Function
```c
s32 func_80098CD8(u8 *src, u8 *dst, s32 count) {
  s32 i;
  for (i = 0; i < count; i++) {
    u32 b = src[i];                                 // u32 + src[i] = lbu direct, no andi
    if (b >= 28) return -1;                          // unsigned -> sltiu (matches ROM)
    *dst++ = *D_800C73F0[((s32)b + i) % 28];         // (s32) cast -> signed /28 magic 0x92492493
  }
  return 0;
}
```
extern: `u8 *D_800C73F0[];`

## S282 FRESH RE-DERIVE (corrects the S221 verdict)
S221 said "exact 38/38, pure scheduler tie-break, permuter 366k plateau 270." Partly STALE (S268):
- The FAITHFUL `u8 b = *src++;` compiles to **40 instrs** (a redundant `andi $x,$y,0xFF` after the
  `lbu` from the QImode zero-extend + an extra src-param `move`), NOT 38. Reaching exact 38 needs the
  form above: `u32 b = src[i]` (SImode lbu-direct, no andi; unsigned -> `sltiu`) + `(s32)b` (signed
  `%28`). This is the state the S221 permuter run should have been measured on.
- At exact 38/38 the residual is TWO things, and only ONE is the "scheduler tie-break" S221 named.

## Residual at exact 38/38 (assembler ruled out, binutils S282)
1. **Uniform +1 register rotation (the terminal core).** ROM: i=`$t0`, src=`$t1`, magic=`$t2`,
   table=`$t3`, mfhi=`$t4`. Mine: i=`$a3`, src=`$t0`, magic=`$t1`, table=`$t2`, mfhi=`$t3` — every
   register shifted DOWN one; ROM SKIPS `$a3` entirely. gcc-2.7.2 has NO `REG_ALLOC_ORDER` override
   (config/mips/mips.h), so allocation is default-ascending and `$a3` (=$7) is the forced first free
   caller-saved pick -> mine takes it for the top-priority qty (i). For ROM to skip `$a3` it would need
   `$a3` reserved, but the fn is genuinely 3-arg (no phantom param; call site confirms). No source form
   (10+ tried: while / do-while / fn-scope temps / walking vs indexed src+dst / value temp / split sum)
   and no permuter run (366k iters, plateau 270, S221) skips `$a3`. This is the irreducible
   `#local-alloc-qty-permutation`.
2. **Store/increment schedule swap (coupled, NOT the driver).** ROM: `sb; dst++($a1); i++; slt; bnez;
   src++ (delay)` (store FIRST). Mine: `src++; i++; sb; slt; bnez; dst++ (delay)` (increments hoisted
   above the store). This HALF yields to an empty `__asm__ __volatile__("")` after the store (38 -> 34
   cmpfn diffs), but **the register rotation persists even then** — proving the rotation is INDEPENDENT
   of the schedule, contradicting the S221 "one root" framing. And the barrier is PO-declined (S282, see
   [[empty-asm-volatile-sched-barrier]] and func_80098C6C).

## Verdict
TERMINAL `#local-alloc-qty-permutation` (uniform `$a3`-vs-`$t0` rotation). Permuter-proof (S221 366k
plateau; the class has 0 project cracks). The exact-count-first discipline (S272) reached exact 38 and
the register permutation still did NOT resolve — a genuine no-lever qty coin, not a mis-count. Do NOT
re-grind. Escalation if ever revisited = `#cross-project-matched-corpus-mining` for a matched fn with
the same 3-arg byte-map + `%K`-table idiom to confirm the qty ordering.
