# func_8007399C — near-match (S307, re-measured S308)

`src/main/func_80071370.c`, `0x254` / **149 instructions**. Measured state: **149 of 149 at the
ROM's exact `-0x50` frame**, **70** differing lines in `tools/cmpfn.sh` (S307 recorded 84), and the
residual is confined to **basic block 0** plus the two lines it drags with it.

## The carve is NOT the blocker any more (S308)

S307 recorded this leaf as `cohort-blocked`: bank it with `func_800754BC` and `func_8007580C` or not
at all. **That is wrong, and the sprint proved it.** The host object's `.rodata` carve splits at any
8-aligned jump-table boundary inside the pool, so S308 moved the carve from `[0xACBE0]` to
`[0xACB38]` and banked the other two leaves (`place_glyph_sprite_run`, `emit_glyph_sprite_dl`) with
`jtbl_800D16D0` still sitting in the extracted `[0xACAD0, rodata]` blob. When this leaf closes, the
carve moves once more, `[0xACB38]` -> `[0xACAD0]`, size `0x110` -> `0x178`. Nothing else is needed:
no sibling, no cohort commit.

## Semantics (re-derived from the `.s`, confirmed by the call site)

Measures the on-screen extents of a stored glyph string. `place_glyph_sprite_run` (`func_800754BC`,
banked S308) calls it as `func_8007399C(runId, size, &w, &h, 0)`.

```c
void func_8007399C(s32 id, s32 size, s32* out_width, s32* out_height, s32 player);
```

- `id` indexes the glyph-run table `D_800E1C48`: `[id*2+2]` is the run's start, `[id*2+3]` its length.
- Each code unit is `D_800E1C48[0][D_80104FD0 + start + i]`. Both globals are re-read every
  iteration, so they must stay direct global references.
- `c < 0x100` is an ordinary glyph: `0xDE`/`0xDF` are skipped, `size == 0xC` adds the per-glyph
  advance `D_800C4390[c]`, anything else adds `size`.
- `c > 0x7FFF` dispatches through `jtbl_800D16D0` on `c - 0x8000`, 26 dense arms: 25 = newline,
  15 = `sprintf` the player-name buffer, 20-23 = a per-player s32 at `D_8013F4DC[c-0x8000]` offset by
  `player * 44`, 0-10 and 12-14 = fixed `0xC`, the rest = no advance.
- The three width slots start from the 12-byte zero template at `D_800D16C0`; the result is
  `max(w[0], w[1], w[2])`, stored to `*out_width` **twice**, and the accumulated height to
  `*out_height`.

## Body — replay this, do not re-derive (149/149, exact frame, 70 lines)

```c
extern s32 func_80073BF0(u8* str);
extern u8 D_80105118[];

typedef struct {
  s32 v[3];
} LineWidths;

extern LineWidths D_800D16C0;
extern u8 D_800D1588[];
extern u8 D_800D158C[];
extern s32 D_8013F4DC[];

/* Measures the on-screen extents of the stored glyph run `id`: `out_width`
 * gets the widest of the run's up-to-three lines, `out_height` the accumulated
 * line advance. Control codes above 0x7FFF dispatch through a dense 26-arm
 * table; 25 starts a new line, 15 measures the player-name string and 20..23
 * a per-player s32 read at `player * 44`. */
void func_8007399C(s32 id, s32 size, s32* out_width, s32* out_height,
                   s32 player) {
  LineWidths w;
  s32 total;
  s32 count;
  s32 start;
  s32 c;
  s32 max;
  u16* row;
  s32* oh = out_height;

  total = 0;
  w = D_800D16C0;
  row = D_800E1C48[0];
  count = row[id * 2 + 3];
  row += id * 2;
  start = row[2];
  if (count != 0) {
    s32 line = 0;
    s32 i;

    for (i = 0; i != count; i++) {
      c = D_800E1C48[0][D_80104FD0 + start + i];
      if (c < 0x100) {
        if ((u32)(c - 0xDE) < 2) {
          continue;
        }
        if (size == 0xC) {
          w.v[line] += D_800C4390[c];
        } else {
          w.v[line] += size;
        }
      } else if (c > 0x7FFF) {
        switch ((s32)(c - 0x8000)) {
          case 25:
            line++;
            total += 6 + size;
            break;
          case 15:
            sprintf(D_80105118, D_800D1588, &D_801321B0);
            w.v[line] += func_800738BC(D_80105118, size, 2);
            break;
          case 20:
          case 21:
          case 22:
          case 23: {
            s32* pe = &D_8013F4DC[c - 0x8000];
            u8* q = (u8*)pe + player * 44;

            sprintf(D_80105118, D_800D158C, *(s32*)q);
            w.v[line] += func_80073BF0(D_80105118) * size;
            break;
          }
          case 0:
          case 1:
          case 2:
          case 3:
          case 4:
          case 5:
          case 6:
          case 7:
          case 8:
          case 9:
          case 10:
          case 12:
          case 13:
          case 14:
            w.v[line] += 0xC;
            break;
        }
      }
    }
  }
  max = w.v[0];
  if (max < w.v[1]) {
    max = w.v[1];
  }
  *out_width = max;
  if (max < w.v[2]) {
    max = w.v[2];
  }
  *out_width = max;
  *oh = total;
}
```

## The residual: one cause in block 0 (`sched.c`, pre-reload)

`cmpfn` lines 2-31 are block 0 in a different order, and lines 35-36 / 100-102 follow from it. The
ROM's block 0 is:

```
sw a3,0x24(sp)      # spill out_height
lw a3,0x60(sp)      # player, REUSING a3
... callee saves + the a1/a2 parameter copies ...
lui a2,%hi(D_800D16C0) ... sw a1,0x18(sp)     # the 12-byte template copy
lui v0,%hi(D_800E1C48); lw v0,%lo(...)        # the run-table pointer
move s4,zero        # total = 0
sll a0,a0,2; addu a0,a0,v0; lhu s5,6(a0); lhu s7,4(a0); beqz s5
```

Mine loads `player` first (into `t0`/`a2`/`t1` depending on the spelling) and the run-table pointer
second, and puts the `out_height` parameter copy at the **end** of block 0, so the incoming `$a3` is
live across `player`'s definition and `player` cannot colour `$a3`. Everything else in the diff is
that one colouring plus its knock-on order.

**The pass is `sched.c`'s pre-reload birthing boost, not `rank_for_schedule` priority.**
`adjust_priority` (sched.c:2506) raises an insn to `max_priority` when `birthing_insn_p`
(sched.c:2469) holds: the insn is a `SET` whose `REG` destination is live at that point **and whose
pseudo has `reg_n_sets == 1`**. Both parameter insns qualify, so both are boosted; a boosted insn is
selected as soon as it is ready, and because `schedule_block` runs bottom-up, *selected early means
placed late*. The `out_height` copy has no in-block dependent, so it is ready at T-2 and lands at the
bottom; the template copy (a `PARALLEL` with a `MEM` destination) can never be boosted, so it floats
to the top. Reading the `-dS` dump for block 0 is what shows this:

```
;; ready list at T-2: 19 (7f000001) 22 (1) 54 (3), now 19 54 22
```

`19` is the `out_height` copy at `7f000001` = `LAUNCH_PRIORITY`. **If it were unboosted it would be
selected last and placed first, which is exactly the ROM's layout** — the tie against the template
copy then breaks on LUID, and `assign_parms` gives the parameter copy the smaller LUID.

So the open question is a single one: **what source form gives the `out_height` parameter pseudo a
second `SET`** (`reg_n_sets > 1`), since that is what removes the boost. Every folded spelling
(`out_height = &out_height[0]`, `+ 0`, `| 0`, a self-copy, an aliasing local `s32* oh`) is optimised
away before `sched1` sees it, and a real second assignment changes the instruction count.

## Levers measured (base = the body above unless noted)

| lever | effect |
| --- | --- |
| index form `w.v[line]` + `line++`, no walking pointer | **required.** The ROM's `addiu s0,sp,0x10` is a strength-reduced giv init in the preheader, not a source pointer; a source `s32* wp` puts it in block 0 and adds a cse `move` (150 instructions) |
| `u8* q = (u8*)pe + player * 44;` as its own statement | fixes the case-20 arm's `addu` operand order (`addu v0,v0,s6`, not `s6,v0`); 84 -> 72 lines |
| `row` computed inside the case-20 arm, not hoisted above the `if` | 92 -> 80; the multiply belongs in the loop body so `loop.c` hoists it into the preheader |
| `*(LineWidths*)w = D_800D16C0;` struct copy | **required.** Three scalar `D_800D16C0.v[k]` reads re-materialise `%hi` per element (+1 instruction) |
| block-0 statement order (all 12 permutations of copy / total / count / start) | 70 or 80; the 70-class is every order with the template copy NOT between the two table reads. Does not touch the colouring |
| `u16* row` pointer, `row += id * 2`, two-set block-local pointer | no change (cse folds the second set) |
| local declaration order, `s32 w[3]` vs `LineWidths w`, `const` on the template | no change |
| `bcopy`/`volatile` template | 147 instructions, structurally wrong |
| `player *= 44` (a real second set for `player`) | 82; `player` becomes callee-saved (`s4`) because the reassigned variable is then live across the loop's calls |
| permuter, 550+ iterations at `--stop-on-zero` | base 915, best **340** (`w = D_800D16C0;` moved ahead of the table reads). No zero; the class is the whole-block ordering `docs/workflow/loop.md` records as plateauing |

## The requirement, stated exactly (S309, from the `-dS` ready lists)

S308 named the pass; this states what a candidate source form has to satisfy, and it is two
conditions, not one. Block 0 holds ten schedulable insns. `schedule_block` walks **bottom-up**, so
*selected first means placed last*, and the selection rule is: highest priority wins, ties break on
**descending LUID** (source order). Boosted insns carry `7f000001`, everything else `1` or `3`.

Mine, and what each insn is (numbers from the baseline body above):

| insn | what | boosted? |
| --- | --- | --- |
| 19 | `oh = out_height`, the `a3` copy | **yes** (`reg_n_sets == 1`) |
| 12 | the 5th-argument (`player`) load | yes |
| 22 | `total = 0` | no (`total` is the loop accumulator, 2 sets) |
| 28 | the `D_800D16C0` template block move | no (`PARALLEL` with a `MEM` dest) |
| 33 | the `D_800E1C48` load | yes |
| 37/39/44/54/57 | `sll`, `addu`, the two `lhu`, `beqz` | 44/54/39 yes |

Selection mine: `57 54 19 44 39 37 28 33 12 22` -> placement `22 12 33 28 37 39 44 19 54 57`.
Selection the ROM needs: `57 54 44 39 37 22 33 28 12 19` -> placement `19 12 28 33 22 37 39 44 54 57`,
which is the ROM's block 0 line for line.

Reading the two orders against each other gives both conditions:

1. **Insn 19 must be unboosted** (`reg_n_sets(oh) >= 2`), so it stops being selected at T-3 and falls
   to T-10, which places it first — and that is what frees `$a3` for `player`.
2. **`total = 0` must have a HIGHER LUID than the template copy**, i.e. the `w = D_800D16C0;`
   statement must come *before* `total = 0;` in the source. Both are priority 1, so at T-6 the tie
   breaks on LUID and the ROM picks `total = 0` there, the template copy at T-8. With the doc's
   statement order the tie goes the other way. This is why S307's 12-permutation sweep read "does not
   touch the colouring": condition 2 is invisible until condition 1 is met.

## Refuted at zero cost (S309, all measured; base = the 70-line body)

| form | result |
| --- | --- |
| `s32* oh = *(&out_height);` (address-taken **parameter**) | 150 instr, 293 lines. An addressable parameter is homed at its incoming slot `0x5C`, not the ROM's local `0x24`, and the store still lands after the register saves |
| `s32* oh; s32** ohp = &oh;` (address-taken **local**) | 149/149, exact frame, **80** lines. The store is no longer a `SET` of a `REG` so the boost is gone, but the slot moves to `0x20` and the store is still placed 17th, because a `MEM`-dest insn with no in-block consumer is ready from T-2 and simply loses every tie |
| drop `oh` entirely, `*out_height = total;` | 149/149, exact frame, 80 lines, and the spill slot is back at the ROM's `0x24`. The copy insn survives as insn 19 regardless — the parameter is live across four calls, so `cse` cannot substitute `$a3` the way it does for `id` in `$a0` |
| `oh` re-set from `out_height` immediately before the final store | 80 lines; `cse` folds the pair and the block-0 copy disappears instead of gaining a second set |
| `oh` re-set at the tail of the `if (count != 0)` body | 152 instr (+3) |
| `oh` re-set inside the `if (max < w.v[2])` arm | 154 instr, frame `-0x58` |
| reuse the `row` pseudo as the out-pointer (`row = (u16*)out_height;` + `*(s32*)row = total`) | 150 instr (+1), 123 lines. It *does* unboost the copy — only one insn now hoists above the saves, as in the ROM — but the reuse costs one `move` and the copy still is not selected last, because its LUID is above `total = 0`'s |

So every spelling that gives the pseudo a real second set costs at least one instruction, and every
spelling that removes the `SET`-of-`REG` shape (addressable local, direct parameter) leaves the insn
ready from T-2 with nothing to lose a tie to. The two known-free directions are both closed:
`sched.c:3196-3213` does skip the leading parameter copies out of the scheduling range entirely, but
that region ends at the first non-`INSN` in the chain, and every body statement sits after
`NOTE_INSN_FUNCTION_BEG`; and the sibling entry copies it *does* skip (`move s1,a1`, `move s8,a2`)
print interleaved with the register saves, not ahead of them, so being skipped would not put the
store where the ROM has it either.

## S313: the residual is the block-move scratch clobbers, not the boost by itself

The S308/S309 reading above is correct about the pass but wrong about what the schedule *buys*.
Re-derived from `-dS` (sched1), `-dR` (sched2), `-dl`/`-dg` and the emitted object, the causal chain
that decides the whole 30-row head cluster is:

1. `w = D_800D16C0;` compiles to one `movstrsi_internal` insn (`mips.md:3587`) whose four scratch
   registers are `match_scratch "=&d"` — **allocated, not fixed**. They take the four lowest-numbered
   free `d` registers live-wise at that insn. The ROM's are `v0,v1,a1,a2` (`lui a2` is the address
   temp); mine were `v1,a1,a2,a3` or `t0,v1,a1,a2`.
2. Every scratch is a hard-register conflict for any pseudo live across the block move. `player`
   (the 5th argument, a global allocno assigned after `local-alloc` places the scratches) is live
   across it, so **`player` gets `$a3` only when the scratch set does not**, i.e. only when `$v0` is
   free there and the scratch stops at `a2`.
3. `$v0` is free there only if the `D_800E1C48` row-table load is scheduled *after* the block move at
   sched1 time (the row pointer is a block-0 local quantity and takes `v0`). With the doc's body
   sched1 places the load first, because the block move is ready at T-5 and nothing outranks it at
   T-7 while the load is still queued behind its 3-cycle load-use chain (`beqz` -> `lhu` -> `addu`).
4. `$a3` must also be dead before the `player` load, which means the `a3` parameter copy must be
   *outside* the scheduled region (no `oh` user variable at all — then it is one of the leading
   parameter copies `sched.c:3190` skips) or first inside it (the boost question above).

So the two conditions are real, but they are conditions on the **scratch set**, and 3 and 4 pull in
opposite directions: dropping `oh` satisfies 4 and breaks 3 (block 0 loses its tenth insn, so the
block move is selected at T-7 and the row load lands ahead of it), keeping `oh` satisfies 3 and
breaks 4.

**Measured states (all 149/149 at the ROM's `-0x50` frame unless noted):**

| body | `cmpfn` rows | scratch set | `player` | note |
| --- | --- | --- | --- | --- |
| doc baseline (`oh`, `total = 0` first) | 70 | `t0,v1,a1,a2` | `t1` | S307-S309 state |
| same, `w = D_800D16C0;` before `total = 0` | 80 | — | `t1` | condition 2 alone is worse |
| `LineWidths w = {0,0,0};` + `oh` | 80 | **`a2,v0,v1,a1`** | `t0` | scratch set is the ROM's; only the `a3` copy's position is left |
| drop `oh`, `*out_height = total;` | 80 | `a3,v1,a1,a2` | `t0` | `a3` copy is in the skipped prefix, but the row load beats the block move |
| drop `oh` + `s32* ow = out_width;` for both stores | **68** | `t0,v0,v1,a1` | `t1` | the tenth insn restores the ROM's block-move/row-load order |
| ... + `ow = out_width;` again after the loop | 240 (**151 instr**) | `a2,v0,v1,a1` | **`a3`** | **rows 2-25 are the ROM's, exactly**; costs two `move`s |
| `s32 sz = size;` as the tenth insn | 80 | — | `t0` | the copy coalesces away, so there is no tenth insn |

**The tenth insn cannot be free.** A body copy of a parameter is only a separate insn if the
parameter stays live past it; if it dies there, `local-alloc` coalesces the copy and block 0 is back
to nine insns (the `sz` row). The `ow`-twice form is the one that reaches the ROM's head, and its two
`move`s are exactly that: `ow` and the `out_width` parameter are both live across the loop (the
parameter must reach the second set), so they cannot share `$s8` and the copy survives on both paths.

**And the second-`SET` class the S309 note left open is now measured, in both directions.** A
second set that survives `cse` must be conditional (an unconditional one makes the first set dead
and `flow` deletes it, which returns the copy to the prologue — 98 rows). A conditional second set
after the loop necessarily removes the loop from that variable's live range, which raises its
`global.c allocno_compare` priority above the other out-parameter's and flips which of the two gets
`$fp` and which gets the `0x24` spill: `oh` 3 refs / len 92 / 326 against `out_width` 3 refs / len
102 / 294 (`tools/allocno_report.py`). Since the ROM spills `out_height` and keeps `out_width` in
`$fp`, the flip costs +3 instructions (152). Placements measured: end of the `if` body (326 vs 294),
inside `case 25:` (329, and `player` returns to `t1`), before the final store (`cse` folds it), an
`else` arm (folded), first statement of the loop (folded).

**Also settled: `D_800D16C0` is not an extern.** It is the 12-byte zero constant pool of a local
aggregate initializer, `LineWidths w = {0, 0, 0};` — which is why it sits immediately before
`jtbl_800D16D0` in the same object's `.rodata` and why the carve is the single `[0xACB38]` ->
`[0xACAD0]` move. Spell it as the initializer, not as a copy from an `extern`.

## Next probe

Two directions, in order of expected cost.

1. **Make the tenth insn free.** The requirement is one body insn in block 0 whose source parameter
   is still needed afterwards but which does not add a live range spanning the loop. The `ow`-twice
   form fails only on that overlap, and it is otherwise byte-exact for 25 instructions. A form where
   the second read of `out_width` happens where the parameter is *already* dead would cost nothing —
   nothing in the seven forms measured so far does that.
2. **Attack the scratch allocation directly.** The scratch registers are ordinary local quantities
   (`local-alloc.c:306 alloc_qty_for_scratch`), so anything that makes `$v0` busy-free at the block
   move without a tenth insn reaches the same place: e.g. a spelling that gives the row-table pointer
   a live range starting after the block move, or one that makes the row pointer a *global* allocno
   (allocated after the scratches) instead of a block-0 local.

A `gcc -dS` dump of block 0 is still the 30-second check, but read it for **the order of the block
move against the `D_800E1C48` load**, not for insn 19's priority; and confirm with
`mips-linux-gnu-objdump -d`, where the address temp of the `la` in the template copy must be `a2`.
