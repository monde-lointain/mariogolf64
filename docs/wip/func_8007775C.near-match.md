# func_8007775C near-match (S206 carry)

**File:** `src/main/func_800772B0.c` (spline-interpolation pack)
**Best:** score 3940 / percent 0.732, 147/151 rows.
**Class:** `#pervasive-regalloc-classical-main` / S158 FP-regalloc wall.

## What it is (fully RE'd)
`void func_8007775C(f32 *x, f32 *y, f32 *out)` — cyclic/periodic cubic-spline
coefficient solver (Thomas tridiagonal elimination + Sherman-Morrison corner
correction for the periodic wrap). Leaf, no jal. Frame 0x90 = `f32 sp[36]`, three
logical 12-wide rows: `0x00` knot spacings h, `0x30` tridiagonal diagonal b,
`0x60` RHS/perturbation e. x = knot abscissae x[0..10], y = values, out = 11
output coefficients (out[0]/out[10] the wrapped endpoints). 7 loops: build h + Δ,
build diagonal, initial RHS, forward elimination, back-substitution, corner
correction, final normalize.

## Best body
`nonmatchings/func_8007775C/base.c` (on disk) — all 7 loops structurally correct
(147/151 rows key-match). `f32 sp[36]` single object; h[i]/b[i]/e[i] share one
strength-reduced base (+0/+0x30/+0x60); b[i+1]/e[i+1] as explicit walking
pointers q/r. Loop-bound forms: loops 1,2,4,5,6,7 use `!=` (`bne`), loop 3 uses
`<` (`slti`). `d=sp[0]+sp[9]` commutative flip + distinct temp e fix FP coloring;
loop6 assigns r before q for the b->$a0 / e->$a1 arg coloring.

## Why it walls (4 coupled items)
1. loop3 `slti` vs target `bne` (row ~54): writing `!=` gives the local `bne` but
   cascade-regresses loop5/6 `$a0/$a1` allocation to ~11k. loop3's `slti` is a
   required local sub-optimum for the good global coloring.
2. post-loop2 `d+d` accumulator lands `$f0` vs target `$f2` (rows 36-43): load-reg
   assignment and commutative operand order coupled oppositely, unwinnable by
   source swap.
3. loop5/6 pointer-setup `addiu` ordering (scheduling).
The target reaches a global register allocation (all loops `bne`, correct FP
coloring, correct `$a0/$a1`) unreachable from equivalent C without permuter
regalloc perturbation.

## Verdict
Carry. Structural-complete; residual is coupled global-vs-local register
coloring. Same class as S183/S188/S189 FP carries. Retry: permuter (< 0.97 now),
or cross-project KMC-2.7.2 corpus mining.
