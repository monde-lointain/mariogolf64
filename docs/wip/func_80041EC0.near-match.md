# func_80041EC0 near-match (get_tile_attribute.c, S218)

STATUS: NEAR-MATCH, 99.08% (216/218 instr byte-identical). Blocked by a
compiler/ABI wall, NOT a source-shape issue. base.c at
`nonmatchings/func_80041EC0/`. PERMUTER N/A (the wall is ABI, not regalloc).

## The wall
Callee `func_80041E8C` @0x80041E8C takes its arg in `$v0` (non-o32). Both call
sites in func_80041EC0 pass `&buf` in `$v0`
(`addiu $v0,$sp,0x10 ; jal func_80041E8C`). KMC GCC 2.7.2 o32 always routes arg0
via `$a0`, so no C form emits it. The ONLY residual byte diffs:
- 0x800421E0 (row 201): target `27A20010` (addiu v0,sp,0x10) vs mine `27A40010` (a0)
- 0x80042204 (row 210): same
See memory `func-80041e8c-v0-arg-convention-wall` (verified: func_80041E8C entry
`sw $v0,0(sp)`). Both func_80041E8C and func_80041EC0 carry until the convention
is cracked. Tried forms for the v0 arg: normal call, stored-result, K&R,
`register void* p asm("$2")` (-> `move $4,$2`), 1/2/5-word struct return -> all $a0.

## What DID match (levers)
- Outer/inner loop solved. Bind col into the inner-for header:
  `for (j=0, col=6; j<0x10; j++, col+=2)` (writing `col=6;` as an outer stmt
  hoisted `li s2,6` to preheader AND flipped the sprintf a1/a2 order).
- Wrap-normalize loop as `while(1){ s32 v=D_800BB088; if(v<0){...}else if(v>=8)
  {...}else break; }` matches the single-load + dual `j` back-edge shape.
- `flags=D_800FED18` local for the 8 bit-tests, then direct
  `D_800FED18 & 0x8000/0x4000` for the last two.

## Callees (all func_/D_ auto-resolve at link)
clear_text_grid @0x8004D794; check_and_print_grid(void*,s32,s32) @0x8004DA24;
flag_is_set(s32) @0x800299F4; set_flag_based_on_param(s32) @0x8004D99C;
func_80041E8C(void*)->s32 @0x80041E8C [v0-arg wall]; func_80029A30(s32) @0x80029A30
(set-bit in D_801052F8); func_80029A6C(s32) @0x80029A6C (clear-bit);
sprintf @0x800AADF0. Globals D_800BB080/084/088 (grid cursor), D_800FED18 (u16
input flags), strings D_800CAA7C/84/8C/94, D_801B5588, D_8012F4E8.
