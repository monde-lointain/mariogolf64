# func_800958D8 near-match (S248)

**Semantics (100% RE'd):** gsDPPipeSync + gsDPSetScissor DL emitter.
```c
void func_800958D8(void) {
    ScissorRect r;              // 4x s16 at D_801B7F30 {ulx, uly, lrx, lry}
    r.ulx = 0; r.lrx = 0x140; r.lry = 0xF0; r.uly = 0;   // 320 x 240
    gDPPipeSync(glistp++);
    gDPSetScissor(glistp++, G_SC_NON_INTERLACE, r.ulx, r.uly, r.lrx, r.lry);
}
```
The scissor macro does the `(s32)(coord * 4.0f) & 0xFFF` 10.2 fixed-point conversion (0xED000000 = G_SETSCISSOR); mode 0 = G_SC_NON_INTERLACE.

**Status:** percent 0.833, score 1200, 72/72 rows, empty top_mismatches. Body structure,
FP conversion chain (mtc1/cvt.s.w/mul.s 4.0/trunc.w.s/mfc1/andi 0xFFF/sll 12/or), the
pipesync + scissor DL words, and the signed `lh` reads all match. The best of 5 forms.

**Residual (the only diff):** addressing mode. The ROM references `D_801B7F32/34/36` as
SEPARATE per-field globals (each with its own `%hi/%lo`, 0 displacement) and holds
`&D_801B7F30` in a base register `v1` for field 0 only (store + read of the same address).
The struct form folds all four into ONE base + displacement (`0/2/4/6(base)`).

**Forms tried (why the ROM pattern resists a faithful source):**
1. plain separate `s16` globals -> GCC CONST-FOLDS the scissor coords (forwards the
   just-stored constants; emits a literal DL word, no runtime `lh` at all). The ROM does
   NOT fold — it re-reads each field via `lh`.
2. `volatile s16` -> defeats the fold but forces `lhu`+`sll 0x10`/`sra 0x10` sign-extend;
   the ROM uses a direct signed `lh`. (percent ~0.14)
3. struct member (this form) -> defeats fold, keeps signed `lh`, correct 72-row structure;
   only the base-vs-separate-symbol addressing differs. (percent 0.833)
4. array-of-1 per field (`s16 D_[]; D_[0]`) -> materializes a base per field. (percent 0.578)
5. volatile-cast store + plain read -> re-folds / diverges. (percent 0.008)

**Class:** `#base-register-vs-displacement` + a store->load fold barrier. Separate scalar
globals give the ROM's separate-symbol addressing but fold the constants; the struct/array
forms that defeat the fold impose base+displacement. No faithful source reproduces
separate-symbol addressing AND the no-fold runtime `lh` together. The ROM's no-fold implies
its GCC could not prove `*glistp` (the DL write) doesn't alias the rect, a barrier a faithful
same-compiler C form does not recreate. Permuter does not flip base-vs-displacement
(project history = 0). Carry.

Scratch: `nonmatchings/func_800958D8/base.c` (struct form, 0.833).
