# func_800525C4 near-match (S183) — pervasive-regalloc wall (structure correct)

**BANKED S314** (`3c8ea76`), 156/156. Note the doc's pointer to "the S183 commits" for the near-miss C: it was never committed, and the body was re-derived from the `.s`. The 84-diff cascade was structural, not a coloring wall.

Two near-identical branches on D_801B60A0 (!=0 / =0). Each loops s1<D_801B6098 comparing
val0 = D_801B71FB[arg0row] + func_80052264(D_801B6094, s1, D_801B71F9[arg0*184]) vs
val1 = (same for arg1). Branch1: val0<val1->1, val1<val0->2, else 0. Branch2: accumulate
s2 += (val0<val1) - (val1<val0), then if (fp+1 < |s2|) return s2<=0?2:1 else 0; fp=s0-D_801B6098-1.
s0 = func_800521C0()+func_800521DC().

## Struct-fold KEY (reproduced): D_801B6094/D_801B6098 accessed via base &D_801B60A0-0xC (s7).
Reference as `(&D_801B60A0)[-3]` (=D_801B6094, func_80052264 arg0) and `(&D_801B60A0)[-2]`
(=D_801B6098 loop bound). The pre-loop `s1<D_801B6098` checks + branch2 `fp` use DIRECT D_801B6098
(%hi/%lo). D_801B6094 is NOT a placed symbol -> must go through D_801B60A0.

## Residual: 84-diff REGISTER-PERMUTATION cascade (structure byte-correct)
Frame -0x38, all 10 reg saves, and the `addiu base,base,-12` s7-derivation all MATCH. Mine allocates
the derived base to s5/s6 where the ROM uses s7 -> cascades. #pervasive-regalloc-classical-main.
Full near-miss C committed to git history (see S183 commits) then reverted to INCLUDE_ASM.

## Retry: permuter (--main, big cascade) or #cross-project-matched-corpus-mining.
