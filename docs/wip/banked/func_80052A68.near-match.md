# func_80052A68 near-match (S183) — output-param min/tie reduction, structure divergence

`s32 func_80052A68(s8* out)` (out=NULL -> &stack byte). Pass 1: over D_801B6090 entries, for flag
D_801B71F6[i*0xB8]==1 track min=min(func_80052324(i)) + best index + tie count; else cnt3 += flag==3.
If best!=-1: pass 2 writes out[i] (0/2) per flag+g compare, computes s5; if s5: if ties<=0 pass 3
marks out[i]=1 for winners, ret 1; else ret 2; else ret 0. If best==-1: zero out[], ret cnt3==count?2:0.
D_3FFFFFFF = 0x3FFFFFFF sentinel LITERAL (not a symbol). g(x)=func_80052324 (BANKED).

## Residual: 165 mine vs 162 target, 150 diffs = STRUCTURE + regalloc divergence.
3 loops + output writes + double-g-call (compare+assign, not CSE'd) + sentinel. Same
#pervasive-regalloc-classical-main TU. Full near-miss C in git history. Needs control-flow shaping +
permuter. Carried.
