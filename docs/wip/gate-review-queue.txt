# Conjunctive / disjunctive gate review -- RESOLVED, S284. Kept as the audit record.
#
# Finding: all 119 surviving gate occurrences kept their binding. Zero needed a
# restatement.
#
# The queue was raised in S283 on the theory that Pass A's invariant
# (lower(before) == lower(after)) proves no WORD changed but not that no MEANING
# changed, and that the caps on AND / OR / BOTH / ALL / EITHER / EVERY were doing
# the binding work on a gate that docs/prompt-style.md requires stay joined.
#
# The theory was over-cautious. prompt-style.md ## Conditions are logic already
# said the right thing: "De-shout the glyph if it is shouted, but keep the
# condition and its binding intact." Pass A did exactly that. The binding is
# carried by the WORD, not by its capitalization, and the invariant guarantees the
# word survived. Checked mechanically: for every one of the 119 live rows, the gate
# word is still present in the surviving text.
#
# Read the strongest candidates as they now stand:
#
#   "assert both counts across the rewrite -- `grep -c 'INCLUDE_ASM'` ... and the
#    file's function list, diffed before and after"
#   "the union of the BACKLOG.md parked names and every docs/wip/<fn>.*.md note"
#   "16-aligned at both edges, which requires every function contributing to that
#    pool to be C"
#
# Each still states a conjunctive gate, and each still binds. Capitalizing `both`
# added emphasis, not logic.
#
# 11 further rows are gone because the text carrying them was deleted when the
# fan-out narrative was compressed to its trigger table.
#
# What the exercise DID establish, and what docs/prompt-style.md now records: the
# real risk is a pass that changes words, not one that changes case. A case-fold
# cannot drop a gate word; a restructuring pass can. So the gate check belongs on
# the word-changing pass, where tools/deshout.py `guard` enforces it by asserting
# logic tokens, numeric literals and code spans are preserved per changed line.
#
# No action outstanding.
