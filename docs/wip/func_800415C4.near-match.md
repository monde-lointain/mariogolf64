# func_800415C4 near-match (get_tile_attribute.c, S218)

> **CRACKED + BANKED S281** as `set_lod_grid_vertex` (0x800415C4), 173/173 byte-exact via a
> compiler-source fan-out. S218 `#local-alloc-qty-permutation` verdict REFUTED. Levers: same NON-VOID
> `s32` return + function-scope index temp as the sibling set_lod_tile_attribute, PLUS (3) the store
> macro must be a PLAIN `{ }` block NOT `do{}while(0)` — the loop notes double REG_N_REFS inside it and
> re-tier two regs via local-alloc.c:1587 qty_compare floor_log2; and (4) the `tb` two-statement address
> split `tb=(u8*)D_80185220+(sel<<8); dst=(GridVertex*)(tb+(k<<4))` reproduces the ROM `(base+sel8)+k16`
> association (one-statement form reassociates in expand). See memory do-while-doubles-reg-n-refs-qty-tier.
> The residual below is HISTORICAL.


STATUS: NEAR-MATCH, best score 10565. 173/173 instrs, identical mnemonics+order
(175/177 asm-differ rows). Residual = pure local-alloc register permutation in
the signed div-by-4 idiom, repeated in all 3 sub-tile blocks.
`#local-alloc-qty-permutation` WALL. base.c at `nonmatchings/func_800415C4/`.
PERMUTER CANDIDATE (pure regalloc, structure byte-identical in sequence) but
project history walls this class; corpus-mining is the real closer.

## What it is
The 16-byte GridVertex-copy SIBLING of func_80041878 (setter that does a full
`*dst=*src` copy instead of sh+3sb). `func_800415C4(s32 col, s32 row,
GridVertex* src)`. Even/even -> `set_direct_grid_vertex(col/2,row/2,src)`. Else
writes src into base + up/left edge-neighbor LOD cells, each parity-guarded and
gated by a `g_terrain_tile_lod_selector[tile*640]>=0` lookup.

## Levers that worked
- 16B `*dst=*src` -> lwl/lwr/swl/swr x4 (matches set_direct_grid_vertex).
- Helper MUST fold the copy into the `sel>=0` branch (void, no ptr return) or
  GCC materializes a NULL + extra beqz. Named local `dst` required or the
  D_80185220 hi/lo reloc re-emits before every store half (+48 instrs).
- sel/dst at FUNCTION scope (one allocno each): block-scope evicts src a2->t1
  (adds `move a2,t1`). Fn-scope 25405->12825, keeps src=a2/dst=a3, sw ra in the
  beqz delay.
- if/else with the ODD path as `then` (even = out-of-line else) gives target layout.
- `s32 t` per-block index steer: 10565 (best) vs 12825 without.

## New externs (beyond file head; GridVertex + set_direct_grid_vertex exist)
g_terrain_tile_lod_selector[] 0x801336A4 (stride 640); u8 D_80185220[] 0x80185220
(16B cells); u8 D_800BA9B0[][8] 0x800BA9B0 (2D form REQUIRED); u8 D_800BA9D0[]
0x800BA9D0 + u8 D_800BA9B4[] 0x800BA9B4 (each its OWN symbol; reloc is vs the
symbol, not base+offset).

## Root-caused pass
Same as func_80041878: gcc-2.7.2 local-alloc.c ties `(row/4)*8` to the dying
row-quotient reg (v0/v1) instead of a fresh a0 (target), driven by pre-alloc
emit order of the two independent div-by-4 idioms in `(row/4)*8 + col/4`.
Identical divergence in all 3 blocks. See func_80041878.near-match.md.
