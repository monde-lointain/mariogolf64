# func_80052384 near-match (S183) — base-pointer-hoist regalloc wall

`void func_80052384(void)` — for each of 8 rows (s0=0,200..1400): fill buf[18] from D_800C1435
strided by 10, func_8005B150(buf,out,18) (sort), then rank-store D_800C1435[out[i]*10+s0]=18-i.

## Near-match source (compiles; inner1 load matches, inner2 store hoists a base ptr)
```c
extern s8 D_800C1435[];
extern void func_8005B150(s32* buf, s32* out, s32 n);
void func_80052384(void) {
  s32 buf[18]; s32 out[18]; s32 s0; s32 i;
  for (s0 = 0; s0 < 1600; s0 += 200) {
    s32 idx = s0;
    for (i = 0; i < 18; i++) { buf[i] = D_800C1435[idx]; idx += 10; }
    func_8005B150(buf, out, 18);
    for (i = 0; i < 18; i++) { D_800C1435[out[i] * 10 + s0] = 18 - i; }
  }
}
```

## Residual (52 mine vs 50 target; +8 frame -0xB8 vs -0xB0, +1 saved reg s3)
- Mine HOISTS `&D_800C1435` into callee-saved `s3` (lui+addiu, saved) used ONLY in inner2's store
  (`addu v0,v0,s3; sb 0(v0)`). Target COPIES row base `s0->a3` (caller-saved) + RE-MATERIALIZES
  `lui %hi; addu; sb %lo` in inner2 (like inner1's load, which already matches).
- The s3 hoist + 2-instr shift cascades a reg permutation (profile-probe -O2 diff = 31).

## Ruled out
- array decl `D_800C1435[]` vs `(&scalar)[]`: no change (still hoists).
- store-index operand swap `s0 + out[i]*10`: no change.
- explicit `row=s0` local for inner2: WORSE (53, extra copy + still hoists).
- profile-probe (TU-wide): -O2 confirmed best; O1/O0/-g/-fdelayed-branch all worse. NOT a flag artifact.
- permuter (--main, base 850 -> best 250, ~4k iters): did not reach 0.

## Retry ideas
- Prevent loop.c hoisting &D_800C1435 into s3 (force re-materialize in inner2's store): a source form
  that makes the row base a caller-saved copy (a3) like the target, without the extra copy instr.
- Longer permuter run; or #cross-project-matched-corpus-mining for the inner2 store idiom.
