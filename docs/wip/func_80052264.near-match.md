# func_80052264 near-match (S183) — 1-register regalloc wall

Logic 100% proven (m2c-confirmed + full asm analysis). Table index matched via the
`#register-reuse-nudge` array-index `+` operand-order lever. Hazard nops (mfhi->mult) correct
(KMC-as inserts them; interlocks=0). Residual = ONE transient scratch register only.

## Near-match source (byte-exact EXCEPT the divide-1 mfhi scratch reg)
```c
s32 func_80052264(s32 arg0, s32 arg1, s32 arg2) {  /* arg0 unused (dead a0) */
  return -arg2 / 18 -
         (arg2 % 18 >= (&D_800C1435)[func_80051FCC() * 200 + arg1 * 10]);
}
```
(signed /18 magic 0x38E38E39 sra 2; table stride: R*200 + arg1*10; D_800C1435 = s8 base.)

## Residual (only real diff, all else byte-exact incl the 2 mfhi->mult nops)
- word 8: target `mfhi t0`($8)  vs mine `mfhi a0`($4)
- word 17: target `sra s1,t0,2` vs mine `sra s1,a0,2` (cascade of word 8)

The transient mfhi-hi qty (used once by the sra->s1) and the post-jal `arg1*10` qty compete for a0;
mine's mfhi-scratch grabs a0 first (find_free_reg ascending, no REG_ALLOC_ORDER; mfhi-hi has short
life => high qty_compare priority => allocated first => lowest free = a0). Target's build let
`arg1*10` take a0 first, pushing the mfhi-hi to t0. A general-priority coin-flip.

## Ruled out
- 4th unused param (arg3): does NOT reserve $7 (GCC frees dead-on-entry param).
- rem-as-local: shifts the whole schedule (much worse).
- permuter (--main, 2 bases: pre-swap base=320->best 10; post-swap base=10->no improvement,
  full run): plateaus at exactly this register. UNREACHABLE via source transforms.

## Retry ideas
- #cross-project-matched-corpus-mining (sibling KMC 2.7.2 decomps: an idiom that makes the
  post-jal arg1*10 qty win a0 before the mfhi-hi qty).
- A compiler-config/patchlevel confirmation.
