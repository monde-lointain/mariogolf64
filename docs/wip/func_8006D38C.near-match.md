# func_8006D38C — near-match (base-register-vs-displacement wall)

**Subseg:** `src/main/func_8006A2C0` (`src/main/func_8006A2C0.c`)
**Isolated score:** 10620 (`decomp_loop --profile main`); structurally byte-exact except the base-register class below.
**Seed:** `nonmatchings/func_8006D38C/base.c` (the faithful C is reproduced below).

## What the function does
Min-search over a 0xB8-stride struct array (field at +4 = `D_801B7254`), skipping `-1` entries, with a
tie flag: two equal minima set the result index `D_800C4144 = -1`. Then a weighted u16-table
redistribute over `D_8012D3CA` (14-byte rows = 7 u16 each): the winner row's first u16 gets
`+= D_800C4134 * (count-1)`, every other row's first u16 gets `-= D_800C4134`. `count = D_801B6090`,
`n = D_801B60BB` (s8). On a tie/no-winner, `D_800C4134 += n`; on a winner, `D_800C4134 = n`.

## Faithful C (byte-exact except the wall)
```c
void func_8006D38C(void) {
    s32 best, i, off, v, idx, j;
    if (D_801B60BB <= 0) return;
    best = 0x3FFFFFFF;
    D_800C4144 = -1;
    if (D_801B6090 != 0) {
        off = 0;
        for (i = 0; i != D_801B6090; i++) {
            v = *(s32*)((u8*)&D_801B7254 + off);   // byte-offset lever: matches ROM's lui;addu;lw %lo(SYM)(reg)
            if (v != -1) {
                if (v < best) { D_800C4144 = i; best = v; }
                else if (v == best) { D_800C4144 = -1; }
            }
            off += 0xB8;
        }
    }
    idx = D_800C4144;
    if (idx == -1) {
        D_800C4134 = D_800C4134 + D_801B60BB;
    } else {
        *(u16*)((u8*)&D_8012D3CA + idx * 14) += D_800C4134 * (D_801B6090 - 1);
        if (D_801B6090 != 0)
            for (j = 0; j != D_801B6090; j++)
                if (j != idx) *(u16*)((u8*)&D_8012D3CA + j * 14) -= D_800C4134;
        D_800C4134 = D_801B60BB;
    }
}
```
Externs added: `s8 D_801B60BB; s32 D_801B6090; u8 D_801B7254[]; s32 D_800C4144; s32 D_800C4134; u16 D_8012D3CA[];`

## The single residual divergence (base-register-vs-displacement, [#base-register-vs-displacement])
The array walk matches (byte-offset-cast lever landed). The residual is on the SCALAR globals:

| access | ROM (target) | my gcc |
|---|---|---|
| `n = D_801B60BB` | `lui a0;addiu a0,%lo; lb 0(a0)` (materialize full &sym into a0) | `lui v0; lb %lo(v0)` (fold %lo into load) |
| count loop-bound | `lw t0,-0x2B(a0)` (reuse a0=&D_801B60BB, disp -0x2B → D_801B6090) | `move t0,v1` (reuse guard load) |
| inner-loop bound re-read | `lw v0,0(a3)` (hold a3=&D_801B6090) | `lui v0; lw %lo` (re-materialize) |

Because a0 is consumed by the held base in the ROM, the whole loop's register coloring cascades:
ROM has counter=a2 / offset=a0 / best=a1; my build has counter=a0 / offset=a1 / best=a2. That coloring
swap is the bulk of the diff and is a direct consequence of the base-materialization decision, not
independently fixable.

## Root cause (gcc-2.7.2, cited)
`config/mips/mips.h` `GO_IF_LEGITIMATE_ADDRESS` **:2318-2349** accepts `(plus reg CONSTANT_ADDRESS)`
as a legitimate address: gcc folds `%lo(sym)` into the mem displacement over an index/base reg and
emits a fresh `lui %hi(sym)` per access. The in-source comment **mips.h:2325-2335** is explicit:
"the assembler can use $r1 to load just the high 16 bits ... whereas the compiler generates a 4
instruction sequence. On the other hand, **CSE is not as effective**. It would be a win to generate
the lui directly, but the MIPS assembler does not have syntax to generate the appropriate relocation."
So from faithful C referencing these as file-scope `D_` globals, gcc never CSE-shares the `%hi(sym)`
into one base register held across the nearby-symbol accesses; the ROM's opposite choice
(materialize full `&D_801B60BB`, reach `D_801B6090` via numeric `-0x2B` displacement, hold
`&D_801B6090` across the inner loop) is an addressing-mode + allocno-coloring artifact.

## Why no source form reaches it (probes run)
1. Plain globals (the faithful C above) → 10620, coloring counter=a0/offset=a1/best=a2.
2. `u8 *p=(u8*)&D_801B60BB; *(s8*)p` for n + `*(s32*)(p-0x2B)` for count → 11160; gcc constant-folds
   `&sym±k` back to a single symbol+addend relocated access (`lui a3;lw -43(a3)` with its OWN %hi),
   never sharing the base with the n load, and adds an extra beqz.
3. Struct-anchored at count (`u8 *h=(u8*)&D_801B6090; *(s8*)(h+0x2B)` n, `*(s32*)h` count) → 10620,
   same coloring; still one `lui` per access.

All three collapse `&sym±const` to an independent relocated load (the mips.h:2343-2349 folded path),
so the shared-numeric-displacement base is unreachable. Consistent with the hazard doc: the permuter
does not flip this (S210 ~9500 iters, no crack) — it is an addressing-mode/coloring decision, not a
source-reachable permutation. Score is far below the 0.97 permuter threshold and the divergence is not
permuter-reachable regardless.

**Verdict: WALL** (base-register-vs-displacement). Carry the faithful C above.

## S259 UPDATE — 2 of the 3 access shapes are source-reachable

Build is now **84/84 instructions** (exact); the residual is a register
permutation plus the one addressing shape below.

| access | ROM | S241 verdict | S259 |
| ------ | --- | ------------ | ---- |
| `n = D_801B60BB` | `lui a0;addiu a0;lb 0(a0)` | unreachable | **FIXED** — hold it in a pointer local `s8* flag = &D_801B60BB;` and read `*flag` |
| inner-loop bound re-read | `lw v0,0(a3)` (holds `&D_801B6090`) | unreachable | **FIXED** — read it as an ARRAY element (`extern s32 D_801B6090[]` + `D_801B6090[0]`); MEM_IN_STRUCT_P defeats the CSE that folded it to a single load |
| count loop-bound | `lw t0,-0x2B(a0)` | unreachable | still open — see below |

The inner loop must also be a `do`-`while`. The ROM has only ONE zero-guard (the
`if (D_801B6090 != 0)`), so a top-tested `for` emits a second `beqz`, while a
`for` over a cached `count` variable lets CSE fold the bound into the guard's
load (2 instrs short).

### The one shape still unreached

The ROM reaches `D_801B6090` by NEGATIVE DISPLACEMENT off the held
`&D_801B60BB` base (`-0x2B`), which is what makes `a0` live across the guard and
cascades the loop colouring. Spelling the bound `*(s32*)(flag - 0x2B)` is CSE'd
against the array-form guard read (84 -> 82 instrs); keeping the two spellings
distinct restores the count but gcc still emits a fresh `lui` rather than
reusing the base. Measured: array-form winner update (`D_8012D3CA[idx * 7]`) and
a hoisted `delta` in the else-loop are both NEUTRAL (150 rows either way);
reading `D_801B60BB` directly at the tail instead of through `flag` costs +1.

### Best reconstruction (S259, 84/84)

```c
void func_8006D38C(void) {
  s8* flag = &D_801B60BB;
  s32 best;
  s32 count;
  s32 i;
  s32 off;
  s32 v;
  s32 idx;
  s32 j;

  if (*flag <= 0) {
    return;
  }
  best = 0x3FFFFFFF;
  D_800C4144 = -1;
  if (D_801B6090[0] != 0) {
    i = 0;
    off = 0;
    do {
      v = *(s32*)((u8*)&D_801B7254 + off);
      if (v != -1) {
        if (v < best) {
          D_800C4144 = i;
          best = v;
        } else if (v == best) {
          D_800C4144 = -1;
        }
      }
      i++;
      off += 0xB8;
    } while (i != D_801B6090[0]);
  }
  idx = D_800C4144;
  if (idx == -1) {
    D_800C4134 = D_800C4134 + *flag;
  } else {
    *(u16*)((u8*)&D_8012D3CA + idx * 14) += D_800C4134 * (D_801B6090[0] - 1);
    if (D_801B6090[0] != 0) {
      for (j = 0; j != D_801B6090[0]; j++) {
        if (j != idx) {
          *(u16*)((u8*)&D_8012D3CA + j * 14) -= D_800C4134;
        }
      }
    }
    D_800C4134 = *flag;
  }
}
```

Externs: `s8 D_801B60BB; s32 D_801B6090[]; u8 D_801B7254[]; s32 D_800C4144;
s32 D_800C4134; u16 D_8012D3CA[];`
